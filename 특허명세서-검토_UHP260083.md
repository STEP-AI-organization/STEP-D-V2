# 특허명세서 검토 — UHP260083 (전사 데이터 기반 영상 클립 생성 장치 및 방법)

> 원문 직접 수정 없음. 검토 의견만 정리.
> 세 갈래: ① 잘못된 사항(고쳐야 함) · ② 업데이트할 부분(실제 구현과 정합) · ③ 더 생각해볼 부분(전략·청구 강화).
> ※ 청구범위 전략은 변리사 확인 필요. 아래는 발명자 관점의 사실·정합성 지적 위주.

---

## ① 잘못된 사항 (명백한 오류 — 출원 전 반드시 수정)

### 1-1. 도면부호 오타 `30ssssssssss3`
- 위치: 【과제의 해결 수단】문단 — "복수의 배포 채널(301, 302, **30ssssssssss3**) 각각에 대하여…"
- `303`의 오타. 문자열이 깨져 들어감. **즉시 `303`으로 수정.**

### 1-2. 배포 채널별 파라미터 도면부호가 두 세트로 혼용 (91/92/93 ↔ 1141/1142/1143)
- 【도 11】첫 문단: "제1 점수의 부여 기준(**1141**), 선정 구간의 길이(**1142**) 및 자막의 합성에 이용되는 설정 정보(**1143**)"
- 같은 도 11 뒷 문단: "제1 점수의 부여 기준(**91**), 선정 구간의 길이(**92**) 및 자막의 합성에 이용되는 설정 정보(**93**)"
- **동일 대상에 부호 두 개**가 붙어 있음. 도면과 대조해 **하나로 통일** 필요(70·80번대 체계와 맞추려면 91/92/93 권장). 도 7·도 10에서도 "설정 정보(1143)"/"설정 정보(93)"가 섞여 인용되므로 명세서 전역 검색해 일괄 교체.

### 1-3. 깨진 문자 (인코딩 오류 의심)
- 【도 5】동작 S540 문단: "변경된 시작 시점 **이OOO** 및" — `이전`이 깨진 것으로 보임(추출 아티팩트일 수도 있으니 원본 docx에서 해당 위치 확인).
- 문서 전체를 한 번 훑어 깨진 글자·이중 공백 점검 권장.

### 1-4. 도 11 동작번호 누락
- 도 11 설명이 업로드 동작 서술 후 곧바로 **`동작 S1120`부터 시작** — `S1110`(클립 업로드/예약 업로드) 번호가 라벨 없이 지나감. 순서도(도면)와 본문 동작번호가 어긋나지 않는지 대조 필요.

### 1-5. 최종 점수 식의 가중치 명명이 직관과 반대
- 본문: `최종 점수 = 제2 점수 × 제1 가중치 + 제1 점수 × 제2 가중치` (제1 가중치=0.70이 **제2 점수**에, 제2 가중치=0.30이 **제1 점수**에)
- 오류는 아니지만 "제1 가중치가 제1 점수에 곱해진다"는 오독을 유발. 심사관·독자 혼동 방지 위해 명명을 바꾸거나(예: 시각 가중치/텍스트 가중치) 문장에서 대응관계를 명시 권장.

---

## ② 업데이트할 부분 (최근 구현과 정합 — 반영하면 실시예·권리 보강)

### 2-1. 캐스트(출연자) 레지스트리 — 명세서에 아예 없음
- 실제로 구현된 핵심 차별화(이름표 OCR → 출연자별 등장 타임라인)가 청구항·실시예 어디에도 없음.
- 도 9(PPL)는 **브랜드/상품**만 다루고 **인물(출연자)** 타임라인은 별개 기능. "전사 데이터 + 화면 내 이름표(하단 자막) 인식을 교차하여 출연자별 등장 구간을 결정"하는 실시예/종속항을 추가하면 실구현과 정합되고 경쟁사(OpusClip 등) 대비 차별점이 명세서에 남음.

### 2-2. 채널별 생성 = 현재는 "다른 순위/프리셋"이지 "다른 경계의 재합성"까지는 미구현
- 청구항 10/20·도 11(92)·도 13은 "배포 채널별로 **상이한 선정 구간·상이한 클립**"까지 폭넓게 청구(범위 확보는 좋음).
- 다만 실제 구현은 **동일 후보를 채널별로 랭킹만 다르게 + 렌더 프리셋(Shorts 9:16/60s·Reels 9:16/90s·SMR 16:9)** 적용 단계. "경계는 동일하고 순위/프리셋만 채널별로 다른" **저비용 변형**을 별도 실시예로 명시해두면(LLM 재호출 없이) 실구현과 청구가 어긋나지 않음.

### 2-3. 전사 데이터의 "단일 테이블 영속 + 단계별 체크포인트 재개"
- 실구현 핵심: 전사/분석 결과를 단일 스토리지에 영속하고, 실패 시 완료 단계부터 재개.
- 명세서는 "메모리(120)에 저장 + 작업 큐/재시도"로만 언급. **"단계별 체크포인트를 저장하고 재시도 시 완료 단계부터 재개"**를 실시예/종속항으로 구체화 가치(신규성·진보성 있는 독립 특징).

### 2-4. 성과 환류(도 11)의 신호를 구체화
- 실구현은 배포 채널 성과 중 **리텐션 커브(초 단위 시청 이탈)**를 수집·활용. 명세서는 "성과 지표(조회수·좋아요·시청 유지 정도)"로 포괄.
- "시청이 유지된 구간(리텐션 커브)을 학습 신호로 하여 제1 점수의 부여 기준을 갱신"하는 실시예를 넣으면 폐루프(자기 개선) 청구가 더 견고.

### 2-5. 대표 수치의 실구현 근접
- 선정 구간 길이 "20~75초, 38초 선호"는 실제 프리셋(Shorts 60s·Reels 90s·SMR 180s)과 상이. 전부 "예를 들어"로 한정 회피돼 법적 문제는 없으나, 대표 실시예 수치를 실제 운영값에 근접시키면 일관성↑. (appeal 1–5 스케일 등도 마찬가지)

---

## ③ 더 생각해볼 부분 (전략·청구 강화 — 변리사 상의)

### 3-1. 독립항 구성
- 현재 독립항은 장치(청구항 1)·방법(청구항 11) 2개. 한국 실무상 **"컴퓨터로 읽을 수 있는 기록매체/컴퓨터프로그램" 독립항** 추가를 흔히 함(말미에 관련 기재는 있음). 추가 검토.

### 3-2. 청구항 1의 요소 밀도
- 청구항 1이 (전사 1회 → 표현사전 대조/제1점수 → 경계정제/확정구간 → 선별·VLM/제2점수 → 최종점수 → 리프레이밍·자막합성)까지 **한 항에 다 담아** 권리범위가 좁아질 수 있음.
- 핵심(전사 1회 공용 + 경계정제 + 저비용선별→고비용평가 2단계 스코어링)만 독립항에 남기고 리프레이밍/자막합성을 종속항으로 내리는 안 검토. (심사 전략이라 변리사 판단 영역)

### 3-3. PTS 기반 시간 통일 — 종속항 승격 가치
- 29.97fps 드롭프레임 환경에서 PTS로 시간축을 통일한다는 특징은 강한 기술적 차별점인데 **실시예에만 있고 청구항엔 없음.** 방어 가치 있으면 종속항으로.

### 3-4. 프록시 영상(판단=저해상 / 실행=원본) — 종속항 후보
- 도 12의 "분석은 프록시, 렌더는 원본, 시간축은 PTS로 대응" 구조도 청구항 미반영. 비용 상수화 논리와 함께 종속항화 검토.

### 3-5. 비전-언어 모델의 비결정성 서술
- 명세서가 "제2 점수는 실행마다 편차"를 스스로 인정하고 제1 점수로 재현성을 보정한다고 서술 — 논리는 좋음. 다만 심사관이 "비결정 요소 포함"을 문제삼을 여지가 있으니, **결정론적 부분(표현사전·경계정제·PTS)**을 전면에 두는 현재 기조 유지가 방어적.

### 3-6. 용어·범위 일관성 최종 점검
- "대상 영상 / 원본 영상 / 프록시 영상" 3자 관계가 도 12에서 교차 정의됨 — 청구항 1의 "대상 영상"이 프록시인지 원본인지 모호해지지 않게 용어 정의 문단 추가 검토.
- 후보 구간(20)·탐색 구간(30)·확정 구간(40)·선정 구간(50) 4단계 명칭은 일관적(양호).

---

## 우선순위 요약
- **즉시(오류):** 1-1 오타 `303`, 1-2 부호 통일(91/92/93), 1-3 깨진 글자, 1-4 동작번호 대조.
- **출원 전 반영 권장:** 2-1 캐스트 레지스트리 실시예/청구, 2-3 체크포인트 재개.
- **변리사 상의:** 3-1 매체 독립항, 3-2 청구항 1 분해, 3-3/3-4 PTS·프록시 종속항 승격.
