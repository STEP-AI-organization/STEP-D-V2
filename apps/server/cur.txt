  ✔ #rrggbb → 정수, 못 읽으면 흰색 
  ✔ #Shorts 보장은 제목 해시태그가 0인 채널에는 적용되지 않는다 
  ✔ **coupa.ng 도 막는다** — 콘솔 미리보기 iframe 이 클릭으로 집계될 수 있다 
  ✔ **status 가 아예 없는 옛 링크도 안 붙는다** — 모르는 건 안 내보낸다 
  ✔ **그 원본의** 타임라인을 연다 — 열려 있는 남의 시퀀스를 쓰지 않는다 
  ✔ **날짜 경계** — UTC 로는 전날인 순간도 KST 날짜로 나온다 
  ✔ **비번이 틀리면 자격증명을 지운다** — 반복 시도가 계정을 잠근다 
  ✔ **한 번만** 소비된다 — 안 지우면 폴링할 때마다 같은 회차로 계속 끌려간다 
  ✔ /api/media/from-youtube 는 잔액 0 이하를 402 로 거부한다 
  ✔ /export 가 실제로 내는 코드·문구를 분류가 알고 있다 
  ✔ /export 라우트가 그 헬퍼를 실제로 쓴다 — 산수가 두 벌이 되면 테스트가 헛돈다 
  ✔ /tmp 를 반드시 지운다 — Cloud Run 의 /tmp 는 RAM 이라 쌓이면 OOM 이다 
  ✔ __name shim 을 **문자열** evaluate 로 심는다 (문자열은 변환을 안 거친다) 
  ✔ `..` 는 해석하지 않고 거부한다 
  ✔ `.hidden` 이 ID 선택자를 이긴다 
  ✔ `exportFull=true` 를 넘긴다 — 빼면 작업 영역만 나가 '앞부분만 올라갔다' 가 된다 
  ✔ ⊘ 전체 실패 처리 금지 — 한 건이 막혀도 나머지는 나간다 
  ✔ ⊘ 조용한 제외 금지 — 제외된 건은 전부 사유와 코드를 갖는다 
  ✔ ⚠️ response_schema 를 쓰지 않는다 — 프롬프트가 형식을 직접 말한다 
  ✔ 0 과 빈 값은 막는다 
  ✔ 0 은 허용한다 — '유예 없음'은 의도할 수 있는 선택이다 
  ✔ 0·음수·NaN 은 0 (없는 사용량을 만들지 않는다) 
  ✔ 0·음수·비숫자는 기본값으로 되돌린다 — 상한 없음으로 해석되면 안 된다 
  ✔ 0·음수·소수·빈값·쓰레기는 undefined (서버가 MAX+1 로 매김) 
  ✔ 0·음수·쓰레기는 0 으로 
  ✔ ① 폴링 창 — 오래된 예약을 영원히 조회하지 않는다 
  ✔ 1080p 상한 · 짝수 해상도 보정 · yuv420p — 플랫폼이 받는 모양으로 고정 
  ✔ 16:9 클립은 해시태그를 좁게(5~8개) 유지한다 — 롱폼 추천은 다른 축이다 
  ✔ 1분 안에 다시 오면 쓰지 않는다 
  ✔ ② 채널별 그룹핑 + 배치 — 예약 영상은 소유자 토큰이라야 상태가 보인다 
  ✔ 2MB 초과 후보는 건너뛰고 다음 것을 쓴다 
  ✔ ③ 확정 신호일 때만 전환 — 조회 실패·private 는 손대지 않는다 
  ✔ 30초짜리도 1분으로 센다 (파이프라인은 한 번 다 돈다) 
  ✔ 3분짜리 구간을 60초 배포처로 내면 60초로 잘리고 그 사실을 돌려준다 
  ✔ 3회째에 확정한다 — 15분 주기 × 30분 벨트에서 **실제로 닿는** 값 
  ✔ ④ 배치 단위 로그 — 건별 로그는 quota 초과 시 폭증한다 
  ✔ 401 이면 재로그인 후 **한 번만** 재시도한다 (비밀번호가 바뀌면 로그인도 401 → 루프 방지) 
  ✔ 5-값 enum 의 채움·사각형 숫자가 실제로 고정돼 있다 
  ✔ accepts beat-shaped plans and builds bounded piecewise tracking 
  ✔ active 는 통과한다 
  ✔ AENA가 finalize 직후 ingest해도 duration=0 placeholder는 분석하지 않는다 
  ✔ AI 가 한글 #쇼츠 를 뽑아도 표준 #Shorts 하나로 통일된다 (중복 없음) 
  ✔ align → mAlignment (0 왼쪽 · 1 가운데 · 2 오른쪽) 
  ✔ ALL_LANE_TYPES 는 머신 전용 레인(gebd·naver·download)을 포함하지 않는다 
  ✔ all_sent — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ allows an early queue even when the activity window starts at the target time 
  ✔ AME 큐가 아니라 즉시 렌더다 — 큐에 넘기면 언제 끝났는지 몰라 '딸깍' 이 성립하지 않는다 
  ✔ analysis/{id}/… 를 조립하는 라우트는 전부 getMedia 를 부른다 
  ✔ analysis_blocked — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ analysis_failed — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ analyzing — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ API 이름이 어긋나면 실제 모듈 구성을 콘솔에 쏟는다 — 다음 시도를 추측으로 하지 않으려고 
  ✔ api_keys 경로는 시스템 스코프를 명시한다 
  ✔ api-reference.md 의 라우트 수 
  ✔ app/api/proxy/[[...path]]/route.ts 가 목록을 실제로 적용한다 
  ✔ app/api/render-proxy/[[...path]]/route.ts 가 목록을 실제로 적용한다 
  ✔ approve_first 는 승인 없이 안 나간다 
  ✔ apps/web 의 roles.ts 와 권한 값이 일치한다 
  ✔ ASS_FS_PER_CSS_PX == (winAscent+winDescent)/unitsPerEm (설치 폰트 실측) 
  ✔ Authorization 헤더에서 키만 뽑는다 
  ✔ auto 접두 actor 는 자동 충전으로 분류된다 
  ✔ autoEditorState 가 titleLine1·titleLine2 를 둘 다 읽는다 
  ✔ automation-cycle 는 approved 에 released 기반 값을 넘긴다 (!held 금지) 
  ✔ automation-cycle.ts 의 export 요청이 body 로 channel 을 싣는다 
  ✔ automation-cycle.ts 이 대상 채널을 autoRenderChannel 로 정한다 
  ✔ auto-topup.ts — 자동 충전·미정산 정산이 발송을 부른다 
  ✔ baseline 이 top 이 아니면 보정하지 않는다 — 이미 기준선이다 
  ✔ basename 
  ✔ blockedReason 이 있으면 blocked — 업로드 경로(index.ts)가 남기는 모양 
  ✔ blocks export before cache lookup unless the current AI plan is ready 
  ✔ body 를 통째로 흘리지 않는다 — 빌링키·PG 내부 필드가 화면에 새면 안 된다 
  ✔ brandIcon 과 다르면 보존한다 — 사람이 고른 값이다 
  ✔ brandIcon 을 가진 프로그램이 하나도 없으면 아무것도 안 건드린다 
  ✔ brandIcon 이 없는 프로그램(포스터 시드)은 보존한다 
  ✔ buildEditorAss 가 staticToPng 로 정적 제목·채널명을 ASS 에서 뺀다 (이중 그리기 방지) 
  ✔ cap 을 넘으면 샷 경계부터 버린다 — 분위수·전환 지점이 우선이다 
  ✔ CAPTION_CHUNK_MAX_CHARS · CAPTION_CHUNK_MIN_SEC 가 1:1 
  ✔ captionAssStyle 이 고른 글꼴을 그대로 쓴다 — 별칭을 쓰면 조용히 폴백된다 
  ✔ channel_rule — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ chargeWithBillingKey 는 빌링 채널키로 결제한다 — 일반결제 채널이면 빌링키를 못 찾는다 
  ✔ claim 은 실패시키지 않고 건너뛴다 
  ✔ claim 이 회사 상태로 거른다 
  ✔ claimJobInner 를 찾는다 
  ✔ clamps tiny planner boundary drift to the canonical clip range 
  ✔ classifies the latest rendered landscape/portrait bytes in both directions 
  ✔ CLAUDE.md 의 라우트 수·잡 종류 
  ✔ commerce.link 잡이 도는 레인에 있다 
  ✔ compareId 는 fingerprint 형식(24 hex)만 통과한다 — 경로 세그먼트라 형식이 곧 방어다 
  ✔ computes Fit windows as the complement of Fill segments 
  ✔ connection·content-length 는 목록에 없다 — 프록시가 따로 다룬다 
  ✔ creditBalance 는 credit_ledger 의 SUM(delta) 다 — 캐시 컬럼이 아니다 
  ✔ crop-main/crop-sub/letterbox/crop-full 이 verbatim 반환된다 
  ✔ daily_cap 은 KST 날짜가 바뀌면 만료 — 같은 날이면 유효 
  ✔ dailyQuota 를 표시용으로 직접 읽지 않는다 — 슬롯 계획에서 틀린 수가 된다 
  ✔ date(1082)·timestamp(1114) 는 건드리지 않는다 — 날짜 전용 컬럼이다 
  ✔ DB 도 숫자 10자리만 받는다 
  ✔ db-pg 가 파서를 설치한다 
  ✔ db-pg 왕복 — SELECT·INSERT·UPDATE(id) 셋 다 orientation·reframe 를 안다 
  ✔ dedupe 로 눌린 줄은 '말했다'가 아니다 — 대신 상태가 유휴 판정에 들어간다 
  ✔ dedupe 키는 회사·nonce 로 갈린다 
  ✔ destroyTenantSessions 는 superadmin 세션을 남긴다 
  ✔ disconnected 는 안 돈다 — 사람이 끊은 채널은 절대 성공하지 않는다 
  ✔ drain 기동에서 팬아웃된다 (프로덕션은 drain 이다) 
  ✔ editor-panel Swatches 가 <input type="color"> 를 품는다 
  ✔ editor-preview 가 getAspectPreset 으로 사각형을 %로 배치한다 
  ✔ editorState 의 다른 값은 건드리지 않는다 
  ✔ editorState.aspect 를 clip.aspectRatio(5-값 enum)와 일치시킨다 — /export 는 editorState 를 최우선으로 읽는다 
  ✔ ensureMaster 는 **파일명만** 돌려준다 — 다운로드 대기를 건너는 자리라 객체를 못 들고 나온다 
  ✔ evaluate 콜백 안에 이름 붙는 내부 함수를 만들지 않는다 
  ✔ factory 라우트는 factory 스코프로 열린다 
  ✔ factory.ts 의 export 요청이 body 로 channel 을 싣는다 
  ✔ factory.ts 이 대상 채널을 autoRenderChannel 로 정한다 
  ✔ factory: 길이 게이트와 채택이 factoryAspect 한 벌을 본다 
  ✔ factoryAspect — 숏폼만 세로 
  ✔ false 반환을 실패로 읽는다 — 안 보면 0바이트를 업로드하러 간다 
  ✔ ffmpeg renderShort 가 overlayPngPath 를 전체프레임 overlay=0:0 으로 합성한다 
  ✔ fingerprints planner inputs but ignores editor overlays and Beat order 
  ✔ font(패밀리 id)+stroke 를 준 아이템도 유효한 PNG 로 렌더된다 
  ✔ frame 방식은 인물 등록·narrative 를 요구하지 않는다 
  ✔ gate_off — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ geminiGenerate 가 schema 호출에서 thinkingBudget=0 을 보낸다 
  ✔ getRawPool() 호출과 같은 문장에 RLS 표가 오지 않는다 
  ✔ hashtagTemplate 의 {program}·{episode} 가 치환돼 설명에 들어간다 
  ✔ headless 로 띄우지 않는다 — 세션이 유효해도 차단된다 
  ✔ held_waiting — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ hevc 는 변환한다 — 브라우저·플랫폼 지원이 갈린다 
  ✔ holdClip 재보류는 released_at·released_by 를 NULL 로 되돌린다 
  ✔ http·타 도메인·공백·스크립트는 거부한다 
  ✔ id 가 겹치지 않는다 
  ✔ id 가 다르면 거부 (재전송 방어) 
  ✔ id 를 손으로 주는 경로는 남아 있다 (마이그레이션·픽스처용) 
  ✔ id 를 트랜잭션 안에서 매긴다 
  ✔ index 가 정적 오버레이 아이템을 만들어 PNG 로 렌더하고 renderShort 에 넘긴다 
  ✔ index.ts — 웹훅·저장카드 충전이 발송을 부른다 
  ✔ isShort 미지정이면 쇼츠로 본다 — 자동배포 기본 산출물이 쇼츠 
  ✔ kind 는 모르면 api 로 떨어진다 
  ✔ kind_mismatch — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ KST 경계에서 요일이 갈린다 — UTC 로 읽으면 월요일 아침이 일요일이 된다 
  ✔ kstDay 이전에 저장된 알림은 lastAt 으로 판정한다 — 배포 순간의 기존 알림도 늙는다 
  ✔ LayoutState 의 모든 키가 POST /api/automation/rules 화이트리스트에 등장한다 
  ✔ marks a ready plan stale when trim inputs change 
  ✔ media.prepare가 길이와 운영 경로를 채우면 공장 분석을 진행할 수 있다 
  ✔ member 는 관리 권한이 없다 — 작업은 같이 하되 조직은 못 바꾼다 
  ✔ meta_waiting — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ methods[].card 에서 발급사와 마스킹 끝 4자리를 뽑는다 
  ✔ mogrt 가 막히면 PNG 로 물러난다 — 제목이 아예 안 나오는 것보다 낫다 
  ✔ mogrt 가 아니면 던진다 — 받아 두면 나중에 조용히 실패한다 
  ✔ mogrt 는 **줄마다 editor 를 다시 얻는다** — 앞 삽입이 뒤를 무효로 만든다 
  ✔ mogrt·PNG 두 경로가 **같은 줄**을 쓴다 — 줄 목록 수집은 한 군데다 
  ✔ monthly_cap 은 달이 바뀌어야 만료 — 같은 달 다른 날은 유효 
  ✔ monthUsageCredits 가 usage + publish 를 세고 환급을 자연 차감한다 
  ✔ Motion 의 위치·배율을 **한 트랜잭션**으로 건다 — 되돌리기가 두 번이 되면 안 된다 
  ✔ MXF 컨테이너는 변환한다 
  ✔ naver.login 이 도는 레인에 있다 
  ✔ net-zero: 옛 size×scale(옛 렌더) == size×계수(마이그레이션) → 렌더가 그대로 쓴 출력 px 
  ✔ no_episode — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ no_pending — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ normalizes core beats into the renderer plan and preserves tracking 
  ✔ normalizeStatus — 모르는 값은 전부 pending 
  ✔ OFF 면 productQueries 지시가 한 글자도 없다 
  ✔ off_day — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ off_hours — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ ON 이면 지시와 출력 필드가 함께 들어간다 
  ✔ overflow-hidden 은 남아 있어야 한다 (장식이 아니라 스테이지 축소본을 자르는 기능) 
  ✔ overlap — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ overlay-canvas 가 텍스트 레이어 PNG 렌더러를 export 하고 ASS 와 같은 폰트를 등록한다 
  ✔ owner 급은 owner 와 superadmin 뿐 — admin 은 owner 를 건드릴 수 없다 
  ✔ owner 라고 봐주지 않는다 
  ✔ owner 이메일이 없으면 회사를 안 만든다 
  ✔ owner·admin·superadmin 만 동료를 초대·관리할 수 있다 
  ✔ PAID 가 아니면 올리지 않는다 
  ✔ PAID 인데 금액을 안 주면 통과 — 긁힌 결제를 금액 누락만으로 버리지 않는다 
  ✔ paymentId 에 랜덤을 쓰지 않는다 
  ✔ PCM 오디오는 변환한다 — mp4 로 copy 가 안 된다 
  ✔ PG 가 준 거절 사유(pgMessage)를 꺼낸다 — 사용자가 카드사에 물을 수 있어야 한다 
  ✔ PNG 와 **같은 입력**을 받는다 — 좌표는 출력 px, 결과는 0..1 
  ✔ PNG 재요청 키가 font 변화를 감지한다(에디터 <img> 가 갱신) 
  ✔ program.titlePrompt 가 서버 제목 경로 두 곳에 실제로 실린다 
  ✔ PUBLIC_URL 이 없으면 가짜 링크를 만들지 않는다 
  ✔ PUBLIC_URL 이 있으면 바로 보낼 수 있는 링크를 만든다 
  ✔ publish_failed — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ publishAt(scheduledSlotAt)의 index 는 발행 순번 — 같은 시각 여러 건은 같은 시각 
  ✔ published 는 어떤 조합에서도 초기 상태로 나오지 않는다 
  ✔ queues two hours before the target and preserves KST 
  ✔ quota_done — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ record 모드는 예약 여부와 무관하게 recorded 다 
  ✔ record 모드에서는 published·pending·scheduled 가 절대 나오지 않는다 
  ✔ rect 면 그 사각형에 cover, contain 이면 담기 — 배율·위치를 그렇게 계산한다 
  ✔ rejectHold 는 rejected_at 만 세운다 — released_at 을 건드리면 approve_first 가 뚫려 되레 게시된다 
  ✔ rejects unsafe or ambiguous planner output 
  ✔ render_stopped — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ render_waiting — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ renderDims 가 세로 enum 4종을 1080×1920 으로 매핑한다 
  ✔ renders a fit-fill-fit synthetic clip with audio and target dimensions 
  ✔ renderShort 의 밴드 크롭 ffmpeg 수식이 crop,scale,pad 형태다 (AENA 이식) 
  ✔ resolveSession 이 ops_role 을 조회한다 
  ✔ resolveSession 이 opsRole 을 반환 객체에 담는다 
  ✔ resolveSession 이 회사 상태를 읽고 판정한다 
  ✔ resolveTenant 가 키를 해석한다 
  ✔ revoked 도 안 돈다 (기존 동작 유지) 
  ✔ RLS 가 아닌 표를 잘못 걷지 않는다 
  ✔ RLS 표라 시스템 스코프로 읽는다 
  ✔ rule_id 가 있으면 `= $1` 로 인덱스를 탄다 
  ✔ rule_id 없는 워크스페이스 사유도 dedupe 된다 
  ✔ ruleId 로 계획 몫만 센다 
  ✔ runAsSystem 은 횡단 스코프를 준다 
  ✔ runContentAnalyze 가 시작 전에 checkCredits 로 판정한다 
  ✔ runWithTenant 안에서는 그 테넌트가 보인다 
  ✔ schema 없이 JSON 을 뽑는 호출부는 thinking:false 를 명시한다 
  ✔ score_blocked — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ screenForPublish 에 우회 인자가 없다 — 관리자도 통과시킬 자리가 없다 
  ✔ selectCandidates 와 순방 관측치가 같은 함수를 쓴다 
  ✔ settledAt 이 없으면 createdAt 으로 폴백한다 
  ✔ shim 설치가 첫 evaluate 보다 먼저 온다 
  ✔ SHORTFORM_MAX_SEC = core/recommend/recommend.py MAX_SHORT_SEC 
  ✔ SHORTS_RENDER_PRESETS 의 maxSec = index.ts RENDER_PRESETS 의 maxSec 
  ✔ SMTP 3종이 다 있어야 발송 경로가 열린다 
  ✔ sorts/clamps the plan and fills every uncovered range with fit 
  ✔ status 가 없으면 미결정이다 (기본값이 '판단됨'이면 안 된다) 
  ✔ status 만 adopted 이고 클립 id 가 없으면 채택으로 치지 않는다 
  ✔ status=all 로 받는다 — pending 만 받으면 작업할 게 사라진다 
  ✔ STEP D 세션 쿠키 이름이 쿠팡 필터에 걸리지 않는다 
  ✔ stroke 를 안 주면 템플릿 값을 그대로 둔다 
  ✔ SUBTITLE_DEFAULTS(y·size·color)를 읽을 수 있다 
  ✔ superadmin 은 자기 워크스페이스 안에서 owner 처럼 행동한다 
  ✔ superadmin 은 자기 회사가 정지돼도 안 막힌다 — 잠김 방지 
  ✔ suspended·closed 는 막고, 사유를 다르게 말한다 
  ✔ tiktok 다계정도 계정 정체성으로 갈린다 — openId 가 열쇠다 
  ✔ tiktok 은 게이트가 켜졌을 때만 실업로드(드래프트)다 — 기본은 record 
  ✔ timestamptz 만 바꾸고, 기본 파서 위에 얹는다 
  ✔ titlePrefix 가 제목 앞에 붙는다 
  ✔ titlePrompt(프로그램별 운영자 지시)가 실리고, 없으면 블록 자체가 빠진다 
  ✔ too_long — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ top3_cap — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ topup·usage 는 손으로 못 쓴다 
  ✔ TZ 없는 datetime-local 문자열에 +09:00 을 부여한다 
  ✔ undici 가 거부하는 헤더가 목록에 전부 있다 
  ✔ unwrapPayment — 감싸임/맨몸 양쪽에서 같은 결제 객체가 나온다 
  ✔ upload 모드만 차감한다 — record(기록만)는 실물이 안 나가므로 무과금 · 0이면 무과금 스위치 
  ✔ URL 로 제공자를 찾는다 
  ✔ uses atomic JSONB paths and request CAS rather than whole-entity worker writes 
  ✔ UTC 05:56 → KST 14:56 (+9시간) 
  ✔ UXP manifest v5 · Premiere 25.6+ (UXP 정식 지원 시작 버전) 
  ✔ v1, 접두 없이 와도 통과한다 
  ✔ vague_account — 개수가 달라도 문구는 글자 그대로 같다 
  ✔ variety·drama 만 통과 
  ✔ width 86% · padding 4(설계 px)×scale=출력 px 
  ✔ words 가 텍스트와 크게 어긋나면 신뢰하지 않고 비례배분으로 간다 
  ✔ worker 의 metaForChannel 이 withCommerceLinks 를 통과시킨다 
  ✔ WORKER_JOBS 를 레인 목록으로 파싱한다 (조합 문자열 하드코딩 금지) 
  ✔ worker-queue.md 의 잡 종류 
  ✔ YouTube 게이트와 독립이다 — 한쪽을 켜도 다른 쪽이 열리면 안 된다 
  ✔ YouTube 다운로드 중인 원본도 같은 ingest 대기 상태를 쓴다 
  ✔ YouTube 와 네이버 TV·클립이 실업로드다 
  ✔ YouTube 외 채널에는 붙지 않는다 — 확인된 채널부터 하나씩 
  ✔ YouTube 자격증명 요구·sweep 도 레인 이름으로 판정한다 
  ✔ youtube 잡이 없는 레인은 전부 YT_FREE_LANES 에 있다 
  ✔ youtube.ts 가 thumbnails.set 을 부를 수 있다 
  ✔ YYYY-MM 만 받는다 
  ✔ YYYY-MM-DD 는 그대로 통과 
  ✔ 가로 클립을 숏폼 전용 채널에 못 보낸다 
  ✔ 가로(롱폼)는 길어도 통과한다 — 클립은 원래 길다(최대 15분) 
  ✔ 가로·미지정은 세로가 아니다 — 롱폼에 숏폼 상한을 걸면 15분 클립이 조각난다 
  ✔ 가로는 null — 롱폼을 배포처 길이로 자르면 머리만 남는다 
  ✔ 가장 큰 번호 다음을 준다 
  ✔ 가져오기 대화상자를 띄우지 않는다 — 뜨면 자동 흐름이 사람을 기다리며 멈춘다 
  ✔ 각 라우트 핸들러가 인가 헬퍼를 부른다 
  ✔ 감사 기록에 만들어진 id 가 남는다 
  ✔ 감사 로그는 검색도 된다 
  ✔ 감싸인(billing-key) 응답을 언랩해 대조한다 
  ✔ 값 정규화 — 음수·비수치는 기본 5분, 0 은 즉시로 살린다 
  ✔ 값 체계는 수동 채택 다이얼로그와 동일하다 (orientation·reframe) 
  ✔ 강제 지정이 없으면 프로그램 기본 → 장르 자동 순서다 
  ✔ 강조 배경 위 글자도 라이트·다크 모두 AA 
  ✔ 같은 (날짜, 슬롯) → 항상 같은 값 — 재시도·동시 시도가 포트원 멱등키에서 충돌한다 
  ✔ 같은 결제는 같은 키 (웹훅 재전송 방어) 
  ✔ 같은 계정으로 다시 쓰면 그 항목만 갱신된다 
  ✔ 같은 구간(완전 일치·대부분 겹침)은 중복이다 
  ✔ 같은 미디어는 같은 키 
  ✔ 같은 미디어는 같은 키 (워커 재시도 방어) 
  ✔ 같은 비밀번호도 매번 다른 해시 — salt 가 실제로 붙는다 
  ✔ 같은 사유가 반복되면 한 건으로 센다 — firstAt 보존 + count 증가 
  ✔ 같은 오프셋이라 문자열 정렬 = 시간 정렬 
  ✔ 같은 입력에 항상 같은 결과 
  ✔ 같은 조치를 두 이름으로 부르지 않는다 — 화면 어휘 '직접 충전' 으로 통일 
  ✔ 같은 크기의 파일이 이미 있으면 다시 안 받는다 — 수 GB 를 두 번 받는 건 사고다 
  ✔ 같은 플랫폼 두 계정은 두 항목이다 — 덮어쓰면 매 순방 재업로드가 난다 
  ✔ 같은 회사 안에서는 FIFO 를 지킨다 
  ✔ 개발용 공용 계정 탈출구는 명시적 opt-in 이다 
  ✔ 개발자 말이 사용자 문구로 새지 않는다 
  ✔ 개수 상한을 지킨다 
  ✔ 객체·문자열 둘 다 받는다 
  ✔ 갱신 **전에** 옛 모양을 읽는다 — 뒤에 읽으면 늘 같아서 영영 안 돈다 
  ✔ 거부 라우트가 있고 사람(actor)을 요구한다 
  ✔ 거부 사유는 비어 있지 않다 
  ✔ 거부에는 사유가 있다 
  ✔ 거절(rejected)은 안 붙는다 
  ✔ 거절은 판단이 끝난 것 — 미결정이 아니다 
  ✔ 거절한 추천도 미디어에 없다 
  ✔ 검증 함수가 4종만 통과시킨다 
  ✔ 검토 목록(usableLinks)에는 거절된 것도 남는다 — 사람이 되돌릴 수 있어야 한다 
  ✔ 게시 단계에서 멈춘 상태도 사유가 된다 — 눌린 로그를 계획 단위로 이어 준다 
  ✔ 게시는 만들어진 순서대로 — 최신 우선(엔티티 ord)을 뒤집는다 
  ✔ 게이트 OFF 가 할당량 소진을 이긴다 — 켜기 전엔 할당량이 의미가 없다 
  ✔ 게이트가 꺼져 있으면 설명이 한 글자도 안 바뀐다 
  ✔ 결과 확인이 **넣기 전**에 온다 — 뒤에 있으면 방금 넣은 pending 을 보게 된다 
  ✔ 결제 로그가 실패·대기 건도 보여준다 
  ✔ 결제 수단 **등록**은 billing:write 로만 열린다 (2026-08-20) 
  ✔ 결제 식별자는 우리가 만들고 이상 문자를 지운다 
  ✔ 결제 호출이 던지면 failed, 승인 대조 실패는 pending 유지 
  ✔ 결제가 없으면 빈 인보이스지만 터지지 않는다 
  ✔ 결제가 있는 달만 고를 수 있다 
  ✔ 결제는 금액을 서버가 계산한다 
  ✔ 결제된 것만 담는다 
  ✔ 겹침이 '다 나감'을 이긴다 
  ✔ 경계는 KST 자정(UTC 15:00) 이다 — 서버 타임존과 무관하게 같은 판정 
  ✔ 경로 구분자와 예약 이름을 막는다 
  ✔ 경로 앞뒤로 뭘 붙여도 못 뚫는다 
  ✔ '경로가 없음'과 '권한이 없음'을 구분한다 
  ✔ 계수 = H/stageH = 예상값 (세로 3 · 16:9≈2.133 · 1:1=1.2 · 4:5≈2.109) 
  ✔ 계정 식별자 없는 구 데이터는 보수적으로 already 다 — 중복 게시가 더 나쁘다 
  ✔ 계정 저장은 발급 시점 워크스페이스에서 돈다 (쿠키가 아니라) 
  ✔ 계정 키가 목록에 없으면 진행하지 않는다 — 엉뚱한 계정에 올리느니 멈춘다 
  ✔ 계정이 없으면 **아무것도 하지 않는다** — 공용 계정 폴백이 없다 
  ✔ 계정이 없으면 도우미가 만든다 — 웹에서 미리 안 만들어도 끝난다 
  ✔ 계획 channels[] 의 계정을 플랫폼별 필드로 풀어 넘긴다 
  ✔ 계획 라우트가 orientation·reframe 를 검증해 받는다 — 틀린 값 침묵 저장 금지 
  ✔ 계획 루프 안에서는 실행 로그를 note() 로만 쓴다 
  ✔ 계획 방향이 클립 aspectRatio 에 수동 채택과 같은 매핑으로 적용된다 
  ✔ 계획 저장 라우트가 채널 중복을 막지 않는다 — 같은 채널에 계획 여럿 허용 
  ✔ 계획을 못 찾은 묶음도 기다리지 않고 나간다 
  ✔ 계획을 지우면 그 계획의 보류도 지운다 — 아무도 게시 안 할 유령 항목 방지 
  ✔ 계획의 모양(색·글꼴·자막)은 새 값으로 갈린다 
  ✔ 계획이 없으면(신규 생성) 빈 지문 — 기존 것과 항상 다르다 
  ✔ 계획이 전부 꺼져 있어도 아무것도 안 한다 
  ✔ 고객사 콘솔이 필요로 하는 것이 열려 있다 (자동화·검수·프로그램) 
  ✔ 공급가액 + 세액이 **정확히** 총액이다 
  ✔ 공급가액+부가세 = 총액 — splitVat 역산이라 1원도 새지 않는다 
  ✔ 공급자 표기는 issuerInfo 와 같은 INVOICE_ISSUER_* 를 읽고, 비면 비워 둔다 
  ✔ 공백 차이는 넘어가고, 정식 이름으로 되돌려준다 
  ✔ 공백을 지운다 — `#나는 솔로` 는 `#나는` 까지만 인식된다 
  ✔ 공장 배선이 준비 전 분석 대신 media.prepare를 복구 큐잉한다 
  ✔ 관리 스코프는 아예 없다 · 결제는 조회 + 수단 등록까지만 
  ✔ 관리·결제·배포 라우트는 전부 막힌다 
  ✔ 관문(dispatchPublish)이 이 검증을 쓴다 
  ✔ 관문이 순수 판정을 쓴다 
  ✔ 구간 밖으로 못 늘리게 잠근다(hasHardBoundaries) — AI 가 고른 구간이 바깥 경계다 
  ✔ 구간이 전부 겹쳐 제외됐으면 겹침이라고 말한다 (정상 동작임도 함께) 
  ✔ 권한이 빈 키는 막는다 
  ✔ 규격에 맞으면 통과한다 
  ✔ 규칙에 방식을 저장·조회한다 (컬럼 유실 금지) 
  ✔ 규칙이 고른 템플릿은 시드 표에 없어도 버리지 않는다 — 새 캔바 템플릿이 조용히 무시되던 구멍 
  ✔ 그 id 가 폰트 레지스트리에 있고 Bold(700) 를 갖는다 — 렌더가 800 을 요청해도 700 으로 스냅된다 
  ✔ 그 구간만 잘라 세로 시퀀스로 만든다 
  ✔ 그 함수는 서버의 순수 모듈에서 온다 — 화면이 자기 사본을 두지 않는다 
  ✔ 그림자를 넣지 않는다 
  ✔ 글꼴 이름이 비면 **건드리지 않는다** — 빈 값이면 프리미어가 글꼴을 못 찾는다 
  ✔ 금액이 다르면 올리지 않는다 
  ✔ 금액이 맞고 PAID 여야 올린다 
  ✔ 기다림형 코드 집합은 셋뿐 — 늘리면 리포트가 늦어진다 
  ✔ 기록의 '승인 대기' 줄도 영상 단위로 접는다 — 서버는 채널마다 한 줄을 남긴다 
  ✔ 기본 3 · 0 허용(무과금 스위치) · 쓰레기는 기본값 
  ✔ 기본 선택은 쇼츠 구간만 — 전체 선택 한 번에 14분짜리를 끌고 가면 안 된다 
  ✔ 기본값은 그 자체로 표에 있는 값이다 
  ✔ 기본은 **쇼츠 구간 전부** 선택 — 다 쓰고 빼는 편이 손이 덜 간다 
  ✔ 기본은 꺼져 있다 
  ✔ 기존 회사를 바꾸는 라우트에 requireReason 이 있다 
  ✔ 긴 값을 잘라 담는다 
  ✔ 긴 입력도 통과한다 
  ✔ 길이·문자 종류는 따지지 않는다 
  ✔ 길이가 다르면 막는다 
  ✔ 길이를 먼저 정하고 얹는다 — 안 하면 정지 이미지 기본 길이(5초)로 서로 덮어쓴다 
  ✔ 길이를 몰라도 잔액 0 이면 큐잉 단계에서 막는다 
  ✔ 길이를 못 읽으면 막지 않는다 — 판정 불가로 자동배포를 멈추면 조용히 0건이 된다 
  ✔ 길이를 자른다 — PG 가 장문을 보내도 화면이 무너지지 않는다 
  ✔ 길이에 비례한 타임아웃 — 60분 트랜스코드가 기본 5분에 잘리면 안 된다 
  ✔ 꺼져 있으면 assertTikTokUploadEnabled 가 던지고, 구분 가능한 코드를 갖는다 
  ✔ 꺼져 있으면 assertUploadEnabled 가 던지고, 구분 가능한 코드를 갖는다 
  ✔ 꼬리 한 단어는 앞 조각에 붙인다 (혼자 깜빡이는 게 제일 거슬린다) 
  ✔ 끈이 끊겨도 업로드는 된다 — 링크 하나 때문에 몇 GB 를 버리지 않는다 
  ✔ 끊기면 GCS 가 받은 위치를 되물어 재개한다 — 우리 offset 은 진실이 아니다 
  ✔ 끝 4자리는 맨 뒤 숫자 묶음에서 — 앞 BIN 을 잘못 집지 않는다 
  ✔ 끝내 길이를 모르면 시작하지 않는다 — 모르면 안 내보낸다 
  ✔ 끝이 다음 달 1일이다 (경계 결제를 놓치지 않게) 
  ✔ 나머지는 전부 기록만 — 새 채널이 추가돼도 기본이 record 여야 한다 
  ✔ 나머지는 추천 없음 
  ✔ 낙관 문구도 (클립,채널)당 한 줄로 막는다 
  ✔ 날짜 스탬프는 KST 다 — UTC 15시(=KST 자정)에 날짜가 바뀐다 
  ✔ 날짜만 오면 KST 자정으로 — 그냥 +09:00 을 붙이면 파싱 불가(NaN)가 된다 
  ✔ 날짜순으로 정렬한다 
  ✔ 남기기·집어가기 두 라우트가 있다 
  ✔ 내레이션 토글·문구가 렌더 캐시 키에 들어간다 
  ✔ 너무 짧거나 긴 것, 중복, 배열이 아닌 것을 버린다 
  ✔ 넣은 뒤 **끝 시각을 줄 길이에 맞춘다** — 안 하면 자막끼리 겹친다 
  ✔ 네이버 TV 는 가로 3분 
  ✔ 네이버 클립 설명 10자 하한은 자동으로 채우지 않고 문제로 보고한다 
  ✔ 네이버 클립은 제목 필드가 없다 — 제목 문구를 버리지 않고 설명 첫 줄로 올린다 
  ✔ 네트워크는 STEP-D 와 GCS 둘뿐 · 파일 접근은 request — 심사도 이 좁기를 본다 
  ✔ 노출 컨트롤 기본 문구도 맞춘다 — 안 맞추면 필수 그래픽 패널에 옛 문구가 뜬다 
  ✔ 놓친 슬롯 몫은 오늘 포기(staleMissedSlots) — 저녁에 켠 계획이 아침 몫을 쏟지 않는다 
  ✔ '다 나감'이 기본값을 이긴다 
  ✔ 다 있으면 통과 
  ✔ 다시 찍기 실패가 계획 저장을 되돌리지 않는다 — 저장은 이미 끝났다 
  ✔ 다운로드 완료 → content.analyze 자동 큐잉 전에 정밀 잔액 판정이 선다 
  ✔ 다음 배포 박스와 길이(M:SS) 표기 
  ✔ 다음 배포가 없으면 박스를 그리지 않는다 
  ✔ 다크 sd 팔레트가 실제로 존재하고 모달은 하드코딩 흰 배경을 쓰지 않는다 
  ✔ 다크 보조 문구가 카드·앱·사이드바에서 AA 
  ✔ 단가가 없으면 결제창을 띄우지 않는다 
  ✔ 단가는 양수만 
  ✔ 단일 트랙은 그대로 스테레오로 
  ✔ 대가성 문구가 **반드시** 함께 나간다 
  ✔ 대소문자·공백에 흔들리지 않는다 — 플랫폼 문자열은 DB 에서 온다 
  ✔ 덕킹은 사이드체인이다 — 내레이션이 끝나면 원음이 돌아온다 
  ✔ 던지지 않는다 — 전부 막혀도 예외가 아니라 빈 queue 다 
  ✔ 도우미가 --account 없이도 실행파일 이름에서 계정을 읽는다 
  ✔ 도우미는 커머스 세션 라우트로 PUT 한다 
  ✔ 돈이 나가거나 결제수단을 없애는 경로는 owner/admin 세션만 
  ✔ 동의 URL 은 서버 발급 state 를 쓴다 (base64 JSON 조립 금지) 
  ✔ 두 ASPECT_PRESETS 배열이 (공백 정규화 후) 1:1 로 일치 
  ✔ 두 normalizeEditorCoords(웹·서버)가 같은 계수·같은 마커로 옛 저장분을 올린다 
  ✔ 두 무인 경로 모두 채택 단계에서 길이 상한을 본다 
  ✔ 두 번째부터는 다시 만들지 않는다 — 같은 이름 시퀀스가 쌓이면 안 된다 
  ✔ 두 템플릿 모두 레퍼런스 강조색을 시드로 갖는다 
  ✔ 둘 다 없으면 객체를 새로 만들지도 않는다 
  ✔ 둘 다 있으면 [line1, line2] 를 명시적 두 줄로 쓴다 (폭 기준 재분할 금지) 
  ✔ 둘 다 있으면 통과 
  ✔ 둘째 줄 색도 같은 3단으로 흐른다 (줄만 살리고 색을 버리지 않는다) 
  ✔ 드라마 팩은 스포일러를 금지한다 
  ✔ 드라마·예능은 각자 제 칸으로 간다 
  ✔ 드라마·예능을 알아보고, 모르는 장르는 공통팩으로 간다 
  ✔ 등록 캐스트가 없으면 이름을 지어내지 말라고 명시한다 
  ✔ 등록 캐스트가 있으면 그 표기만 쓰라고 못 박는다 
  ✔ 딱 맞으면 통과하고 잔액 0 이 된다 
  ✔ 라우트가 값을 되돌려주지 않는다 
  ✔ 라우트가 계정을 검증하고 파일명에 accountKey 를 박는다 
  ✔ 라우트를 실제로 떠낸다 (정규식이 죽으면 이 테스트 전체가 무의미해진다) 
  ✔ 라우트와 GCS 객체 경로가 같다 
  ✔ 라이트 보조 문구가 카드·앱·사이드바에서 AA 
  ✔ 러닝타임을 모르면(0) 이 함수는 막지 않는다 — 대신 실행 길목이 막는다 
  ✔ 레이아웃 전환 지점과 샷 경계가 추가되고, 0.2초 이내 중복은 접힌다 
  ✔ 레이어가 남으면 **비운다** — 옛 문구가 화면에 남으면 안 된다 
  ✔ 레이어가 없으면 던진다 
  ✔ 레인은 서로 겹치지 않는다 — 두 워커가 같은 잡을 다투면 안 된다 
  ✔ 렌더 결과는 파일 선택 경로와 **같은 업로드 본체**로 들어간다 — 업로드 로직이 둘이 되면 갈라진다 
  ✔ 렌더 대기가 '다 나감'을 이긴다 
  ✔ 렌더 선행 준비는 할당량 루프 **밖**에 있다 — 채택되면 미리 렌더된다 
  ✔ 렌더 실패 로그에 채널 키를 붙이지 않는다 
  ✔ 렌더 전이면 보내지 않는다 
  ✔ 렌더 확정 실패가 채택 단계 사유를 이긴다 — 만든 게 못 나가는 쪽이 급하다 
  ✔ 렌더: overlay-canvas 가 패밀리 레지스트리로 GmarketSans 를 등록한다 
  ✔ 렌더: renderTextLayerPng 가 fill 전에 strokeText 를 호출한다 
  ✔ 렌더·미디어·게시 중 하나라도 있으면 배포 가능 
  ✔ 렌더가 editorState 를 정규화한 뒤에 굽는다 (생산→소비 배선) 
  ✔ 렌더가 실제로 청킹을 거쳐 자막 이벤트를 만든다 
  ✔ 렌더된 것만 진행하고 나머지는 사유와 함께 반환한다 
  ✔ 렌더와 **같은 줄 나누기**를 쓴다 — 원문을 주면 줄 수가 달라진다 
  ✔ 로그아웃·전체 로그아웃도 같은 토큰을 본다 — 헤더 세션이 안 끊기면 안 된다 
  ✔ 로그인 핸들러가 있다 
  ✔ 로그인이 회사 상태를 본다 
  ✔ 루트는 옮길 수 없다 
  ✔ 리포트: exhausted + 마지막 슬롯 경과면 목표 미달이어도 보낸다 
  ✔ 리프레임 대기는 시도가 아니다 — 카운터도 시계도 안 움직인다 
  ✔ 리프레임을 큐잉한 순방엔 렌더를 걸지 않는다 — /export 의 reframe_not_ready 409 가 순서를 강제한다 
  ✔ 링크 개수 상한을 지킨다 
  ✔ 링크 도메인·대가성 문구는 commerce.ts(레지스트리)와 발급기에만 있다 
  ✔ 링크가 없으면 원문 그대로 — 발급이 늦어도 발행을 막지 않는다 
  ✔ 링크의 제공자는 **URL 이 정한다** — 저장된 provider 필드가 틀려도 URL 을 따른다 
  ✔ 마이그레이션에서 걷어진다 
  ✔ 마커 경로도 세로다 — 러프컷만 세로면 가로 화면 보며 세로 결과를 상상해야 한다 
  ✔ 마커에 STEP-D 추천 id 를 남긴다 — 나중에 되짚을 유일한 끈이다 
  ✔ 막기 전에 자동 결제를 먼저 시도한다 — '돈 받고 시작' 의 돈 받는 자리 
  ✔ 막는 사유를 code 로 구분한다 — 미등록과 해지는 다른 사건이다 
  ✔ 막지 않고 알려만 준다 
  ✔ 막힌 것만 사유와 함께 모은다 
  ✔ 막힌 이동은 사유가 있다 
  ✔ 막힌 이유는 절대 빈 문자열이 아니다 
  ✔ 만든 id 는 형식 검사를 통과한다 
  ✔ 만든 뒤 시퀀스를 연다 — 안 열면 '눌렀는데 아무 일도 안 일어났다' 가 된다 
  ✔ 만든 클립이 다 나갔으면 그렇게 말한다 — '추천 없음'과 다른 상태다 
  ✔ 망가진 슬롯 값은 버린다 — 화면 입력이 그대로 판정에 들어오면 안 된다 
  ✔ 맞는 비밀번호는 통과한다 
  ✔ 매 순방 마주치는 스킵은 전부 dedupe 를 건다 — 실행 로그 50건을 덮지 않는다 
  ✔ 매번 다른 키가 나온다 
  ✔ 맥락은 **렌더 전에** 읽는다 — 렌더가 끝나면 시퀀스가 바뀌어 있을 수 있다 
  ✔ 멈추기 전에 자동 충전을 먼저 시도한다 — 충전되면 알리지 않고 이어간다 
  ✔ 멈춘 계획은 아무것도 안 한다 
  ✔ 메모 없이는 못 넣는다 
  ✔ 메서드가 다르면 다른 라우트다 
  ✔ 메타 대기가 '다 나감'을 이긴다 
  ✔ 멤버십 스코프를 요구하지 않는다 — 부르는 코드가 없다 
  ✔ 멱등 — 두 번 조립해도 문구·링크가 중복되지 않는다 
  ✔ 면제 목록이 조용히 늘지 않는다 
  ✔ 명시적 truthy 값에서만 켜진다 
  ✔ 명시적 truthy 값에서만 켜진다 
  ✔ 명시적 truthy 만 ON 
  ✔ 명시적 truthy 에서만 켜진다 
  ✔ 몇 건 예정 중 몇 건 나갔고 몇 건이 비었는지 말한다 
  ✔ 몇 건을 다시 찍었는지 응답에 실어 준다 — 조용하면 반영 여부를 알 길이 없다 
  ✔ '몇 시간 뒤'를 같이 보여준다 — 12시간 오차가 여기서 드러난다 
  ✔ 모노 여러 트랙은 앞의 둘을 L/R 로 합친다 — 방송 표준(1=L·2=R) 
  ✔ 모델: TitleLine 에 font(패밀리 id) 필드가 있다 
  ✔ 모델: TitleLine·OverlayTextItem 에 stroke 필드가 있다 
  ✔ 모듈이 force/override/bypass/admin 류를 export 하지 않는다 
  ✔ 모든 code 가 심각도로 분류돼 있다 — 미분류가 조용히 생기면 구멍이 재발한다 
  ✔ 모든 JobType 이 어느 레인엔가 속한다 
  ✔ 모든 제공자가 id·이름·대가성 문구를 갖는다 — 문구 없는 제공자는 넣으면 안 된다 
  ✔ 모든 트랜잭션이 **프로젝트를 잠근 채** 돈다 — 이게 공식 처방이다 
  ✔ 모르는 값은 undefined — 임의로 예능으로 밀지 않는다 
  ✔ 모르는 값은 버린다 (에러가 아니라) 
  ✔ 모르는 값을 통과시키지 않는다 
  ✔ 모르는 레인 이름은 던진다 — 오타가 all 워커로 둔갑하지 않게 
  ✔ 모르는 상태값은 막는다 (fail-closed) 
  ✔ 모르는 플랫폼은 null (종전 동작 유지 · 무회귀) 
  ✔ 모서리를 둥글리지 않는다 
  ✔ 모션 컴포넌트는 **matchName** 으로 찾는다 — 표시 이름은 한국어 프리미어에서 '동작' 이다 
  ✔ 모양과 무관한 변경(켜기·할당량·슬롯)은 지문을 바꾸지 않는다 — 재렌더 낭비 방지 
  ✔ 모양이 바뀌었을 때만 다시 찍는다 
  ✔ 모은 서브클립도 그 자리에서 시퀀스로 만든다 
  ✔ 모자라면 조용히 넘기지 않고 던진다 — 그리고 회차에 사유를 남긴다 
  ✔ 목표를 채운 날엔 섹션이 아예 없다 
  ✔ 못 찾은 조각 수를 사람에게 말한다 — 조용히 빠지면 안 된다 
  ✔ 못 풀면 null — 워커를 죽이지 않는다 
  ✔ 무엇이 없는지 이름을 말해준다 
  ✔ 무음 영상도 판정이 성립한다 — 오디오 코덱 없음을 이유로 삼지 않는다 
  ✔ 무편집 제목은 첫 줄 106px·둘째 줄 107px 출력값을 쓴다 
  ✔ 무효화면 **한 번만** 다시 돈다 — 무한 재시도는 멈춘 것처럼 보인다 
  ✔ 문구·글꼴·크기·색을 레이어마다 바꾼다 
  ✔ 문구가 링크보다 **뒤**에 온다 — 잘려도 링크만 남는 일이 없게 
  ✔ 문구까지 같아야 '이미 남겼다'로 본다 
  ✔ 문장부호가 refine 으로 덧붙어도 정렬이 깨지지 않는다 
  ✔ 문장부호에서 우선 끊는다 (의미 단위 = 화면 단위) 
  ✔ 미검토(pending)는 안 붙는다 — 아무도 안 본 상품이 방송사 채널에 나가면 안 된다 
  ✔ 미등록 글꼴 id 는 크래시 없이 기본 패밀리로 폴백한다 
  ✔ 미디어 종류로 거른다 
  ✔ 미래 시각도 같은 오차로 막는다 
  ✔ 미리보기: 편집 중 -webkit-text-stroke 로 근사한다(자막이 쓰는 패턴) 
  ✔ 미리보기가 같은 함수로 끊어 현재 조각만 그린다 
  ✔ 미리보기가 자막을 하단 기준(bottom)으로 subtitle* 값에 그린다 — 렌더 capMV(\an2)와 같은 축 
  ✔ 미리보기는 렌더 산출물을 재생한다 — 편집기 링크는 '편집' 으로 분리 
  ✔ 미리보기에서만 얹는다 — 원본 전체 타임라인에 회차 자막을 깔지 않는다 
  ✔ 미설정·빈값·오타는 전부 OFF 
  ✔ 미설정이면 10분 
  ✔ 미설정이면 5 
  ✔ 미설정이면 꺼져 있다 
  ✔ 미설정이면 꺼져 있다 — 배포만 해서는 아무것도 게시되지 않는다 
  ✔ 미설정이면 꺼져 있다 — 배포만 해서는 아무것도 올라가지 않는다 
  ✔ 미정산 목록은 자동 충전 요청분만 · 미결제 상태만 · 기간 제한 
  ✔ 바이트를 옮겼으면 DB 경로도 즉시 쓴다 — 실제 위치와 기록이 어긋나면 안 된다 
  ✔ 박스형 자막 스타일은 mogrt 로 안 만든다 — 도형을 못 옮긴다 
  ✔ 발급 라우트가 평문을 저장하지 않는다 
  ✔ 발급 모듈이 제휴 리다이렉트 요청을 라우트에서 막는다 
  ✔ 발급 전에 계정을 해석한다 
  ✔ 발급 키는 접두로 알아볼 수 있다 
  ✔ 발급사가 없으면 브랜드 enum 으로 폴백한다 
  ✔ 발송부가 순방과 같은 함수로 목표를 낸다 (소스 스캔) 
  ✔ 발송은 전부 fire-and-forget 이다 — await 로 적립 경로를 막지 않는다 
  ✔ 발행 경로가 채널별 메타를 읽는다 
  ✔ 발행 중 세션 만료 시 자동 재로그인을 시도한다 
  ✔ 발행 후 메타 수정(updatemeta)이 링크를 벗기지 않는다 
  ✔ 방송 코덱(MPEG-2·DNxHD·ProRes)은 변환한다 
  ✔ 방향 미지정이면 추천 종류로 정한다 — 클립(롱폼)은 가로형 
  ✔ 배너가 **위**에 온다 — 통계·항목 목록보다 앞 (사용자 2026-08-28 "밑에 두면 사람이 안 봄") 
  ✔ 배너는 순방 전체가 아무 일도 안 했을 때만 — 계획 하나가 전체를 덮지 않는다 
  ✔ 배선: buildStaticOverlayItems 가 줄별 font 를 아이템에 실어 보낸다 
  ✔ 배선: index 가 stroke 를 아이템에 실어 보낸다 (stroke.width 는 출력 px 그대로) 
  ✔ 배속을 반영한다 — 상한은 구간이 아니라 **출력 길이**에 건다 
  ✔ 배치를 못 받으면 예전 동작(꽉 채우기)으로 물러난다 — 여기서 전체가 멈추면 안 된다 
  ✔ 배치를 서버에서 받는다 — 복제하면 프리셋을 고쳐도 프리미어만 옛 배치로 남는다 
  ✔ 배포 실패가 승인 대기를 이긴다 — 이미 실패한 건이 급하다 
  ✔ 배포 채널 화면은 연결 관리만 하고 별도 설정 UI를 만들지 않는다 
  ✔ 배포 큐 진입점은 한 파일뿐이어야 한다 
  ✔ 배포 행이 없으면 대기 — 다시 찍어도 된다 
  ✔ 배포는 cp 만 
  ✔ 배포채널 카드에 도우미 다운로드 버튼이 있다 
  ✔ 배포처가 여럿이면 **가장 빡빡한 상한**을 고른다 — 렌더는 클립당 한 번이다 
  ✔ 버튼은 넣지 않는다 — 안내는 문구로만 (사용자 2026-08-27) 
  ✔ 버튼이 선택 건수를 말한다 — 몇 건에 작용하는지 모르고 누르면 안 된다 
  ✔ 버퍼를 계획별로 나눠 계획마다 보낸다 
  ✔ 번호는 랜덤해 보이되 결제마다 고정이다 (2026-08-27) 
  ✔ 번호는 회사·월이 같으면 항상 같다 
  ✔ 범위 3종 
  ✔ 범위 밖 전환·샷은 무시하고, 잘못된 범위는 빈 목록이다 
  ✔ 베이스가 없으면(409) 이 PC 의 기본 템플릿을 올리고 **한 번만** 재시도한다 
  ✔ 변환 뒤 메타를 다시 잡고, DB 는 변환본을 가리킨다 
  ✔ 보류(holdClip)로 처리하지 않는다 — 해제가 게시 승인으로 읽힌다 
  ✔ 보존 목록에 트림·훅이 들어 있다 
  ✔ 복사가 fc-cache **앞**에 있다 — 뒤에 있으면 캐시에 안 들어간다 
  ✔ 본문이 한 글자만 달라도 거부 
  ✔ 본채널은 제한 없이 시작한다 
  ✔ 부가줄(channelExtraLines)이 렌더에도 존재한다 
  ✔ 부모를 찾는다 
  ✔ 분류표가 비어 있지 않고, 1차마다 2차가 최소 하나는 있다 
  ✔ 분석 미완이 후보 사유를 전부 이긴다 
  ✔ 분석 실패가 분석 중을 이긴다 
  ✔ 분석 완료 회차가 전부 상한이면 상한 도달 
  ✔ 분석이 **큐잉조차 안 됐으면** '분석 중'이 아니다 — 기다려도 안 끝난다 
  ✔ 분석이 돌다가 실패한 것(error)은 blocked 가 아니다 — 사람이 할 일이 다르다 
  ✔ 분석이 실패했으면 분석 실패라고 말한다 
  ✔ 분석이 안 끝났으면 '추천 없음'이 아니라 '분석 중'이다 
  ✔ 분석이 큐잉조차 안 된 회차를 '분석 중'으로 세지 않는다 
  ✔ 비동기 경계를 넘어도 유지된다 (라우트 핸들러의 await 체인) 
  ✔ 빈 값과 슬래시는 루트 
  ✔ 빈 값만 거부 
  ✔ 빈 값은 저장하지 않는다 
  ✔ 빈 결과는 성공으로 치지 않는다 — 0줄이면 파일을 지우고 null 
  ✔ 빈 계획 목록을 '전체 대상'으로 해석하지 않는다 
  ✔ 빈 렌더 결과를 성공으로 치지 않는다 
  ✔ 빈 목록이면 1 부터 
  ✔ 빈 문자열·공백은 빈 배열 
  ✔ 빈 문자열·공백은 없는 것으로 본다 
  ✔ 빈 문자열이 0 으로 읽히면 안 된다 
  ✔ 빈 블록은 빠진다 — 조립이 지저분해지지 않는다 
  ✔ 빈 아이템은 null 을 반환한다 
  ✔ 빈 요청은 400 — 뭘 열지 모르는 핸드오프는 만들지 않는다 
  ✔ 빈 응답의 원인을 남긴다 (finishReason) 
  ✔ 빈 탐색에 상한이 있다 — 큰 프로젝트에서 패널이 멈춘 것처럼 보이면 안 된다 
  ✔ 빈 텍스트·역전 구간은 아무 것도 만들지 않는다 
  ✔ 빈/누락 status 는 실패다 — 모양이 어긋나면 통과 금지 
  ✔ 빈값·공백은 undefined — 예약 없음으로 처리된다 
  ✔ 빌링키를 화면으로 내보내지 않는다 
  ✔ 사람 말 사유는 상태·코드마다 다르다 — 무엇을 고칠지 갈린다 
  ✔ 사람이 고쳐서 규격을 어기면 잡는다 
  ✔ 사람이 고친 메타를 재생성이 덮지 않는다 
  ✔ 사람이 시킨 재분석은 별개다 (원가가 다시 든다) 
  ✔ 사람이 이미 판단한 것은 다시 잡지 않는다 
  ✔ 사람이 재분석시키면 별개 사용량이다 (원가가 다시 든다) 
  ✔ 사람이 조치해야 끝나는 사유는 안 늙는다 — 카드 거절은 한 달 뒤에도 유효하다 
  ✔ 사람이 편집기에서 정한 값은 그대로 — 계획 변경이 남의 편집을 지우면 안 된다 
  ✔ 사람이 확정하면(heldAwaitingHuman=false) 나간다 
  ✔ 사업자번호가 없으면 빠진 항목을 알려준다 
  ✔ 사용 중지 채널이 가장 먼저 걸린다 
  ✔ 사용자 내역에는 delta 0 행이 안 나온다 
  ✔ 사용자 목록은 사유 없이도 열리지만 기록은 남는다 
  ✔ 사용자·잡·감사 모두 tenant 필터를 받는다 
  ✔ 사용자가 못 고치는 실패는 사유를 가른다 — 단가 미설정은 관리자 몫 
  ✔ 사용자가 못 고치는 실패에 사용자 조치를 시키지 않는다 — 단가 미설정 vs 충전량 오류 
  ✔ 사용자별로 나뉜다 — 남의 핸드오프를 집어가면 안 된다 
  ✔ 사유가 바뀌면 새 알림이다 — 옛 시각을 물려주지 않는다 
  ✔ 사유를 면제한 목록 라우트가 감사 기록을 부른다 
  ✔ 산문 생성은 끄지 않는다 (opts.thinking 으로 되돌릴 수 있다) 
  ✔ 산출물 경로는 clipId 를 파일명 안전 문자로 눌러 담는다 
  ✔ 상수를 못 찾으면 **던진다** — 0 으로 폴백하면 AME 큐로 가 버린다 
  ✔ 상태가 PAID 가 아니면 막는다 
  ✔ 상태만 기록하는 채널(SMR 등)은 생성 문구가 그 사실을 말한다 
  ✔ 상품 페이지 URL 은 어느 제공자로도 안 잡힌다 — 정산 안 되는 맨 링크다 
  ✔ 상품명의 옵션 꼬리를 잘라 읽히게 만든다 
  ✔ 상한 도달이 점수 미달을 이긴다 
  ✔ 상한 알림은 유효기간이 지나면 읽는 쪽에서 만료된다 
  ✔ 상한 없는 즉시 결제와 파괴적 경로는 키로 못 부른다 (스코프 전부 줘도) 
  ✔ 상한 이내면 손대지 않는다 
  ✔ 상한 이하 문장은 그대로 한 조각 
  ✔ 상한은 하루 발행 수를 따라간다 — 하루 6개 계획은 회차 하나에서 6건까지 
  ✔ 상한을 넘는 문장을 여러 조각으로 나눈다 (단어 중간에서 자르지 않는다) 
  ✔ 상한을 넘으면 막고 얼마나 넘었는지 말한다 
  ✔ 상한이 null 이면 길이를 보지 않는다 
  ✔ 상한이 있다 — 회차 전체(수백 줄)를 실수로 얹으면 프리미어가 멎는다 
  ✔ 상호가 없으면 막고, 어느 칸인지 알려준다 
  ✔ 상호와 사업자번호만 필수다 
  ✔ 새 PNG가 완료되기 전에 직전 hash를 null로 버리지 않는다 
  ✔ 새 result 값을 만들지 않는다 — 웹이 모르는 값은 원문 그대로 노출된다 
  ✔ 새 결제 전에 미정산 주문을 먼저 정산한다 
  ✔ 새 디스플레이 패밀리 4종(검은고딕·도현·주아·고딕A1)이 등록되고 파일이 실제로 존재한다 
  ✔ 새벽 예약이면 되묻는다 (오전/오후 착오의 전형적 결과) 
  ✔ 색·글꼴·템플릿이 바뀌면 지문이 바뀐다 
  ✔ 생 채널 ID(UC…)는 사람이 읽는 자리에 안 나온다 
  ✔ 생산: content-pipeline 이 shorts 의 title_line1/2 를 추천 엔티티에 싣는다 
  ✔ 생산: 채널 탭에 아이콘 표시 토글이 있다 
  ✔ 생성 라우트가 response_schema 를 넘기지 않는다 (AENA 결론) 
  ✔ 생성 라우트가 채널 규칙(titlePrefix·hashtagTemplate)을 실제로 넘긴다 
  ✔ 생성과 배치를 한 번에 — 트랙·시각을 우리가 계산하지 않는다 
  ✔ 서명 URL 도메인이 매니페스트에 열려 있다 
  ✔ 서명 URL 이 그 파일명을 강제한다 (Content-Disposition) 
  ✔ 서버 렌더가 captionY·captionSize·captionColor 오버라이드를 읽는다 
  ✔ 서버 렌더가 프리셋을 소비한다 (getAspectPreset → cropRect → renderShort) 
  ✔ 서버가 kind 를 실어 보낸다 — 패널이 길이로 추측하지 않게 
  ✔ 서버가 그 경로를 서빙한다 
  ✔ 서버가 렌더와 같은 재료로 합성한다 — 캔버스로 다시 그리지 않는다 
  ✔ 서버가 원본 파일명을 함께 준다 — 없으면 사람이 매번 골라야 한다 
  ✔ 서버가 제목·채널을 독립 레이어로 렌더할 수 있다 
  ✔ 서버가 출처를 클립에 남긴다 — 이게 유일한 기록이다 
  ✔ 서버는 ASPECT_PRESETS 를 그대로 내려 준다 — 여기서 계산을 새로 하지 않는다 
  ✔ 서버는 그 베이스를 검증하고 저장한다 — 텍스트 레이어가 없으면 거절 
  ✔ 서버도 그 비율을 받아 그린다 — 기본은 쇼츠(세로) 
  ✔ 선택·드래그·타이핑 중에도 서버 PNG가 화면의 텍스트다 
  ✔ 설명 상한을 넘기지 않는다 
  ✔ 설명란 문구는 레지스트리에서 온다 — 붙은 링크의 제공자 문구만 나간다 
  ✔ 설치 스크립트는 관리자 권한 없이 도는 사용자 폰트 설치다 
  ✔ 설치 여부를 사용자·시스템 폰트 폴더에서 본다 
  ✔ 성공 카운트와 시도 슬롯이 같은 'KST 달력일' 을 쓴다 
  ✔ 성공하면 상태를 지운다 
  ✔ 성공하면 세션을 **서버에도** 저장한다 — 다른 워커도 쓰게 
  ✔ 성인용품·품절·여행상품을 제외한다 
  ✔ 세금계산서라고 말하지 않는다 
  ✔ 세로 + 상한 초과만 막는다 
  ✔ 세로 + 아는 플랫폼이면 프리셋 키를 준다 
  ✔ 세로 숏폼은 보낼 수 있다 
  ✔ 세로+AI 면 채택 직후 리프레임을 수동과 같은 라우트로 큐잉한다 
  ✔ 세로면 숏폼 
  ✔ 세션 blob 을 SELECT 목록에 넣지 않는다 (로그·응답으로 샌다) 
  ✔ 세션 쿠키를 expires -1 로 정규화한다 (playwright 규약) 
  ✔ 세션과 **다른 키**를 쓴다 — 하나가 새도 다른 쪽은 안 열린다 
  ✔ 세션이 없으면 계정을 session_expired 로 표시한다 — 조용히 0건으로 끝나지 않게 
  ✔ 셋 다 있으면 통과하고 전화번호는 숫자만 남는다 
  ✔ 소비(렌더): 꺼져 있으면 아이콘 합성을 건너뛴다 
  ✔ 소비(미리보기): 꺼져 있으면 아이콘을 그리지 않는다 
  ✔ 소비: 에디터가 두 줄이 다 있을 때만 2줄로 시드한다 
  ✔ 소비처 도달 — GET /api/automation 과 GET /api/credits 가 알림을 내려준다 
  ✔ 소수는 잘라서 정수로 넣는다 
  ✔ 소스가 비어 폴백해도 제목 상한을 넘지 않는다 
  ✔ 소스를 받은 뒤 길이를 재서 DB 에 백필한다 — 게이트만 통과시키고 정산 0 인 반쪽 금지 
  ✔ 소스에서 걷어진다 
  ✔ 소유자 확인이 파일 존재 확인보다 **먼저** 온다 
  ✔ 소재 고갈 즉시 발송의 전제 — 마지막 슬롯 경과 여부를 따로 알려준다 (2026-08-27) 
  ✔ 손상된 저장값은 던지지 않고 false — 실패 방향이 '로그인 거부'여야 한다 
  ✔ 쇼츠 구간이 먼저, 회차 통짜는 뒤로 — 지우지는 않는다 
  ✔ 쇼츠 만들기와 제목 클릭이 **같은 본체**를 쓴다 — 두 벌이면 갈라진다 
  ✔ 쇼츠 제목 1·2줄은 출력 기준 106px·107px이고 채널은 세로 82%다 
  ✔ 쇼츠는 해시태그를 넓게(핵심+인접 확장) 뽑되, 사실 날조는 본문에만 적용된다고 카브아웃한다 
  ✔ 숏폼 전용은 세로 + 길이 상한을 갖고 시작한다 
  ✔ 수동 채택 클립도 존중한다 — 목록에 섞여 있으면 막힌다 
  ✔ 수동 카드충전은 멱등키 + 테넌트 잠금으로 이중 결제를 막는다 
  ✔ 수익은 editor·cp 만 
  ✔ 순방 예약 계약 — 비-public 도 슬롯 시각을 보존하고, publishAt(공개 전환)은 public 에만 
  ✔ 순방 평가에 시스템 스코프(runAsSystem)가 없다 
  ✔ 순방: 아무것도 못 했고 사유가 전부 '안 풀리는' 쪽일 때만 exhausted 
  ✔ 순방과 같은 로고 기본값을 쓴다 — 다르면 '다시 찍었더니 처음과 모양이 다르다' 
  ✔ 순방은 **배포 행**으로 중복을 막는다 — 계획이 아니라 
  ✔ 순방은 사람이 누르는 배포와 같은 관문을 쓴다 
  ✔ 순방은 이미 열린 보류를 다시 쓰지 않는다 — 최초 사유가 보존돼야 한다 
  ✔ 순방의 슬롯 순번은 카운터(slotIndex)다 — 산식으로 쓰면 두 번 데였다 (소스 스캔) 
  ✔ 순방이 이 가드를 실제로 통과시킨다 — 소스 배선 고정 
  ✔ 숨기는 컨테이너들이 실제로 ID 로 display 를 잡고 있다 — 그래서 위 규칙이 필요하다 
  ✔ 숫자 문자열과 숫자를 받는다 
  ✔ 숫자가 아닌 기존 id 는 무시한다 
  ✔ 숫자를 그대로 쓴다 
  ✔ 스코프가 없으면 열린 라우트도 못 부른다 
  ✔ 스타일별 % 가 1:1 로 일치 
  ✔ 스트리밍 파일명 화이트리스트 — 목록 밖 이름은 전부 거른다 
  ✔ 슬롯 계획: 늦게 켜서 포기한 몫은 목표에서 빠진다 (staleMissed 와 같은 어휘) 
  ✔ 슬롯 계획: 다 나갔으면 published 가 목표에 닿는다 
  ✔ 슬롯 계획: 슬롯 전이면 목표 그대로·마감 전 
  ✔ 슬롯 합을 손으로 더하지 않는다 — perDayCount 한 벌만 쓴다 
  ✔ 슬롯은 지난 개수만큼만 허용한다 — 정각 편성을 순방 주기 위에서 표현하는 방법 
  ✔ 슬롯이 없으면 예전처럼 하루 할당량이다 
  ✔ 승계: 채택이 추천의 두 줄을 클립으로 넘긴다 
  ✔ 승인 대기가 채널 규칙 미달을 이긴다 
  ✔ 승인 대조 뒤에만 원장에 올린다 
  ✔ 승인 상한은 승인된 것들 중에서 센다 — 거절이 자리를 잡아먹지 않는다 
  ✔ 승인 큐(openHolds)·대기 판정(isHeldAwaitingHuman)에서 거부는 빠진다 
  ✔ 승인된 것만 골라 붙는다 — 섞여 있어도 
  ✔ 승인은 cp·pd 
  ✔ 시간 정보가 없는 쪽은 판정하지 않는다 (막지도, 터지지도 않는다) 
  ✔ 시스템 잡을 죽이지 않는다 — NOT EXISTS(<> active) 여야 한다 
  ✔ 시작·끝·25/50/75% 분위수가 기본으로 들어간다 
  ✔ 시작이 0이 아니어도 **길이** 기준으로 자른다 
  ✔ 시퀀스 이름에서 추천 id 를 되읽는다 — 만들 때와 같은 규칙 
  ✔ 시퀀스가 없어도 돈다 — 만들어 주려는 기능이 '활성 시퀀스 없음' 으로 막히면 안 된다 
  ✔ 시크릿이 다르면 거부 
  ✔ 시크릿이 비어 있으면 거부한다 — 검증을 건너뛰지 않는다 
  ✔ 식별자가 회사별로 갈린다 
  ✔ 실업로드 채널 목록도 서버 정본을 쓴다 — 안전 문구 역전 재발 금지 
  ✔ 실업로드 채널에는 '기록만' 이라고 말하지 않는다 
  ✔ 실업로드 채널은 실행 중 — channelPublishMode 와 같은 목록 
  ✔ 실제 채널만 통과한다 
  ✔ 실존 번호를 통과시킨다 
  ✔ 실측 12채널 기준 — 매 스윕 12개 → 2개 (83%↓) 
  ✔ 실패 메시지에 **단계 이름**이 붙는다 — 원문만으론 어디서 났는지 모른다 
  ✔ 실패 사유는 사람이 조치할 수 있는 문구다 — 포트원 body 의 pgMessage 를 꺼낸다 
  ✔ 실패 사유와 다음 행동이 실린다 — 자동 재시도가 없다는 사실까지 
  ✔ 실패 항목엔 '영상 열기' 버튼이 없다 — 열 영상이 없다 
  ✔ 실패가 있으면 프리헤더가 그 사실부터 말한다 — 열기 전에 보인다 
  ✔ 실패를 **직접 호출과 같은 분류**에 태운다 — 두 경로가 다른 판정을 내리면 안 된다 
  ✔ 실패를 boolean 으로 삼키지 않는다 — 사유를 읽어 분류한다 
  ✔ 실패의 성격을 나눈다 — 대기와 영구 실패를 뭉뚱그리지 않는다 
  ✔ 실패하면 예전처럼 그 시각으로 이동한다 — 아무 일도 안 일어나는 것보다 낫다 
  ✔ 실패한 기록은 already 로 치지 않는다 — 사람이 재시도할 길을 막으면 안 된다 
  ✔ 실행 중 계획만 평가한다 
  ✔ 썸네일 등 나머지 항목은 그대로 남는다 — 통째로 새로 만들지 않는다 
  ✔ 썸네일 선택은 사람 선택 → AI 생성물 → 렌더 프레임 3단이다 
  ✔ 썸네일 실패가 배포를 실패로 만들지 않는다 
  ✔ 썸네일을 떼면 파일이 확 줄어든다 — 자막은 수십 장을 내린다 
  ✔ 쓰기 스코프는 업로드가 실제로 쓰는 둘뿐이다 
  ✔ 아예 없으면 전부 알려준다 
  ✔ 아이디를 가려서 보여준다 
  ✔ 아이콘 좌표는 channelBadgeLayout 이 만든 것을 overlay 에 넘긴다 
  ✔ 아이콘이 없는 클립·editorState 없는 클립은 그대로 통과한다 
  ✔ 아직 진행 중인 계획의 적립분은 버퍼에 남는다 — 보낸 것만 뺀다 
  ✔ 알림 저장이 maybeAutoTopup 안에 있다 — 호출부별 배선이면 한쪽이 반드시 빠진다 
  ✔ 알림은 테넌트당 한 행이다 — 새 표 없이 기존 KV 를 쓴다 
  ✔ 애널리틱스 재동의가 필요한 채널은 계속 돈다 (의도) 
  ✔ 애널리틱스가 24시간을 넘기면 돈다 
  ✔ 액션 패턴을 지킨다 — 만들기만 하면 아무 일도 안 일어난다 
  ✔ 어느 계정에 붙는지 사람에게 보여준다 (자동 선택이든 수동 선택이든) 
  ✔ 어느 계정으로 정산되는지 사람에게 말한다 
  ✔ 어떤 경우에도 done/progress 로 시작하지 않는다 
  ✔ 어떤 소스도 link.coupang.com 으로 이동하지 않는다 
  ✔ 어절 단위 word 타이밍(whisper 모양)에 경계를 맞춘다 
  ✔ 언제 생긴 건지 못 읽으면 만료로 본다 — 모르면 '오늘' 이라고 주장하지 않는다 
  ✔ 업로드 가능 판정은 한 곳에서만 한다 — 문자열을 흩뿌리면 재연동해도 권한 없음이 난다 
  ✔ 업로드 직후 썸네일을 설정한다 
  ✔ 업로드가 끝난 뒤의 clip-finalize 도 같은 api() 를 지난다 — 여기서 401 이면 30분을 버린다 
  ✔ 없거나 잘못된 값은 null — 쿼리를 죽이지 않는다 
  ✔ 없는 1차는 거부하고, 고를 수 있는 값을 알려준다 
  ✔ 없는 2차는 거부하고, **그 1차의** 2차 목록만 알려준다 
  ✔ 없는 채널은 추가한다 
  ✔ 없는 키·폐기된 키는 막는다 
  ✔ 없으면 원본으로 타임라인을 만든다 — 마커는 시퀀스에 꽂히기 때문이다 
  ✔ 없으면 플래그를 **안 붙인다** — 'false' 와 '모른다' 를 섞지 않는다 
  ✔ 에디터 5-값 enum 을 전부 세로로 읽는다 
  ✔ 에디터 어휘를 프레임으로 좁힌다 
  ✔ 에디터 자막 미리보기가 GmarketSans 를 쓴다 
  ✔ 에디터가 서버 오버레이 PNG 를 content-hash 로 가져올 엔드포인트가 있다 
  ✔ 여러 서명 중 하나만 맞아도 통과한다 (시크릿 로테이션) 
  ✔ 여러 표기를 한 값으로 모은다 
  ✔ 역슬래시와 제어문자를 거부한다 
  ✔ 열어둔 라우트는 통과한다 
  ✔ 영구 실패(404·400)는 한 번에 확정한다 — 시간이 못 고치는 것을 기다리지 않는다 
  ✔ 영상 동기화가 6시간을 넘기면 돈다 
  ✔ 영상 열기 버튼이 항목별 실제 URL 을 단다 — 채널 홈이 아니라 그 영상으로 
  ✔ 영상 트랙이 없으면 **말해 준다** — 성공했다면서 아무 일도 안 일어나는 게 제일 나쁘다 
  ✔ 영상도 채운다 — 배율은 **바꾸기 전** 프레임 크기(=원본 해상도)에서 계산한다 
  ✔ 예약 순방과 지금 확인이 겹쳐도 같은 영상을 두 번 큐잉하지 않는다 
  ✔ 예약 시각은 5분 경계로 올림된다 — 유튜브가 격자 밖 시각을 거부할 수 있다 
  ✔ 예약 시각은 오프셋이 박힌 ISO 로 넘긴다 (KST 해석 여지 없이) 
  ✔ 예약 시각은 정규화 한 번 — 채널별 개별 파싱 금지 
  ✔ 예약 항목은 '예약' 점과 공개 예정 시각으로 표기된다 
  ✔ 예약어는 회사 id 로 못 쓴다 
  ✔ 오디오 트랙이 여럿이면 변환한다 — 안 하면 첫 트랙(모노 반쪽)만 나간다 
  ✔ 오디오가 없으면 -an — 무음 영상도 변환은 된다 
  ✔ 오래된 웹훅은 거부한다 (재전송 공격 창을 좁힌다) 
  ✔ 오래된 핸드오프는 버린다 — 어제 누른 게 오늘 패널에서 튀어나오면 안 된다 
  ✔ 오버레이 실패가 제목을 되돌리지 않는다 — 앞의 성과를 지킨다 
  ✔ 오타·빈값·미래 값이 넓은 권한으로 열리지 않는다 
  ✔ 오타·빈값·유사값은 전부 꺼진다 (실패 방향이 '안 나감' 이어야 한다) 
  ✔ 오타·빈값·유사값은 전부 꺼진다 (실패 방향이 '안 나감' 이어야 한다) 
  ✔ 오타·유사값은 꺼진다 — 실수로 자동 배포가 도는 쪽으로 기울지 않는다 
  ✔ 오프셋이 이미 있는 문자열은 손대지 않는다 — 명시된 TZ 는 존중 
  ✔ 올바른 서명은 통과한다 
  ✔ 왕복한다 
  ✔ 외곽선을 옮긴다 — 자막의 검정 스트로크가 빠지면 배경에 묻힌다 
  ✔ 외부 크리에이터 연결은 **읽기 전용**이다 
  ✔ 요약: 지난 예약을 '예약' 수에 넣지 않는다 
  ✔ 요일 문구는 설정만 싣는다 — dedupe 키라 오늘 요일 같은 변동값이 들어가면 안 된다 
  ✔ 요일 미지정은 매일이다 — 기존 계획이 이 변경으로 멈추면 안 된다 
  ✔ 요일은 ISO(월=1)로 읽는다 — 여기가 하루 밀리면 편성이 통째로 어긋난다 
  ✔ 우리 자산은 **묻지 않는다** — 권한 없는 플러그인 데이터 폴더에 둔다 
  ✔ 우리 주문이 없으면 올리지 않는다 (남이 만든 paymentId 방어) 
  ✔ 우리 코드는 12시간 변환을 하지 않는다 — 입력값을 그대로 보낸다 
  ✔ 우리 키가 아니면 조회조차 하지 않는다 
  ✔ 우선순위: 결제 이메일 → 구매자(사업자/청구) 이메일 
  ✔ 운영·정산 경로는 전부 본다 — user 스코프는 사용자 라우트 한 곳뿐 
  ✔ 워커 핸들러는 실패를 던진다 — 삼키면 잡이 done 이 되어 회수할 실패가 없다 
  ✔ 워커가 그 경로로 올린다 
  ✔ 워크스페이스 역할로는 배포가 열리지 않는다 
  ✔ 원가가 판매가(크레딧 ₩28/분)보다 낮다 — 역마진 감지 
  ✔ 원가는 실측 기준으로 계산된다 — 프로덕션 구성(자막읽기 OFF) 
  ✔ 원문 오류는 상태에 남는다 — 개발자가 볼 곳이 사라지면 안 된다 
  ✔ 원본 클립은 찾은 자리에서 쓴다(콜백) — 돌려주지 않는다 
  ✔ 원본 폴더는 세션 캐시 — 토큰이 안 풀려도 창이 반복되지 않는다 
  ✔ 원본 확보 → 타임라인 확보 → 마커 순서다 
  ✔ 원본을 **파일명으로** 찾는다 — 경로는 PC 마다 다르다 
  ✔ 원본을 변형하지 않는다 
  ✔ 원장 insert 가 dedupe_key 로 멱등이다 — 웹훅 재전송·워커 재시도에 두 번 오르지 않는다 
  ✔ 월 예상 건수는 판정과 같은 함수에서 나온다 — 화면 숫자와 실제가 갈라지지 않게 
  ✔ 월 파라미터를 검사한 뒤에 쿼리한다 
  ✔ 월 한도를 넘기면 멈춘다 
  ✔ 웹 designStageH·outputHeight 가 서버 renderDims(stageH·H)와 1:1 (마이그레이션 계수 basis) 
  ✔ 웹 미러(TEMPLATE_SEED_UI)가 서버 시드와 같은 색이다 — 갈라지면 미리보기가 거짓말한다 
  ✔ 웹 카드가 account 를 붙여 내려받는다 
  ✔ 웹 프록시를 거치는 호출도 키를 낼 자리가 있다 (x-api-key) 
  ✔ 웹과 **같은 렌더러**를 쓴다 — 복제하면 두 경로가 갈라진다 
  ✔ 웹과 같은 3단계를 그대로 탄다 
  ✔ 웹에 그 @font-face 가 실제로 있다 — 없으면 미리보기만 조용히 폴백한다 
  ✔ 웹은 **남기고 나서** 앱을 띄운다 — 순서가 바뀌면 패널이 빈손으로 깨어난다 
  ✔ 웹훅은 일시적 실패에만 503 을 돌려 재전송을 살린다 
  ✔ 위치는 0..1 정규화 좌표로 들어간다 
  ✔ 윈도우2 런처가 commerce 레인을 함께 돈다 
  ✔ 유니코드는 정규화 후 비교 — 자소 분리된 한글도 같은 비밀번호로 취급 
  ✔ 유튜브 가져오기 경로(worker.ts)는 idle + 사유 문구뿐이다 — 그것도 잡는다 
  ✔ 유튜브 숏폼 프리셋으로 자르면 출력이 60초를 절대 안 넘는다 (무작위 구간) 
  ✔ 유튜브 업로드가 publishAt 을 받으면 private 로 올린다 (유튜브가 스스로 공개) 
  ✔ 유효한 값만 쓴다 (오타·빈값·과소는 기본값) 
  ✔ 유휴 사유는 하루 한 줄로 막되, **판정은 dedupe 와 무관하게** 돌려준다 
  ✔ 음수(차감)도 된다 
  ✔ 음수·NaN 은 통과가 아니라 거부다 
  ✔ 음수·비숫자는 기본값으로 — 과거 시각으로 즉시 공개되는 일이 없어야 한다 
  ✔ 음절 단위 word 타이밍(프로덕션 Soniox 모양)에도 경계를 맞춘다 
  ✔ 응답에서 빠진 영상(삭제됨)을 순서로 매칭하지 않는다 
  ✔ 이동뿐 아니라 하위 리소스까지 막는다 (iframe 은 navigation 이 아닐 수 있다) 
  ✔ 이름 규칙이 **한 곳**이다 — 두 곳에서 만들면 갈라지는 순간 빈 시퀀스가 나온다 
  ✔ 이름 변경 라우트가 없어야 한다 
  ✔ 이름에 추천 id 를 넣는다 — 지연 액션이라 만들어진 항목을 못 돌려받는다 
  ✔ 이름으로 먼저 찾는다 — 이름은 await 없이 읽힌다 
  ✔ 이름은 id 에 영향을 주지 않는다 
  ✔ 이메일 오타를 잡는다 
  ✔ 이메일 오타를 잡는다 
  ✔ 이메일 오타를 잡는다 
  ✔ 이메일 형식이 아니면 다음 후보로 넘어간다 
  ✔ 이미 .mp4 면 비켜 간다 
  ✔ 이미 mp4 인 파일은 종전 경로(작으면 faststart 리먹스) 그대로 
  ✔ 이미 그 폴더 안이면 막는다 
  ✔ 이미 나간 클립은 대상에서 뺀다 
  ✔ 이미 있으면 다시 넣지 않는다 — 중복 등록이면 어느 쪽이 쓰이는지 알 수 없다 
  ✔ 이미 차감된 진행 중 행은 다시 물리지 않는다 (더블클릭 이중 차감 방지) 
  ✔ 이미 처리된 충전은 다시 올리지 않는다 
  ✔ 이상한 배속에도 창이 뒤집히지 않는다 
  ✔ 이어 쓰기를 못 하는 버전에서는 **큰 파일을 거부**한다 — 조용히 메모리를 터뜨리지 않는다 
  ✔ 이전 상태가 없어도 새 시드를 그대로 돌려준다 
  ✔ 인벤토리를 로그로 남긴다 — 첫 실파일에서 무엇이 들어 있었는지가 판단 근거다 
  ✔ 인보이스 라우트가 결제에서 만든다 
  ✔ 인보이스가 사업자정보를 싣는다 
  ✔ 인보이스용 앱 디렉토리 복사도 남아 있다 — 지우면 인보이스 PDF 만 조용히 실패한다 
  ✔ 인자를 안 주면 채널 전체를 센다 — 표시용 질의의 축은 남긴다 
  ✔ 인증 자세를 '없다' 고 말하지 않는다 — 안심시키는 방향의 드리프트가 제일 위험하다 
  ✔ 인터레이스(1080i)는 변환한다 — 빗살무늬가 결과물에 박힌다 
  ✔ 인터레이스일 때만 yadif — progressive 에 걸면 디테일이 깎인다 
  ✔ 일 한도에 걸리면 멈춘다 — 조용히 계속 긁지 않는다 
  ✔ 일반결제 채널키로는 열리지 않는다 
  ✔ 일시정지는 새 회차를 잡지 않는 것이다 
  ✔ 일을 했으면 사유가 없다 
  ✔ 읽는 문구는 어그로 카피이지 훅 대사가 아니다 
  ✔ 읽는 쪽 게이트(liveAutoTopupAlert)가 만료분을 null 로 만든다 
  ✔ 임계 위면 충전하지 않는다 
  ✔ 있으면 플래그를 붙인다 
  ✔ 자격증명 blob 이 계정 공용 SELECT 목록에 없다 
  ✔ 자격증명은 OS 키체인(secureStorage)에만 둔다 — 평문 파일 금지 
  ✔ 자격증명이 없으면 시도조차 안 한다 
  ✔ 자기 자신·하위로 옮길 수 없다 
  ✔ 자동 결제 정책은 고정이고, 그 고정값 자체가 안전 불변식을 만족한다 
  ✔ 자동 결제 켜짐은 저장된 on/off 가 아니라 '쓸 수 있는 카드' 로 판정한다 
  ✔ 자동 충전 결제는 수동 저장카드 충전과 같은 안전 경로를 쓴다 
  ✔ 자동 충전은 billing:write 로 열린다 (2026-08-21) 
  ✔ 자동배포 미리보기도 같은 글꼴로 그린다 
  ✔ 자동배포: 길이 게이트와 채택이 adoptAspect 한 벌을 본다 
  ✔ 자동배포가 가로형을 골라도 제목 출력 크기는 106px·107px로 유지된다 
  ✔ 자동배포가 클립을 채택하면 썸네일 생성을 건다 
  ✔ 자동배포는 별도 채널 설정에 의존하지 않고 실제 연결 계정을 읽는다 
  ✔ 자동배포는 카테고리를 박아 넘기지 않는다 — 워커가 프로그램·장르로 푼다 
  ✔ 자동화 흔적을 숨긴다 
  ✔ 자리표시자를 막는다 
  ✔ 자릿수는 맞는데 검증에 걸리는 경우를 구분해 말한다 
  ✔ 자막 mogrt 는 썸네일을 뗀다 — 수십 장을 내리는 자리다 
  ✔ 자막 기본 세로 위치 — 미리보기 y 와 서버 CAPTION_MV_PCT 가 1:1 
  ✔ 자막 기본 크기 — 미리보기 size 와 서버 CAPTION_PCT.korean_pop 이 1:1 
  ✔ 자막 기본색이 흰색 — 렌더 korean_pop 기본색과 일치 
  ✔ 자막 실패가 정규화를 막지 않는다 — try/catch 로 감싼다 
  ✔ 자막 이벤트는 \q1(그리디 줄바꿈)로 접는다 
  ✔ 자막(시간축 수백 개)은 여전히 ASS Caption 이벤트로 남는다 (하이브리드) 
  ✔ 자막도 **편집 가능한 mogrt 가 먼저**다 — PNG 는 폴백이다 
  ✔ 자막은 GCS 에 보관하고, 없으면 STT 로 진행한다고 로그에 남긴다 
  ✔ 자막은 V4 — 제목·로고 위 트랙이다 
  ✔ 자막이 없어도 프롬프트가 만들어진다 — 대사 없는 클립도 메타가 필요하다 
  ✔ 자막읽기를 켠 값은 두 배 — 두 구성을 나란히 둔다 
  ✔ 자식 경로를 만든다 
  ✔ 자정 경계 — KST 자정은 UTC 로 전날 15시다 (날짜가 하루 밀리는 지점) 
  ✔ 자정은 00 이다 (h23 — 런타임에 따라 24 로 나오는 걸 막는다) 
  ✔ 작업 대상은 **보이는 것 중 체크된 것**이다 — 목록 전체가 아니다 
  ✔ 작은 계획은 종전 그대로 3건/틱 
  ✔ 잔액 부족이면 자동충전 선시도 후에도 부족할 때 credits 코드로 스킵한다 
  ✔ 잔액은 원장 합계로 다시 센다 — 캐시하지 않는다 
  ✔ 잔액이 부족하면 거부하고 몇 개 모자란지 말한다 
  ✔ 잘못된 값은 400 으로 막는다 (조용히 버리지 않는다) 
  ✔ 잡 테넌트와 계정 테넌트를 대조한다 
  ✔ 잡 테넌트와 계정 테넌트를 대조한다 (naver.publish 와 같은 가드) 
  ✔ 잡이 레인에 등록돼 있다 (레인 밖이면 아무도 안 집는다) 
  ✔ 장르 판정이 clip-metadata 의 장르팩과 어긋나지 않는다 
  ✔ 장르가 유도한 카테고리도 전부 표에 있다 
  ✔ 재등록이 해지 표시를 지운다 
  ✔ 재렌더 대기로 되돌린다 — rendered:false 가 없으면 옛 파일이 그대로 나간다 
  ✔ 재생은 사람 몫이라고 **말해 준다** — UXP 에 트랜스포트 API 가 없다 
  ✔ 재순방에 중복 큐잉이 없다 — 계정별 기록이 계정별 already 판정을 성립시킨다 
  ✔ 재측정한 길이로 크레딧을 **다시** 잰다 
  ✔ 저장 경로는 그대로다 — 기록을 바꾸지 않는다 
  ✔ 저장 폴더는 **한 번만** 묻고 기억한다 — 회차마다 묻는 도구는 아무도 안 쓴다 
  ✔ 저장은 숫자만, 표시만 하이픈 
  ✔ 저장이 검증을 거치고 사유를 요구한다 
  ✔ 적립 항목이 계획 id 를 지닌다 — 없으면 나눌 축이 없다 
  ✔ 전부 superadmin 세션을 요구한다 
  ✔ 전부 통과하면 ok 
  ✔ 전부 통과해야 긁는다 
  ✔ 전부 후기 0 이면 1위를 쓴다 — 빈손보다 낫다 
  ✔ 전화번호 자릿수를 본다 
  ✔ 전환은 className 을 통째로 갈아끼운다 — 다른 클래스가 섞이면 이 방식이 깨진다 
  ✔ 절대 상한 — 정책값이 미쳐도 월 500만원·일 10회에서 멈춘다 
  ✔ 절대 시각이 보존된다 — 기록이 흐려지지 않아야 한다 
  ✔ 절반 이하로 스치는 구간·전혀 다른 구간은 통과한다 
  ✔ 점수 높은 순으로 뽑고, 점수 없는 후보는 뺀다 
  ✔ 점수 미달(사람 몫)이 렌더 대기(시간 몫)를 이긴다 
  ✔ 점수 미달이 종류 불일치를 이긴다 
  ✔ 점수 하한 축이 코드에서 사라졌다 — 채택은 항상 상위 순 (2026-08-26) 
  ✔ 점수가 낮아도 뽑는다 — 하한이 회차 전량을 막아 세우던 함정을 없앴다 
  ✔ 점으로 시작하는 이름을 막는다 
  ✔ 접두만 저장·표시한다 
  ✔ 접두사가 같다고 하위가 아니다 
  ✔ 접두사가 없으면 제목을 건드리지 않는다 
  ✔ 접두사를 두 번 붙이지 않는다 
  ✔ 접두어가 붙어도 제목 상한을 넘지 않는다 
  ✔ 정규화 — 문자열(구형)=1개, 객체=count, 시각순 정렬·중복은 뒤가 이김 
  ✔ 정규화 산출물 경로는 원본과 절대 같지 않다 
  ✔ 정규화 전 원본 메타·URL 을 붙잡아 둔다 — 변환 뒤엔 원본을 못 본다 
  ✔ 정렬 3축 
  ✔ 정렬을 뒷받침하는 인덱스가 있다 
  ✔ 정렬이 원본 배열을 바꾸지 않는다 
  ✔ 정상 대기·진행은 analyzing, 완료는 analyzed, 실패는 failed 
  ✔ 정상 사유는 알림을 만들지 않는다 — 꺼짐·잔액 충분·정산됨·충전됨·카드 미등록 
  ✔ 정상 입력은 계획을 돌려준다 
  ✔ 정상 입력을 통과시킨다 
  ✔ 정상 조합은 정식 이름으로 확정된다 
  ✔ 정상 키는 통과 
  ✔ 정수 아닌 값·0·음수를 거부한다 
  ✔ 정지된 회사의 키는 죽는다 
  ✔ 정체성 없는 쓰기(기록 전용 채널·레거시)는 채널 단독 매칭을 유지한다 
  ✔ 정체성 필드에 upload 모드 조건이 없다 — record 행에도 계정이 남아야 한다 
  ✔ 정확히 상한이면 통과한다 
  ✔ 제공자별로 키가 다르다 — 하나가 새도 다른 쪽이 안 열린다 
  ✔ 제목 비율도 배치를 따른다 — 프레임만 보면 crop-main/sub 를 구분 못 한다 
  ✔ 제목 삽입 실패가 마커까지 버리지 않는다 — 앞의 성과를 지키는 자리다 
  ✔ 제목 상한을 넘기지 않는다 — 단어 중간에서 끊지 않는다 
  ✔ 제목 재생성의 실패 경로 4개가 모두 message 를 담는다 
  ✔ 제목 줄 기본 글꼴이 지마켓 — 계획에서 고른 게 있으면 그게 이긴다 
  ✔ 제목 첫 줄 106px·둘째 줄 107px를 출력 basis로 시드한다 
  ✔ 제목 해시태그 상한이 다른 태그로 다 차도 #Shorts 는 무조건 붙는다 (사용자 2026-08-20) 
  ✔ 제목 해시태그가 관행이 아닌 채널에는 붙이지 않는다 
  ✔ 제목+채널 아이템을 1080×1920 투명 PNG 로 렌더한다 
  ✔ 제목·시간박스는 Pretendard 그대로 — 바꾼 건 자막뿐이다 
  ✔ 제목·채널 CSS 폰트가 line.size / labelPx(출력 px)를 그대로 쓴다 (곱셈 없음) 
  ✔ 제목은 V2 트랙에 올린다 — V1 영상 위에 얹혀야 보인다 
  ✔ 제목은 블록 폭에서 미리 접는다 
  ✔ 제목은 빠진다 — 그건 고칠 수 있어야 해서 .mogrt 로 나간다 
  ✔ 제휴 단축 링크만 통과한다 
  ✔ 조각이 빈틈 없이 이어지고 원래 구간을 벗어나지 않는다 
  ✔ 조정 라우트가 판정을 거친다 
  ✔ 조정은 원장에 INSERT 만 한다 
  ✔ 조치가 필요한 실패는 알림 + 할 일(hint) 을 같이 남긴다 
  ✔ 조치는 대사로만 — 잔액 숫자는 넣지 않는다 (사용자 2026-08-27) 
  ✔ 조치는 사유마다 다르다 — 못 지킬 안내를 고정으로 붙이지 않는다 
  ✔ 조치한 자리에서 알림을 해제한다 — 카드 재등록·카드 삭제·직접 충전 
  ✔ 조회 실패를 성공으로 치지 않는다 
  ✔ 조회는 막지 않는다 
  ✔ 종류 불일치가 겹침을 이긴다 
  ✔ 종류가 다르면 별개다 
  ✔ 종류가 안 맞으면 그 종류를 말한다 — 화면(자동배포 KIND_LABEL)과 같은 어휘로 
  ✔ 좌/우 정렬은 블록 안에서 움직인다 (titleX 에 직접 앵커하지 않는다) 
  ✔ 주 버튼은 **쇼츠 만들기** 하나 — 마커는 보조로 내렸다 
  ✔ 죽은 이름·오타를 거른다 
  ✔ 줄이 더 많으면 마지막 레이어에 **합친다** — 버리지 않는다 
  ✔ 중복 URL·이름 없는 항목·잘못된 형식을 걸러낸다 
  ✔ 중첩되면 안쪽이 이긴다 — 잡이 다른 테넌트로 갈아타는 경우 
  ✔ 지난 잡이 failed 면 그 실패를 순방에 돌려준다 — 성공으로 치지 않는다 
  ✔ 지마켓 폰트를 fontconfig 경로로 복사한다 — 앱 디렉토리 복사만으로는 libass 가 못 본다 
  ✔ 지워진 계획의 클립을 현재 계획이 상속한다 
  ✔ 진행률을 미리 채우지 않는다 
  ✔ 짧게 나뉜 조각은 이웃에 병합한다 — 단 한 줄 예산 안에서만 (최소 노출 보장) 
  ✔ 차감 원장: -publishCost · reason publish · chargeKey·차감량을 행에 남긴다 
  ✔ 차감된 행만, 행에 박힌 차감량으로, chargeKey dedupe 로 한 번만 환급한다 
  ✔ 차감량은 publishCredits(env PUBLISH_CREDITS · 기본 3)에서 온다 
  ✔ 채널 세로 위치 82%·자막 세로 위치 26%를 명시한다 
  ✔ 채널 판정 미달은 사용자가 실제로 해결할 수 있는 편집기로 안내한다 
  ✔ 채널명 x는 최종 렌더와 같은 실제 아이콘 박스를 쓴다 
  ✔ 채널별 메타 없이는 게시하지 않는다 (생성 대기 후 다음 순방에 게시) 
  ✔ 채널이 늘면 예상 건수도 는다 — 할당량이 채널당이기 때문 
  ✔ 채택 경로는 세 곳뿐이다 — 사람 · 공장 · 자동 배포 규칙 
  ✔ 채택 안 한 추천은 미디어에 없다 
  ✔ 채택 여부를 **표시**한다 — 숨기는 대신 보여 주고 판단은 사람이 
  ✔ 채택 직후 clip.metadata 잡을 큐잉한다 — 수동 채택과 같은 배선 
  ✔ 채택 직후 렌더를 건다 — 안 걸면 not_rendered 로 매 순방 스킵되어 자동 게시가 0건 
  ✔ 채택 트랜잭션을 직접 부르는 곳은 adopt.ts 하나뿐이다 
  ✔ 채택된 추천이 미디어를 두 번 만들지 않는다 
  ✔ 채택만 한 클립은 배포 대상이 아니다 
  ✔ 채택은 커밋을 반드시 거친다 
  ✔ 처음 쓰는 키는 바로 기록한다 
  ✔ 첫 실패로 포기하지 않는다 
  ✔ 첫 트랙이 스테레오면 그 트랙만 쓴다 — 나머지는 M&E·예비다 
  ✔ 청구액을 인보이스가 되쪼갤 때 1원도 안 샌다 — 만들기/쪼개기 왕복 
  ✔ 청크는 XHR 로 보낸다 — fetch 는 308(Resume Incomplete)을 삼켜 이어받기가 깨진다 
  ✔ 청크로 받아 이어 쓴다 — 통째로 올리면 프리미어까지 같이 죽는다 
  ✔ 초기 크레딧은 0..상한 정수로 눌린다 
  ✔ 초기 크레딧은 grant 다 — topup 과 섞으면 매출이 부풀어 보인다 
  ✔ 초대 기본값은 출발점이지 관리 권한의 승계가 아니다 
  ✔ 초대를 같은 연결(db)로 넘긴다 
  ✔ 총액을 부가세 포함으로 본다 
  ✔ 최근에 둘 다 돌았으면 건너뛴다 — 헛돌이의 본체 
  ✔ 최상위 card 경로도 지원한다 
  ✔ 최상위(GET /payments) 응답도 그대로 대조한다 
  ✔ 최소 두 패밀리(pretendard·gmarket)를 등록하고 각 파일을 가리킨다 
  ✔ 추가 인증(캡차·2차)은 자격증명을 남긴다 — 비번은 맞을 수 있다 
  ✔ 추천 순서 그대로 늘어놓는다 — 점수 순 목록이 곧 편집 순서다 
  ✔ 추천이 아무리 많아도 클립이 없으면 미디어는 비어 있다 
  ✔ 추출은 두 경로를 순서대로 — 자막 트랙 → 영상 임베드 CEA-608 
  ✔ 충전 경로는 원장을 먼저 쓰고 상태를 나중에 찍는다 
  ✔ 충전 주문 상태를 멱등 기준으로 쓰지 않는다 
  ✔ 충전량 ≤ 임계면 긁지 않는다 — 충전해도 임계 아래라 하루 한도까지 연속 과금된다 
  ✔ 카드 등록 준비가 설정·고객정보를 먼저 본다 
  ✔ 카드 등록·자동충전 설정은 세션 매니저 또는 billing:write 키만 
  ✔ 카드 표는 RLS 대상이라 스코프 있는 풀을 쓴다 
  ✔ 카드가 없거나 해지됐으면 못 긁는다 
  ✔ 카드사 사유가 없으면 **사람 말 폴백** — 우리 원문을 알림으로 내보내지 않는다 
  ✔ 카드정보를 못 찾으면 조용히 null (표시만 못 할 뿐 저장은 된다) 
  ✔ 카테고리가 필요한 채널은 사람에게 묻는다 — 자동 판정하지 않는다 
  ✔ 칸: 미래 예약은 **언제인지**를 보여준다 
  ✔ 칸: 지난 예약을 구분하고, 게시됨으로 단정하지 않는다 
  ✔ 캡슐 id 는 **요청마다 다르다** — 같으면 프리미어가 첫 제목을 캐시해서 재사용한다 
  ✔ 캡슐 id 는 추천마다 다르다 — 같으면 두 번째 제목이 첫 문구로 뜬다 
  ✔ 커스텀 프롬프트 2종이 저장(PATCH)→전달(program_context.json)→core 로 이어진다 
  ✔ 컨텍스트 밖에서 currentScope()는 던진다 
  ✔ 컨텍스트 밖에서 currentTenantId()도 던진다 
  ✔ 켜져 있으면 통과한다 
  ✔ 켜져 있으면 통과한다 
  ✔ 콘솔을 넓혀도 충전은 함께 열리지 않았다 
  ✔ 콜백 4종이 전부 oauthStateGuard 를 지난다 
  ✔ 콜백이 state 를 직접 base64 로 까지 않는다 
  ✔ 쿠키가 먼저다 — 헤더가 브라우저 세션을 가로채지 못한다 
  ✔ 쿠키와 헤더를 한 곳에서 읽는다 — 검증 경로는 resolveSession 하나 
  ✔ 쿠팡 **상품** URL 은 거부한다 — 정산이 안 되는 맨 링크다 
  ✔ 쿠팡 도메인 쿠키만 올린다 
  ✔ 쿼리스트링이 붙어도 같은 라우트다 
  ✔ 큐 선행(slotsReadyForQueue)도 개수 합 — 2시간 리드 안이면 그 칸 전량 
  ✔ 큐잉 안 됨이 분석 실패·분석 중을 이긴다 — 가장 오래 조용한 상태다 
  ✔ 크레딧 × 단가 — 단가는 공급가액이고 청구액엔 부가세가 붙는다 
  ✔ 크레딧 잔액 0 이하면 순방이 정지하고 사유가 공용 상수다 
  ✔ 크레딧 정지도 로그에 남는다 — 배너는 지나간 시간을 설명하지 못한다 
  ✔ 크레딧 표는 RLS 대상이라 시스템 스코프로 읽는다 
  ✔ 클립 행을 INSERT 하는 곳은 db-pg.ts(commitAdoption) 뿐이다 
  ✔ 클립당 export 는 순방 한 번 — 채널 수만큼 때리면 카운터가 배수로 부푼다 
  ✔ 클립도 자동배포 후보가 된다 — core 의 type 을 서버가 kind 로 보존한다 
  ✔ 키 경로가 별도 스코프 우회를 만들지 않는다 
  ✔ 키 경로도 keyBlockReason 과 checkRoute 를 둘 다 탄다 
  ✔ 키 순서가 달라도 같은 모양이면 같은 지문 — 아니면 저장할 때마다 전량 재렌더된다 
  ✔ 키가 없으면 저장을 거부한다 — 평문 폴백 없음 
  ✔ 키가 없으면 저장을 거부한다 — 평문 폴백 없음 
  ✔ 타임라인 비율대로 받는다 — 세로 러프컷에 가로 그림을 얹으면 위치가 어긋난다 
  ✔ 탈락 사유는 '점수 없음' 하나뿐 — 기준을 낮추라는 안내는 없다 (2026-08-26 하한 제거) 
  ✔ 태그 필드가 없는 채널에는 태그를 보내지 않는다 — 해시태그로 녹인다 
  ✔ 테넌트·날짜·슬롯이 다르면 다른 값 — 정상 재충전을 막지 않는다 
  ✔ 테스트 키를 라이브로 오인하지 않는다 
  ✔ 텍스트 레이어 수를 센다 — 없으면 제목을 넣을 자리가 없다 
  ✔ 토큰은 x-stepd-client 를 보낸 호출자에게만 응답에 실린다 — 웹 응답은 그대로(HttpOnly 유지) 
  ✔ 토큰을 URL 인코딩한다 
  ✔ 통계 3칸 — 템플릿 그대로 배포·게시·확인 필요 (사용자: '보낸 html 똑같이') 
  ✔ 통계가 실패를 '확인 필요' 로 세고 '게시' 에서 뺀다 
  ✔ 트랙 미지정 프로그램은 폼 기본값을 비워 둔다 
  ✔ 트랙은 제목보다 위다 — 시간박스가 제목 뒤로 가면 안 된다 
  ✔ 트랙이 V1 뿐이면 **그게 이유라고** 말한다 — 트랙 추가 API 는 없다 
  ✔ 트랜잭션 **하나**에 다 담는다 — Ctrl+Z 한 번으로 전부 되돌리게 
  ✔ 틀린 비밀번호는 막힌다 
  ✔ 파라미터 번호를 이어 붙인다 
  ✔ 파일명 규칙이 실제 이름들에 맞는다 (브라우저 중복 접미사 포함) 
  ✔ 판정 불가면 **아무 말도 하지 않는다** — 근거 없는 경고는 소음이다 
  ✔ 판정~과금이 테넌트 잠금 안에서 돈다 
  ✔ 판정마다 기계 판독 code 를 준다 — 문구가 바뀌어도 소비처가 안 깨진다 
  ✔ 판정은 code 한 곳에서만 한다 — 호출부의 사유 문구 정규식을 되살리지 않는다 
  ✔ 판정이 **무거운 일을 시작하기 전**에 선다 
  ✔ 패널은 작업 중에 폴링하지 않는다 — 업로드·렌더 화면이 가려진다 
  ✔ 편집 가능한 경로가 먼저다 — 사용자 요구: "편집자가 바꿀 수 있길 원한다" 
  ✔ 편집자 본인 타임라인은 건드리지 않는다 — 우리 것만 맞춘다 
  ✔ 폐기는 행을 지우지 않는다 
  ✔ 포트원 env 가 하나라도 비면 결제 경로를 열지 않는다 
  ✔ 폰트 파일을 못 읽으면 빈 이름 — 템플릿 글꼴을 유지시킨다 
  ✔ 폰트 파일이 신고하는 패밀리는 'Gmarket Sans TTF' · 굵기는 Bold 
  ✔ 폴링: 지난 예약으로 무한 폴링하지 않는다 
  ✔ 표시 문구에 카드 번호가 없다 
  ✔ 표시가 오전/오후와 자정·정오를 말로 못 박는다 
  ✔ 표준 mp4 는 건드리지 않는다 — 재인코딩은 화질 손해다 
  ✔ 프레임 이름은 소스 절대초 ms — 정렬이 자명하다 
  ✔ 프레임을 모르면 프레임 때문에 막지는 않는다 
  ✔ 프로그램 brandIcon 과 같으면 뺀다 (렌더가 폴백으로 같은 결과를 낸다) 
  ✔ 프로그램 해시태그가 제목 끝에 붙는다 
  ✔ 프로그램·태그가 전무해도 최소 #Shorts 한 개는 붙는다 — 해시태그 0개로는 안 나간다 
  ✔ 프로그램을 못 찾으면 보존한다 — 모르면 안 지운다 
  ✔ 프로그램이 없으면 base.tags(등록 캐스트·인물)로 채우고 #Shorts 를 보루로 둔다 
  ✔ 프로브 → 판정 → 변환 순서 · 서명 URL 을 입력으로 준다(원본을 RAM 에 안 받는다) 
  ✔ 프로젝트에 있으면 그대로, 없으면 받아서 가져온다 
  ✔ 프리미어를 직접 만진다 — 여기부터가 웹 화면이 아니라 플러그인이다 
  ✔ 프리셋이 없으면(=null·0·NaN) 종전대로 통째로 렌더한다 — 무회귀 
  ✔ 프리헤더가 소재 부족을 먼저 말한다 — 열기 전에 보인다 
  ✔ 플래그만 다르고 나머지는 그대로 — 켜면 블록이 더해질 뿐이다 
  ✔ 플랜 무효(reframe_plan_invalid)는 대기가 아니다 — 아무도 안 잡으면 영구 침묵이다 
  ✔ 플러그인 ID 는 마켓플레이스 등록 후 못 바꾼다 — 지금 값으로 고정 
  ✔ 하나라도 없으면 결제창을 안 띄운다 
  ✔ 하루 20건이면 리드의 절반(60분·4틱) 안에 끝나는 페이스 
  ✔ 하루 몫을 넘겨 쏟지는 않는다 — 폭탄 방지라는 원래 목적은 그대로 
  ✔ 한 단어가 상한보다 길어도 자르지 않는다 
  ✔ 한 문장에 렌더·영상 변환을 섞지 않는다 — 화면 어휘는 '렌더'·'확정(렌더)' 다 
  ✔ 한 번도 안 돈 채널은 즉시 돈다 
  ✔ 한 번도 안 집힌 회사가 맨 앞에 온다 
  ✔ 한 번에 살 수 있는 상한이 있다 (0 하나 더 붙는 사고 방지) 
  ✔ 한 번에 움직일 수 있는 양에 상한이 있다 
  ✔ 한 자리 월·일에 0 을 채운다 
  ✔ 한 자리만 틀려도 잡는다 
  ✔ 한 줄 예산을 넘기면 병합하지 않는다 — 짧은 구간도 여러 줄로 뭉치지 않는다 (사용자 2026-08-21) 
  ✔ 한 줄을 0초에 놓고 한 프레임 뜬다 — 그래야 그 줄이 그림에 나온다 
  ✔ 한 채널이라도 나갔으면 손대지 않는다 — 채널과 DB 가 갈라진다 
  ✔ 한 통이 실패해도 나머지는 나가고, 실패분만 남는다 
  ✔ 한 회차라도 상한이 아니면 상한 탓으로 돌리지 않는다 
  ✔ 한글·공백·확장자는 통과한다 
  ✔ 한도를 판정하는 세 호출부가 전부 rule.id 를 넘긴다 
  ✔ 할당량 계획: 목표 = dailyQuota · 마감 = activeEnd 
  ✔ 할당량 방식(슬롯 없음)도 같은 산식을 탄다 
  ✔ 할당량 소진이 점수 미달을 이긴다 
  ✔ 합계가 줄 금액의 합과 같다 
  ✔ 합성 순서가 렌더와 같다 — 텍스트 PNG → ASS → 배지 
  ✔ 항목 제목이 이스케이프되어 담긴다 — HTML 주입 방지 
  ✔ 해소되면 알림을 지운다 — 해결된 경고가 남으면 다음부터 아무도 안 본다 
  ✔ 해시는 결정적이고 평문과 다르다 
  ✔ 해시에 알고리즘·파라미터가 남아 나중에 교체할 수 있다 
  ✔ 해시태그 소스가 하나도 없어도 제목은 #Shorts 로 끝난다 (프로그램 주어지면 #프로그램도) 
  ✔ 해시태그 자리를 먼저 확보한 뒤 본문을 자른다 — 해시태그가 잘리면 안 된다 
  ✔ 해시태그 템플릿의 프로그램명에서 공백을 뺀다 
  ✔ 해제는 한 함수로만 한다 — 라우트마다 배선하면 반드시 한 자리가 빠진다 
  ✔ 해지가 빌링키를 비운다 
  ✔ 행 잠금이 job_queue 에만 걸린다 
  ✔ 허용 누적(slotsElapsed)은 지난 슬롯의 개수 합이다 
  ✔ 헤더가 없으면 거부 
  ✔ 형식이 깨진 링크만 있으면 블록 자체를 안 만든다 
  ✔ 형식이 아니거나 없으면 오늘 
  ✔ 형제로는 옮길 수 있다 
  ✔ 호출 시점에 env 를 읽는다 — 재배포로 끌 수 있어야 한다 
  ✔ 호출 시점에 env 를 읽는다 — 재배포로 끌 수 있어야 한다 
  ✔ 화면 밖 좌표는 0..1 로 잘린다 — 프리미어가 이상한 위치로 튀지 않게 
  ✔ 화면은 보류를 클립 단위로 접고, 승인은 그 영상의 보류를 전부 푼다 
  ✔ 화면이 입력값을 state 에 남기지 않는다 
  ✔ 화이트리스트에 관리 경로가 섞여 있지 않다 
  ✔ 화이트리스트의 숫자·색 필드는 factory 시드(layoutOverride)도 안다 
  ✔ 확대 실패는 전체를 실패시키지 않는다 — 시퀀스는 이미 세로다 
  ✔ 확인 보류는 사람 말로 — 결제사 상태값·우리끼리 쓰는 말을 사용자에게 내보내지 않는다 
  ✔ 확장자가 다르면 종전대로 확장자만 바꾼다 
  ✔ 확장자와 MIME 둘 다로 판정한다 
  ✔ 확정 뒤에는 매 순방 다시 때리지 않고, 한 시간에 한 번 다시 본다 
  ✔ 확정 문구는 사람 말로, 다음 행동까지 말한다 
  ✔ 확정 문구에 개발자 어휘가 새지 않는다 — 방송사 운영자가 읽는 줄이다 
  ✔ 확정 실패를 '끝나지 않아' 라고 부르지 않는다 — 렌더 대기와 구분이 안 된다 
  ✔ 확정 실패면 낙관 문구 대신 실패로 넘긴다 
  ✔ 확정 횟수는 도달 가능해야 한다 — 못 지킬 약속을 문구에 쓰지 않는다 
  ✔ 활동 시간 밖이 모든 사유를 이긴다 — 평가 자체를 안 했다 
  ✔ 활동 시간창 밖에도 하루 한 줄은 남긴다 — 하루 11~14시간이 통째로 비면 안 된다 
  ✔ 활동 시간창 밖이면 그 사실과 시간을 말한다 
  ✔ 회사 공정성 정렬이 정지 검사보다 뒤에 오지 않는다 
  ✔ 회사 생성 라우트를 찾는다 
  ✔ 회사·초대·크레딧이 한 트랜잭션 안에 있다 
  ✔ 회사를 정지하면 그 회사 세션을 끊는다 
  ✔ 회사별 마지막 처리 시각이 1순위다 
  ✔ 회차 드롭다운이 있다 
  ✔ 회차 번호를 추천에서 물려받는다 — 편집자가 다시 고르지 않게 
  ✔ 회차 상한 문구도 없어진 '점수 기준' 을 안내하지 않는다 
  ✔ 회차 없음이 분석 중을 이긴다 
  ✔ 회차 없음이 조용한 continue 로 돌아가지 않는다 
  ✔ 회차 필터는 episodeId 로 거른다 — 회차 번호는 프로그램마다 겹친다 
  ✔ 회차가 없어도 clip.programId 로 프로그램을 찾는다 
  ✔ 회차가 없으면 회차 없음 
  ✔ 회차는 `분석 대기` 로만 들어간다 — 진행 중이 아니다 
  ✔ 회차당 상한 — 이미 채택한 수만큼 덜 뽑는다 
  ✔ 횟수가 모자라도 30분째 진행이 없으면 확정한다 (리프레임 벨트와 같은 값) 
  ✔ 횡단 스코프로는 **쓸 수 없다** — 어느 테넌트 것인지 모르기 때문 
  ✔ 횡단 스코프에서는 필터가 붙지 않는다 
  ✔ 후기 있는 상품을 우선한다 
  ✔ 훅 재선정은 빈 응답을 오답으로 삼키지 않는다 
  ✔ 훅 프리롤에 TTS 를 얹고, 실패해도 훅은 나간다 
