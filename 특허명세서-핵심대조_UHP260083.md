# 특허 UHP260083 — 청구항 vs 실구현 정밀 대조 + 광역화(미래 포함) 전략

> core/ 코드를 파일:라인까지 열어 청구항과 대조한 결과. 앞선 `특허명세서-검토_*.md`(오타·정합)와 별개로, **"이 특허가 제품과 미래를 실제로 커버하는가"**에 초점.
> ※ 청구범위 확정은 변리사 필수. 아래는 코드 근거 기반 발명자 관점.

---

## 0. 한 줄 결론

**지금 명세서는 "아무도 안 쓰는 방식"을 청구하고, "진짜 하는 방식"은 청구 밖에 둔다.**
청구항 1의 핵심 3요소(표현사전 제1점수 → M프레임 VLM 제2점수 → 0.7·VLM+0.3·텍스트 융합)는 **실제 프로덕션 파이프라인에 존재하지 않는다.** 실제 제품은 **서사-우선 LLM이 완결 편집단위(beat)를 만들고 텍스트 3축으로 채점**하는 전혀 다른 메커니즘이며, 이 진짜 발명은 청구항에 없다. → 방어(자사 커버)도, 공격(경쟁사 침해)도, 미래 확장도 모두 약하다.

---

## 1. 청구항 1 요소별 대조 (코드 근거)

| # | 청구 요소 | 판정 | 실제 코드 |
|---|-----------|------|-----------|
| 1 | STT 1회 → **단어 단위 타임스탬프** 전사 | 🔷 부분 | STT 1회·후속 공유 ✅(`analyze.py:233`) / **단어 타임스탬프는 프로덕션(Gemini)에서 미생성** — `asr.py:250` `"words": []` 주석 "Gemini gives utterance-level, not word-level". 단어단위는 whisper/hybrid 모드만. 기본은 **발화(문장) 단위**. |
| 2 | **표현 사전(범주별 가중치)** 대조 → 후보 + 제1점수 | 🔷 **전혀 다른 메커니즘** | **문자열 사전 대조 없음.** 후보는 Gemini가 서사/전사를 읽고 추론 생성(`recommend.py:639-684, 2919-2985`). 점수는 LLM 3축(hook/payoff/completeness) 또는 beats에서 하드코딩(7/7/8). `prefilter.py` 휴리스틱은 **죽은 코드(미import)**. |
| 3 | 발화경계(무음/화자전환)+**종결어미**로 경계 정제 | 🔷 부분 | 발화경계 스냅·문장완결 확장 존재(`recommend.py:910-987`)하나 (a) **종결어미 패턴 아님, 구두점(.!?…)으로 판정** — 주석에 종결어미 검사는 false positive라 의도 배제. (b) 화자전환 미사용. (c) **프로덕션 beats 경로에선 이 정제가 호출조차 안 됨.** |
| 4 | 제1점수 상위 N → M프레임 → **VLM 제2점수** | ❌ 미구현 | 추천 엔진이 **프레임을 Gemini에 1장도 안 보냄**(from_bytes/from_uri 0건). `vision.py`·`prefilter.py`는 죽은 코드. Vision은 scene_type 분류·PPL·얼굴이름에만 쓰이고 "후보별 제2점수"는 없음. |
| 5 | 최종 = 제2×0.7 + 제1×0.3 (VLM>텍스트) | ❌ 미구현 | 실제 최종 = `3축(0.40/0.35/0.25) × program_fit × channel_fit`(`recommend.py:582`, `channels.py:172`). VLM 항 없음. appeal(1–5)은 3축 역산 UI값. |
| 6 | 9:16 리프레이밍(블러배경) + 전사 자막 번인 | ✅ 일치 | `ffmpeg.ts:159-234` 블러커버(`boxblur=20:1`)+fit overlay+ASS libass 번인. 자막=전사 슬라이스. **유일하게 명확 일치.** |

**종속항:** 7/17(재렌더 해시 무효화)만 ✅ 정확 일치(`index.ts:2683` sha256 캐시). 5/15(종결어미)❌, 8/18(PPL 전사교차)🔷—시각검출만 있고 전사 교차 없음, 9/19(다중 제목·다중화면비 썸네일)🔷—프로덕션 미호출/썸네일 단일, 10/20(채널 폐루프)🔷—채널 프리셋 랭킹은 ✅지만 성과→선정 폐루프는 no-op(`apply_learned_rerank` RESERVED), 도12(프록시)❌, 도13(템플릿복제)🔷 불확실.

---

## 2. 진짜 발명인데 청구 안 된 것 (➕ 미청구 — 여기가 핵심 자산)

실제 제품의 신규성은 명세서가 기술한 "사전+VLM 2단계"가 아니라 아래에 있는데, **전부 청구항 밖**이다:

1. **서사-우선(narrative-first) 후보 생성** — 시나리오(주제)→변형/beat. LLM이 전사·서사를 읽고 완결 편집단위를 추론(`recommend.py:2919-2985`, 기본 `RECOMMEND_MODE=narrative_first`).
2. **beat = AI 정돈 편집 최소 완결 단위** — narrative+STT+shots+shot_types 종합해 "편집자가 그대로 쓸 완결 단위" 생성(`beats.py`). **제품의 진짜 차별점.**
3. **단계별 체크포인트 재개 + 파라미터 지문 무효화** — stt→refined→faces→ppl→scenes→cast→narrative→beats→shorts 단계별 저장, 영상/파라미터 fingerprint로 선택 무효화·완료단계부터 재개(`analyze.py:50,75-154`).
4. **캐스트 레지스트리 + 얼굴 무감독 클러스터링 + 화자↔얼굴 크로스매핑** — 사전등록 명단×화면 이름표 정규화, HDBSCAN 얼굴 클러스터, 화자분리(diarization)×얼굴 시각겹침으로 실명 확산(`faces.py`, `cast.py`, `speaker_face_map.py`).
5. **VAD 시각 스냅 + 전사 아웃티지 방어** — Silero VAD로 시각 스냅(BGM 밀림 보정), 구멍>10%면 잡 실패·재시도(`asr.py:286-375+`).
6. **채널×프로그램 프로파일 승수 + (예약된) 학습 재랭킹 훅** — `channels.py` 프리셋 + `apply_learned_rerank`(RESERVED 훅).

---

## 3. "미래까지 먹는" 강한 특허를 위한 방향 (← 요청하신 핵심)

지금 청구항은 **하나의 구현(사전+VLM)만 좁게** 기술해서, 역설적으로 자사 제품조차 못 덮는다. 강한 특허 = **상위개념(genus)으로 넓히고 + 진짜 신규 요소를 필수항으로**. 두 축을 동시에.

### 3-1. 독립항을 "메커니즘 중립"으로 상위화
요소 2·4·5를 특정 방식으로 못박지 말고 **기능적 상위개념**으로:

- **후보 생성/제1점수** → "표현 사전 대조" 대신 **"전사의 언어적 신호에 기초하여 후보 구간과 제1 지표를 산출"** — 하위에 (a) 표현사전 대조(현 명세서), (b) **언어모델 서사 추론(현 제품)**, (c) 휴리스틱을 **병렬 실시예**로. → 경쟁사가 LLM을 쓰든 사전을 쓰든 모두 포섭.
- **제2 평가** → "VLM 제2점수" 대신 **"후보 구간에 대한 시각적/추가적 평가를 선택적으로 수행"** — 시각 평가를 **선택적 종속항**으로 내려서, 시각평가를 안 하는 현 제품도 독립항에 걸리게.
- **융합** → "0.7/0.3 고정" 대신 **"복수 지표를 기 설정된 방식으로 결합"** — 구체 가중치는 종속항.

핵심: **독립항에서 "표현사전/VLM/0.7·0.3" 같은 구체 한정을 빼면**, 지금 제품(LLM 서사)·명세서 방식(사전+VLM)·미래 변형(학습 재랭킹)이 **한 우산 아래** 들어온다.

### 3-2. 진짜 신규 요소를 새 독립/종속 청구로 (미래 확장 선점)
- **[A] 완결 편집단위(beat) 청구** — "발화·샷·서사를 종합해 그대로 배포가능한 완결 편집단위를 생성"을 독립 특징으로. OpusClip류(구간 accept/reject)와 구조적으로 다른 지점 → 미래 편집기·자동화의 뿌리.
- **[B] 전사 단일원천 + 단계별 체크포인트 재개** — "1회 전사를 후보/경계/자막/PPL/제목/캐스트가 공유 + 단계별 체크포인트로 부분 재개 + 파라미터 지문 무효화". 파이프라인 아키텍처 자체를 청구 → 재구현·우회 난이도↑.
- **[C] 출연자 신원 융합** — "화면 내 이름표 인식 × 전사 화자 × 얼굴 클러스터를 교차하여 출연자별 등장 타임라인 생성". PPL(브랜드)과 대칭인 **인물** 축 → 미래 캐스트 기반 기능(출연자별 클립·검색) 선점.
- **[D] 채널 성과 폐루프 학습** — 현재 no-op(RESERVED)이지만 **미래 핵심**. "배포 성과(리텐션 커브 등)를 후보 선정·재랭킹에 환류"를 지금 청구해두면, 실제 구현이 뒤따라와도 커버. (미구현이어도 명세서 뒷받침이 충분하면 청구 가능 — 변리사 판단.)

### 3-3. 방어적 상위/하위 이중화
- **넓은 독립항**(메커니즘 중립) + **여러 좁은 종속항**(사전·VLM·0.7/0.3·종결어미·프록시·템플릿복제 등 현 명세서 내용 보존). 넓은 항이 무효화돼도 좁은 항이 살아남는 **다층 방어**.

---

## 4. 리스크 & 우선순위

- 🔴 **가장 시급:** 독립항 1이 자사 제품을 안 덮는다(요소 2·4·5 불일치). 이대로 등록되면 "제품 보호 못 하는 특허". → 3-1 상위화가 최우선.
- 🔴 **기재불비/최적실시예:** 명세서가 실제 코드가 안 하는 VLM 2단계를 상세 기술 → 무효심판 빌미 가능. 실시예에 **현 제품 방식(서사-우선 LLM)**을 추가 기술해 뒷받침 보강.
- 🟡 **미청구 자산 유출:** beat·체크포인트·캐스트 융합이 공개(제품 출시·논문·데모)되면 신규성 상실 위험 → **출원 전** 3-2 청구 편입.
- 🟢 앞 파일의 오타(303)·부호(91/92/93) 정리는 병행.

**요약:** 좁게 "한 방식"을 적은 지금 명세서를 → **① 독립항을 메커니즘 중립으로 넓히고 ② beat·전사단일원천·캐스트융합·성과폐루프를 새 청구로 선점**하면, 현 제품 + 경쟁사 변형 + 미래 확장을 한 번에 덮는 강한 특허가 된다. 구체 방식(사전/VLM/가중치)은 종속항으로 남겨 다층 방어.
