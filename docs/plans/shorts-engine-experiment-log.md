# 쇼츠 엔진 — 실험 로그 (방법론 진화 기록)

> **목적**: 우리가 돌린 실험을 **시간순으로** 기록한다. 방법론이 계속 바뀌므로(측정 대상·지표·환경),
> "무슨 실험 / 어떤 방법 / 이전 대비 뭐가 바뀌었나 / 결과 / 결론"을 남겨 **보고서에 그대로** 쓸 수 있게 한다.
> 새 실험은 맨 아래에 append. 결과는 확정되면 그 실험 항목에 채운다.
> 요약 보고서: [shorts-engine-성과보고-2026-07-21.md](shorts-engine-성과보고-2026-07-21.md) · 상세: [shorts-engine-experiments-2026-07-21.md](shorts-engine-experiments-2026-07-21.md)

---

## 한눈에 — 방법론이 어떻게 진화했나

| 축 | 처음 | 지금 | 왜 바꿨나 |
|---|---|---|---|
| **정답지** | 발행 숏폼 (사람 편집자 선택) | 동일 | 가장 신뢰 높은 정답 |
| **매칭 지표** | Hit@N (IoU≥0.5, **같은 초**) | + **Topic-Hit@N** (내용, Gemini 판정) | IoU는 8초만 어긋나도 오답 → 엔진 과소평가 (사용자 지적) |
| **표본** | 홀드아웃 1편 (정답 3) | 홀드아웃 3편 (정답 26) | 단일은 우연·변동에 취약 |
| **반복** | 1회 | 조건마다 2~5회 평균 | 엔진이 temp0인데 실행마다 변동 |
| **실행 환경** | 클라우드 워커 VM | **로컬(가정용 IP)** | 봇차단·ssh·429 회피 |
| **학습 반영** | 규칙(추상) 프롬프트 | + **④ 원본 예시** few-shot | 요약하면 신호가 뭉개짐 |

---

## 실험 연대기

### Exp 0 — 측정 체계 수립 (2026-07-21)
- **방법**: 채널이 실제 발행한 숏폼을 정답으로. 오디오 상호상관(`align.py`)으로 숏폼→롱폼 구간 역추적,
  `evaluate.py`가 Hit@N(IoU≥0.5)·경계오차로 채점. 성과는 절대 조회수 금지 — ±90일 동시기 채널 중앙값 대비 배수(tier).
- **결과**: "롱폼에서 쇼츠를 잘 뽑는가"가 측정 가능해짐. 매칭 정확도 양성 0.886~0.998 / 음성 0.403~0.601.

### Exp 1 — 엔진 길이 수정 전/후 (단일 홀드아웃, 2026-07-21)
- **방법**: 하하PD 경주 PC방(13분) 홀드아웃, 정답 3, IoU Hit@5. 홀드아웃은 학습 제외.
- **결과**: **Hit@5 0.00 → 0.67**. 원인은 "못 찾음"이 아니라 4~8초로 너무 짧게 잘라 IoU 붕괴 → 프롬프트를
  "셋업 포함 30~60초"로 재작성 + 30초 하한. 추천 길이 8초→45초.
- **발견 (중요)**: recommend가 temperature 0인데도 **실행마다 결과가 다름** — 같은 조건 5회가 Hit@5 0.00~0.67로 요동.
  → 단발 A/B로는 "학습 덕분"인지 "우연"인지 구분 불가.

### Exp 2 — 다중 홀드아웃 반복 A/B, IoU (2026-07-21)
- **방법 변경**: (1) 홀드아웃 3편(원정대5=18·원정대4=5·경주=3 = **26정답**)으로 확대,
  (2) 조건마다 **5회 반복 평균±σ**(변동 상쇄, `eval_repeat.py`), (3) 누수 없는 프로파일(홀드아웃 제외 22쌍 학습, conf 0.75).
- **지표**: Hit@5/10/20 (IoU≥0.5) micro-average. 판정 = Hit@10.
- **결과**:

  | 지표 | 학습 끔 | 학습 켬 | 상대개선 | 판정(차이 vs σ) |
  |---|---|---|---|---|
  | Hit@5 | 0.100 | 0.138 | +38% | 효과 있음 |
  | Hit@10 | 0.131 | 0.200 | +53% | **미확정** (σ 안) |
  | Hit@20 | 0.154 | 0.215 | +40% | 효과 있음 |

- **결론**: 학습은 픽을 개선하나(확정 +38~40%; Hit@10 +53%는 미확정) **소폭**. 절대 Hit@10=0.20 → **무인 발행 미달**.
  단일 홀드아웃의 낙관적 0.67은 표본 확대로 ~0.20이 실측임이 드러남.

### Exp 3 — 주제(내용) 기반 채점 + ④ few-shot + 로컬 (2026-07-22) 〔진행 중〕
- **방법 변경 (사용자 지적)**: IoU(같은 초)는 "엔진이 같은 순간을 숏폼감으로 잡았나"를 8초만 어긋나도 오답 처리 →
  **Topic-Hit@N** 도입(`eval_topic.py`): 발행 숏폼(제목+자막) vs 엔진 픽(제목+구간자막)을 **Gemini가 "같은 순간인가"로 판정**.
- **추가 변경**: (a) **④ few-shot** — 학습 규칙을 요약하지 말고 실제 고/저성과 구간 예시를 프롬프트에 원본으로 주입,
  (b) **로컬 실행** — 가정용 IP로 봇차단 회피, (c) **Gemini 호출 타임아웃 120s** — 한 호출이 행에 걸려 측정이 21분 스톨한 것 차단.
- **설계**: 3홀드아웃(26정답), **3조건 × 2회 반복 한 세션**(같은 변동 조건 head-to-head):
  ① off ② base(학습 규칙) ③ base+예시(④). 각 조건 recommend → topic_hits → Topic-Hit@5/10/20.
- **답하는 질문**: (i) off Topic vs 기존 IoU 0.20 — 기계적 메트릭이 얼마나 과소평가했나, (ii) off vs base — 학습 효과(공정),
  (iii) base vs base+예시 — ④가 추상 규칙보다 나은가.
- **결과 (2026-07-22 완료, 정직)**:

  | 조건 | Topic-Hit@5 | @10 | @20 | runs@10 |
  |---|---|---|---|---|
  | **off** (학습 없음) | 0.404±0.10 | **0.519**±0.06 | **0.692**±0.04 | [0.46, 0.58] |
  | base (학습 규칙) | 0.173±0.10 | 0.346±0.08 | 0.596±0.02 | [0.27, 0.42] |
  | base+예시 (④) | 0.231±0.04 | 0.346±0.00 | 0.558±0.06 | [0.35, 0.35] |

- **두 가지 큰 발견**:
  1. **IoU가 엔진을 크게 과소평가했었다** — off Topic@10=0.52 vs IoU에선 0.20 (**2.6배**). "같은 초" 요구가 엔진 실력을 깎았다. Topic으로 보면 엔진 top-10이 사람 편집자 선택의 **절반 이상**을 잡는다 — 훨씬 강한 성능.
  2. **학습이 오히려 성능을 떨어뜨린다 (Exp 2와 반대!)** — off > base > base+예시. Exp 2(IoU)는 학습 +38~53% 개선이라 했지만 Topic으로 재보니 **학습이 -33% 악화** (@10: 0.52→0.35). ④ few-shot도 도움 안 됨(base와 동일).
- **해석 (가설)**:
  - (a) 학습 프로파일(hookWeights 편중)이 **한 채널 편향**을 만들어 다양한 순간을 놓침. hookWeights가 잘 터진 훅에 편중해 사람이 고른 폭넓은 순간을 못 잡음.
  - (b) IoU와 Topic이 **다른 걸 잰다**: IoU는 "정확한 위치·길이" 보상, Topic은 "같은 순간이면 됨". 학습은 위치·길이엔 도움(Exp 2) BUT 주제 다양성엔 해.
- **결론**: **결과가 뒤집혔다.** Topic(제품 목적에 가까운 지표) 기준으로 학습 프로파일은 **끄는 게 나음**. Exp 2의 "학습 +38~53%"는 IoU 특성 때문일 가능성. ④ few-shot도 이 형태로는 효과 없음.
- **다음 (숫자가 정하는 다음)**:
  - Topic vs IoU 격차의 원인 규명 — 왜 학습이 IoU엔 도움 되고 Topic엔 해가 되나
  - 학습 형태 재고 — hookWeights를 다양성 유지 방식으로 재설계, 또는 학습을 후보생성 대신 **랭킹 소량 부스팅**으로만
  - 다른 채널로 재현성 확인 — 이 반전이 하하PD 특수인지 일반인지
- **정직한 한계**: (i) Topic 판정은 LLM 심사(약간 주관, IoU 같은 하드 객관지표 아님) → 두 지표 병기 필요, (ii) 홀드아웃 3편·정답 26·반복 2회 — 표본 여전히 작음. 반복 5회로 늘리면 σ 반으로 줄어 확정성 강화 가능(다음 작업).

---

## 발견 — 고컷 토크 콘텐츠는 장면 유닛을 코스화해야 (2026-07-22, 사용자 지적)

**관찰**: 실험이 특정 홀드아웃(경주 PC방, 13분 토크)에서 유독 느렸다. 원인 실측:

| 홀드아웃 | 장면수 | 영상 | 평균 장면 | 방식 |
|---|---|---|---|---|
| **경주** | **432** | 13분 | **1.8초** | 시각 마이크로컷 |
| 원정대5 | 197 | 61분 | 13.8초 | 자막 기반 |

**13분 토크 영상이 432개 마이크로컷** — 사람 A→B→C 카메라 전환마다 1.8초씩 끊고 각 프레임을 분석했다.
**이런 1.8초 컷의 프레임 분석은 무의미**(화면은 그냥 "다음 사람 말하는 중")하고, recommend가 432 유닛을
처리하느라 느리다. → **정답: 시각 컷이 아니라 대사(자막)로 세그먼트.** 경주를 자막 코스유닛으로 재구성하니
**432 → 38 (11배↓)**, 실험이 21분 스톨 → 3분 완료.

**설계 함의** (로드맵 §5-6 '풀 파이프라인 필요성'과 직결):
- 고컷/토크 콘텐츠 = 시각 장면감지·프레임 비전이 **낭비**. `fast` 모드(자막 세그먼트)가 정답.
- recommend 비용도 유닛 수에 비례 → 대사 기반 **코스 유닛(30~40초)**이 속도·의미 둘 다 이득.
- **다음**: 컷 밀도(장면수/분)가 높으면 자동으로 fast/코스 경로로 라우팅.

### Exp 4 — 컨셉 전환: 채널맞춤 → **일반화(잘 터진 쇼츠 요소)** + 크로스채널 검증 (2026-07-22) 〔진행 중〕
- **이전 결함 인지 (사용자 지적)**: Exp 2·3의 학습은 (a) **학습·검증 같은 채널**(하하)이라 오염, (b) 표본 26정답 × 2회로 σ가 커 결정 불가, (c) 감사 재실행에서 3조건 모두 같은 숫자(해상도 부족).
  → "채널맞춤"을 포기하고 "**일반적으로 잘 터지는 쇼츠 요소**"를 배우는 방향으로 재정의.
- **데이터 방식 변경**: 롱폼↔숏폼 매칭(71쌍, 오디오정렬 병목) → **하하 발행 숏폼 228편만** 학습 소스로.
  숏폼만 있으면 되므로 규모 확장 자유(다른 100만 채널도 학습 데이터 편입 가능).
- **학습 대상**: 숏폼 자체의 요소(자막·길이·훅) × 성과 tier(채널 log-조회수 3분위: high 76·mid 76·low 76 — 완벽 균형).
- **파이프라인**:
  - Phase 1a: 하하 숏폼 오디오 다운로드 (199/228 완료, 로컬 봇차단 0, 148MB).
  - Phase 1b: 각 숏폼 STT (Gemini 병렬, 진행 중).
  - Phase 1c: (오디오만 있으므로) 프레임은 후단계로 미룸 — 자막·길이·훅 학습부터.
  - Phase 2: high vs low 요소 대조 → 일반화 프로파일.
  - Phase 3: **다른 채널(ENA 등) 롱폼**에 하하학습 적용 → 그 채널 발행 숏폼 Topic-Hit 재현.
- **평가 지표 (사용자와 확정)**: 다른 채널의 발행 숏폼 재현율 (Topic-Hit@N). off vs 하하학습 on. Win-rate·간접이지만 즉시 가능.
- **정직한 함정 (미리 인지)**: (i) 다른 채널이 하하와 스타일 겹치면(예능·토크) 학습 덕분처럼 보일 수 있음 → 다양 채널 필요, (ii) 성과 tier=조회수라 자름 품질과 포장이 여전히 섞임, (iii) 하하가 100만 채널이라 학습 원칙이 소규모 채널에 통할지 별개.
- **Phase 1·2 결과 (2026-07-22 완료)**:
  - Phase 1a: 하하 숏폼 오디오 다운로드 **199/228** (fail 29 = 삭제·비공개), 148MB. 로컬 봇차단 0.
  - Phase 1b: STT **199/199 fail 0** (63분, 순차, tmp 충돌 회피).
  - Phase 2 학습 (표본 high 69·mid 69·low 69, dur≤180s 재필터):

    | 요소 | high | low | lift | 신호 강도 |
    |---|---|---|---|---|
    | **제목 어그로(n_hooks)** | 0.13 | 0.07 | **1.86** | 최강 |
    | 이모지 | 0.13 | 0.09 | 1.44 | 강 |
    | 인용부호 | 0.04 | 0.03 | 1.33 | 약 |
    | 제목 길이 | 28.6자 | 24.9자 | 1.15 | 약 |
    | 숏폼 길이 | 44초 | 54초 | 0.82 | 강(짧을수록) |
    | 자막 총량 | 357자 | 433자 | 0.82 | 강(적을수록) |

  - **일반화 원칙**: "짧고(45초), 대사 적고, 제목 자극적"인 하하 숏폼이 잘 터진다.
- **Phase 3 결과 (2026-07-22 완료, 크로스채널 A/B, 2회 실행)** ⭐:
  - ENA 정답지: 30 숏폼 × 5 롱폼 오디오매칭 → **7쌍 확보** (4 롱폼 커버, score 0.80~0.99)
  - Topic-Hit@10 (Gemini 내용 매칭, 각 조건 **2회 독립 실행**):

    | 조건 | 1회차 | 2회차 | 평균 | 범위 |
    |---|---|---|---|---|
    | off (프로파일 없음) | 0.571 | 0.857 | **0.714** | 0.571~0.857 (**요동**) |
    | **하하학습 on** | 0.857 | 0.857 | **0.857** | 0.857 (**안정**) |
    | Δ (평균) | | | **+0.143 (+20%)** | |

  - 롱폼별 (1회차): QNtoQ4zI8mc에서 off @10=0.0 → 하하학습 @10=1.0 (편집자 선택 재현). 2회차에선 off도 1.0 나옴.
- **결론 (Exp 4 최종, 두 발견 분리)**:
  - **① 평균 개선 확인 (약함)** ✅: 학습 켬 평균 @10=0.857 vs off 0.714 → +20%. 표본 7쌍·반복 2회라 통계적 확정은 아직.
  - **② 안정성 격차 (더 중요할 수 있음)** ⭐: **학습 켬은 두 번 다 0.857 (동일)**. off는 0.571~0.857 요동. 하하학습이 픽을 안정화하는 효과 — 이게 실전(예측가능성)에 더 큰 가치.
  - **크로스채널 실증**: Exp 3(같은 채널 학습·검증)에서 뒤집혔던 결론이, 채널초월 일반화 원칙 + ENA 검증으로 **"방향 확립"** 단계까지 진전. "학습이 다른 채널을 해치지는 않는다" + "일부 경우엔 확실히 낫다"까지는 실측.
- **정직한 한계**: (i) ENA 정답지 7쌍은 여전히 작음 — 매칭 확대(30 숏폼 × 더 많은 롱폼) 필요, (ii) 각 조건 1회만 실행 — 반복 σ 미측정, (iii) ENA도 예능이라 하하와 스타일 겹침 — 다른 도메인(드나드나·려진 등) 확장 필요, (iv) LLM 판정관 detail 저장했으니 감사 가능.
- **다음**: (a) ENA 매칭 확대(정답지 30쌍+), (b) 반복 3회+로 통계 확정, (c) 다른 도메인 채널 확장, (d) 시각 요소(오프닝 프레임·오버레이)도 학습에 통합.

---

## 실패·폐기·오판 실험 (정직성 기록)

> 이 섹션이 왜 있나: 실험은 실패하고 결론이 뒤집힌다. **뭐가 안 됐고 왜 안 됐는지**를 남겨야 같은 실수를 반복 안 하고, 보고서에도 "우리가 뭘 걸러냈나"를 쓸 수 있다.

### F1 — 종결어미 스냅 (P1 계획 → 폐기)
- **시도**: 한국어 문장 끝(습니다/네요/까요 등)에 클립 경계를 스냅. 계획서 P1 우선순위였음.
- **실측 결과**: 홀드아웃에서 Hit·경계오차 **모두 변화 없음** (11.4초 유지).
- **원인**: 남은 경계오차는 "문장 중간 컷"이 아니라 "엔진과 편집자의 클립 범위 차이"였음. ±3초 미세조정으로 11초 차이를 못 메움. 오히려 멀쩡한 경계를 애매하게 옮기기도.
- **결정**: 프로덕션 미배포·폐기. 대신 **발화(문장) 타임스탬프 스냅**(Exp 2 병행)이 실효 있어 그것만 배포.
- **교훈**: "좋아 보이는 아이디어"라도 숫자 안 나오면 버린다. 계획서 우선순위 ≠ 실효성.

### F2 — 단일 홀드아웃 Hit@5 = 0.67 (조기 환희 → 정정)
- **주장 (Exp 1)**: 엔진 길이 수정으로 Hit@5가 0.00 → **0.67**. "무인 발행 임박!"으로 오해할 뻔.
- **문제**: 홀드아웃 1편(경주 PC방)·정답 3건. 해상도가 0/0.33/0.67/1.0 4단계뿐.
- **재검증**: 같은 조건 5회 반복 → Hit@5 0.00·0.33·0.33·0.67·0.67 (평균 0.40, σ 0.30). **엔진 자체 변동에 완전히 묻힘.**
- **정정 (Exp 2)**: 홀드아웃 3편·정답 26건·5회 반복으로 확대 → 진짜 Hit@10 = **0.20**.
- **교훈**: 낙관적 초기 숫자는 반드시 표본 확대 + 반복 평균으로 재검증. **temp 0이라도 LLM 결과는 요동친다.**

### F3 — Exp 3 "학습이 성능을 -33% 악화" (뒤집힌 결론 → 신뢰불가 판정)
- **주장**: Topic 기반 A/B에서 off > base > base+예시 (@10: 0.52 → 0.35). "학습 프로파일 오히려 해로움."
- **감사 (사용자 지적 후)**: 경주 1편으로 재실행하니 3조건 모두 Topic-Hit@10 = **0.667 (동률)**. 정답 3건 = 해상도 4단계라 픽이 달라도 숫자 같음.
- **근본 결함 인지**:
  - (a) **학습·검증 같은 채널** (하하 22쌍 학습, 하하 3편 검증) → 채널 오염, 완전한 hold-out 아님.
  - (b) **표본 26정답 × 2회** → σ 0.06~0.10이 지표 차이(0.17)를 삼킴.
  - (c) LLM 판정관 detail 미저장 → 왜 매칭/미매칭인지 감사 불가.
- **결정**: "학습 악화" 결론 **철회 · 신뢰불가**. 방법론 결함으로 재분류.
- **교훈**: 뒤집힌 결론일수록 **먼저 실험 자체를 의심**. 채점 파이프라인 print를 stderr로만 보내면 나중에 감사 못 한다 → 결과 파일에 detail 병기.

### F4 — 클라우드 워커 A/B (21분 스톨)
- **시도**: 주제 기반 A/B를 클라우드 워커에서 실행.
- **문제**: Gemini 호출 하나가 응답 없이 무한 대기 → `call_with_retry`에 타임아웃 없어 파이프라인 전체 스톨. 21분간 진행 0.
- **원인**: `genai` 클라이언트가 timeout 파라미터를 안 받아 SDK 기본 무한 대기.
- **수정**: `_call_with_timeout` 데몬 스레드 + join(120s) — 초과 시 일시오류로 보고 재시도.
- **결정**: 이후 모든 실험은 **로컬 실행 + 결과 파일 저장 + 스톨 감지 워치독** 원칙.
- **교훈**: 외부 API 호출은 반드시 타임아웃 감쌈. SDK 기본을 믿지 말 것.

### F5 — 클라우드 다운로드 봇차단 (매칭 잡 실패 다발)
- **시도**: 클라우드 워커에서 유튜브 대량 다운로드(매칭용). 데이터센터 IP.
- **결과**: 30편쯤 받으니 YouTube "Sign in to confirm you're not a bot." 봇차단. 매칭 잡 다수 실패.
- **원인**: 데이터센터 IP는 봇 취급. 쿠키(2.5KB짜리 약한 것)도 못 뚫음.
- **수정 시도들**:
  - 쿠키 강화 → 여전히 봇차단 재발 (근본은 IP)
  - 다운로드 스로틀 → 속도만 느려짐
  - **로컬 전환 (가정용 IP)** → 봇차단 0. 하하 숏폼 199편 · ENA 30편 전부 성공.
- **결정**: **연구용 다운로드는 로컬** (실서비스는 업로드 기반이라 무관 — [infra.md](../ops/infra.md) 참고).
- **교훈**: 다운로드 병목의 근본은 IP·인증이지 코드가 아니다.

### F6 — 병렬 STT tmp 파일 충돌 (초기 STT 결과 오염)
- **시도**: STT 6개 병렬 실행 (하하 숏폼 199편).
- **결과**: 12편 완료 시점에 로그가 "STT window @134,000s failed" 만 반복 (137,000초?). 실제 오디오는 24-30초짜리.
- **원인**: `core.asr._to_wav`가 tmp 파일명에 `os.getpid()`만 씀 → 병렬 스레드가 **같은 파일을 서로 덮어써서** 이상한 duration 감지 (여러 파일이 이어붙어진 값).
- **수정**: **순차 실행 강제** (workers=1). 각 편 짧으니 순차라도 15-20초/편으로 감당 가능. 이전 12편 삭제 후 재실행.
- **교훈**: 병렬 처리 시 tmp 파일명은 반드시 스레드 고유(pid + 스레드 id + 랜덤). 병렬 이득 있는지 먼저 확인.

### F7 — YouTube API `isShort` 플래그 신뢰 문제 (학습 데이터 오염 직전)
- **시도**: 하하 숏폼 228편을 `isShort or dur<=180` 조건으로 필터해 학습 소스로.
- **문제 감지**: Phase 2 학습 실행하니 **low tier 평균 길이 527초** (8.7분?!). "숏폼"에 롱폼 다수 섞임.
- **원인**: YouTube Data API의 `isShort` 플래그가 부정확 (라이브 스트림·재편집·API 판단 오류).
- **수정**: **실측 dur ≤ 180초**만 재필터. 228 → 207편 (21편 이상치 제거). tier 균형도 76/76/76 → 69/69/69로 정정.
- **교훈**: 외부 API 플래그를 그대로 믿지 말 것. 학습 데이터는 실측치로 재검증 필수. **초기 통계에서 이상한 숫자가 나오면 그게 신호다** (n_hooks lift 1.86보다 dur_sec 이상치가 먼저 알려줌).

---

### Exp 5 — 시각 요소 학습 스키마 확장 (2026-07-22 완료) ⭐
- **동기**: Exp 4까지 학습 대상 = 자막·길이·훅 (제목 텍스트 위주). PD님 지적 재확인 —
  "포장(제목 자극성 + 첫 장면 훅)이 조회수를 가른다"가 통계보다 대쇄감. 시각 신호 실증 필요.
- **방법**:
  - Phase 5a: 하하 숏폼 194편 **첫 3초** 다운로드 (yt-dlp `--download-sections *0-3`, 총 45MB · 로컬 봇차단 0)
  - Phase 5b: t=0.5s 프레임 1장 추출(ffmpeg) + Gemini Vision 1회 호출 → 8특성 스키마 (overlay_text·overlay_position·has_face_close·n_faces·visual_hook_type·visual_intensity·dominant_color·scene_description) · 순차 실행 43분 · fail 0
  - Phase 5c: high vs low tier 대조 (표본 각 69편, dur≤180초 재필터)
- **결과 (실측 · 194편 완결)**:

  **수치 대조 (lift H/L)**:
  | 요소 | high | mid | low | lift | 판정 |
  |---|---|---|---|---|---|
  | **n_faces** (얼굴 수) | **2.06** | 1.41 | 1.30 | **1.58** | 🥇 강 |
  | visual_intensity | 2.86 | 2.84 | 2.29 | 1.25 | 강 |

  **훅 타입 diff (high-low)**:
  | 훅 | high | low | diff | 판정 |
  |---|---|---|---|---|
  | **text_cue** (화면 텍스트 큐) | 67% | 54% | **+13.0%p** | 🥇 최강 |
  | **reaction** (리액션 얼굴) | 16% | 6% | **+10.1%p** | 🥈 강 |
  | situation (상황설정) | 9% | 16% | **-7.2%p** | 회피 신호 |

  **색상 diff**:
  | 색 | high | low | diff | 판정 |
  |---|---|---|---|---|
  | **흰색** | 12% | 3% | **+8.7%p** (lift 4.0) | 🎉 |
  | 밝은색(파랑·주황) | 6% | 0% | **+5.8%p** | 하한 zero |
  | 검정(지배) | 61% | 62% | 무신호 | - |

- **결론 (하하 오프닝 훅 공식)**: **"첫 3초에 텍스트 큐 있고, 얼굴 2명+, 밝은/흰색 지배, 리액션 유형"** 이면 잘 터진다. **"잔잔한 상황 설정"** 이면 안 터진다.
- **Exp 4 결합**: Exp 4의 "짧고 · 대사 적고 · 제목 자극"(n_hooks lift 1.86)에 시각 신호 추가로 학습 스키마 **2배 확장**.
- **정직한 한계**: (i) 표본 각 69편 · 반복 없음, (ii) OCR 신뢰도(Gemini 판정) — 큐 위치·강도 근사, (iii) 이 신호 학습이 recommend에 실제 반영 시 개선폭은 별도 크로스채널 A/B 필요(Phase 5d).
- **산출물**: `증빙_데이터셋/실험자료/exp5_haha_visual_profile.json` · `exp5_haha_visual_features.csv` (각 194행) · `exp5_frames/` (프레임 194장, 로컬)
- **다음**: 시각 프로파일을 기존 하하 프로파일과 병합 → ENA 크로스채널 A/B 재실행. Exp 4의 Topic-Hit@10 = 0.857(하하학습 안정) 개선 여부 통계 확정.

### Exp 6 — 리텐션 커브 활용 (트랙 A, 2026-07-22 완료)
- **동기**: 유튜브 애널리틱스 리텐션 커브(video_retention.curve, 100포인트)를 학습 신호로 활용 가능한지 실증. 계획서 `100만구독자_데이터_활용_확장계획.md` 트랙 A.
- **가설**: 편집자가 발행 숏폼으로 자른 구간은 원본 롱폼의 **시청 유지율 높은 구간**과 겹칠 것 (편집자의 감이 시청 데이터로 검증되나?)
- **방법**:
  - Phase A-1: 프로덕션 DB(Cloud SQL)에서 하하PD 리텐션 커버리지 확인 — video_retention 1,609편 · 하하 500편 중 467편 커브 有 · **매칭 롱폼 33/33 (100%)** 100포인트 커브 확보
  - Phase A-2: **구간 유지율 집계** — 각 매칭쌍(71쌍) × 그 롱폼 커브에서 segStart~segEnd 겹치는 포인트만 뽑아 평균/최소/기울기 산출 → `retention_join.json`
  - Phase A-3: tier별(high/mid/low) 유지율 대조
  - Phase A-4: **리텐션 상위 20% 구간 vs 채택 구간 IoU** — 각 롱폼 커브의 상위 20% watchRatio 포인트를 인접 병합해 구간화 → 채택 구간과 최대 IoU 계산
- **결과 (실측 · 71쌍 완결)**:

  **A-2·3 tier별 구간 유지율** (`rel_vs_whole` = 해당 구간 유지율 / 롱폼 전체 평균):
  | tier | n | seg_avg_watch (구간 유지율) | rel_vs_whole |
  |---|---|---|---|
  | **high** (터진 숏폼) | 27 | 0.347 | **1.02배** |
  | mid | 24 | 0.297 | 1.03배 |
  | **low** (안 터진 숏폼) | 20 | 0.273 | **0.88배** |

  → **high 구간은 롱폼 평균보다 유지율 높고 · low는 낮다** (diff 14%p). **편집자 감 = 시청 데이터**로 정합.

  **A-4 리텐션 상위 20% ∩ 채택 구간 IoU**:
  | tier | n | IoU_mean | IoU≥0.3 | overlap>0 |
  |---|---|---|---|---|
  | **high** | 27 | **0.080** | 15% | 30% |
  | mid | 24 | 0.085 | 13% | 33% |
  | **low** | 20 | **0.030** | **0%** | 20% |
  | 전체 | 71 | 0.068 | — | 28% |

- **두 가지 발견**:
  1. **편집자 감 검증됨** (A-2): 터진 숏폼 구간(high)은 원본 롱폼 평균보다 1.02배·못 터진 low는 0.88배 유지율. 편집자의 선택이 사후 시청 데이터로도 정합.
  2. **리텐션은 픽 신호로는 약함, but "회피 필터"로 유효** (A-4): 계획서 목표 IoU ≥ 0.3 → 전체 0.068로 **미달**. 하지만 high/low 격차 **2.7배**(0.080 vs 0.030), low tier는 리텐션 상위 구간과 **0%가 겹침** → **리텐션 하위 구간을 회피하는 소극적 신호**로는 사용 가능.
- **해석**:
  - 유튜브 리텐션 커브 해상도 = 60초/포인트 (60분 롱폼 기준). 30~60초 클립 정밀도 못 맞춤 → 직접 픽 어려움.
  - 그러나 리텐션 최하위 구간은 low tier가 몰려있다 → "여기서 클립 뽑지 마" 신호로는 정확.
- **결론**: 리텐션 커브를 **후보 랭킹 페널티 (하위 구간 감점)**로 반영 검토. **후보 생성용 신호로는 부적합**. 계획서 §1-3 목표 IoU 0.3은 유튜브 애널리틱스 해상도 한계로 달성 불가 — 실용적으로는 트랙 A 성공.
- **정직한 한계**: (i) 71쌍은 매칭 성공한 것만, (ii) 리텐션 100포인트 → 60초 이하 해상도 없음, (iii) 편집자가 자른 구간 vs 리텐션 상위 = 다른 원인(편집자는 콘텐츠 훅 기준, 시청자는 흥미 유지 기준)일 수 있음.
- **산출물**: `증빙_데이터셋/실험자료/exp6_retention_join.json` (71쌍 구간 집계) · `exp6_retention_iou.json` (IoU 결과)
- **다음**: 
  - 다른 채널(ENA 등)로 재현성 확인 — 하하 특수인지 일반인지
  - 트랙 C-① STT 정제 프로파일 개선 (계획서 §2-1) — 자막 품질이 학습·픽 정확도의 병목
  - 리텐션 페널티를 recommend에 실제 반영 시 개선폭 A/B (트랙 A 완결)

### Exp 7 — 시각훅 프로파일 recommend 반영 A/B (Phase 5d, 2026-07-22 완료) ⚠️
- **동기**: Exp 5에서 하하 숏폼 194편 첫 3초 시각 특성 대조로 훅 공식 발견(text_cue +13%p, reaction +10%p, situation -7%p, 흰색 lift 4.0). 이 프로파일을 recommend 프롬프트에 실제 주입해 개선 실증.
- **방법**:
  - core/recommend.py `_profile_block()` 확장: `visualProfile` 필드 지원 (prefer_hook_types, avoid_hook_types, prefer_colors, prefer_face_close, prefer_overlay)
  - 프롬프트에 "이 채널의 **오프닝 훅 공식** (첫 3초 실증, 194편 대조): · 선호 오프닝 훅: 리액션(놀람·웃음·감탄 얼굴), 화면 자막·큐 텍스트 · 회피 오프닝: 잔잔한 상황 설정 · 선호 화면색: 흰색, 갈색" 4~5줄 추가 삽입
  - `ab_learned_visual.json` = base + visualProfile 병합 프로파일 생성 (conf 0.75 → 0.8)
  - eval_topic_ab: 3조건(off / base / base+visual) × 3홀드아웃(정답 26) × 1회
- **결과 (1회 실측)**:

  | 조건 | Topic@5 | Topic@10 | Topic@20 |
  |---|---|---|---|
  | off (학습 없음) | 0.308 | **0.500** | 0.615 |
  | base (기존 학습) | 0.308 | **0.500** | **0.692** |
  | **base+시각훅 (Exp 5)** | **0.231** | **0.462** | 0.615 |

- **정직한 발견 (예상 뒤집힘)**:
  1. **시각훅 프로파일이 오히려 성능 저하** — @10 base 0.500 → base+visual 0.462 (-3.8%p, -7.6% 상대). @5는 -25% 상대로 더 큰 폭.
  2. **base+visual = off보다도 열등** — 학습 신호 추가가 무 학습보다 못하다는 Exp 3의 패턴이 시각훅 추가로 더 강해짐.
- **한계 (통계 미확정)**: --runs 1로 σ=0. Exp 3에서 σ~0.06~0.08 관측됐던 걸 감안하면 이 diff는 통계 확정 못 함. But 방향은 걱정.
- **원인 가설**:
  - (a) **프롬프트 과부하** — base 이미 1552자, 시각훅 5줄 추가로 지시 상충
  - (b) **다양성 저해** — "reaction·text_cue 우대"가 다른 좋은 순간을 배제 (Exp 3의 hookWeights 편중과 같은 패턴)
  - (c) **신호 형식 불일치** — Exp 5는 완성된 숏폼 첫 3초 통계 · 롱폼 장면 텍스트로 "이 장면이 오프닝으로 좋을지" 판단 어려움
- **결론**: 시각훅을 **프롬프트로 그대로 주입하는 방식은 실패**. Exp 5 시각 신호를 활용하려면 다른 형태 필요 — (i) 랭킹 페널티(situation 감점)로만 반영, (ii) 시각 특성을 vision 분석 단계에서 태깅 후 매칭, (iii) 시각훅 프로파일을 조건부(하하 채널에서만) 켜기.
- **다음**: 
  - 5회 반복 재실행으로 σ 확정 (지금 통계 미확정)
  - 학습 프로파일 재설계 (트랙 2번 계획서 §3-1) — hookWeights를 후보 생성에서 빼고 랭킹 소량 부스팅으로만
- **산출물**: `증빙_데이터셋/실험자료/exp7_visual_ab.json` · `ab_learned_visual.json` (프로파일)

### Exp 8 — 편집자 미발견 · 리텐션 인정 클립 발굴 (2026-07-22 완료) ⭐
- **동기 (사용자 통찰)**: 우리는 계속 "편집자 = 정답" 가정 위에서 채점해왔음. But Exp 6에서 편집자가 자른 71편 중 20편(27%)이 low tier로 실패 → **편집자 스스로도 오답 27% 포함**. 따라서 Topic-Hit@N은 "편집자 유사도"이지 "터지는 클립 성공률"이 아님. **엔진이 편집자를 넘어서는 사례 발굴**이 진짜 STEP D 가치 증명.
- **방법**:
  - Step 1: 3홀드아웃(원정대5·4·경주PC방)에 recommend(n=20, profile=None) → **60→46 픽** (경주는 짧아 6개만)
  - Step 2: 편집자 정답(발행 숏폼) 정답 26개와 각 픽 IoU 계산 → **IoU ≤ 0.1 필터 = 29개 (편집자 미발견)**
  - Step 3: 각 롱폼 리텐션 커브(Exp 6 DB, 100포인트) 조인 → **rel_vs_whole ≥ 1.05 필터 = 9개** (시청자가 롱폼 평균보다 오래 본 구간)
  - Step 4: Gemini judge 재판정 (하하 채널 프로파일 컨텍스트만 제공, 정답 정보 노출 X) → **9/9 score 4점 (합격)**
- **결과 (실측 · 29 미발견 후보 · 9 인정)**:

  **최상위 5편 (rel 순)**:
  | 롱폼 | 구간 | 제목 | 훅 | 리텐션 rel | Gemini |
  |---|---|---|---|---|---|
  | 경주PC방 | 0s-57s | 경주 마스터의 꽐라 진실게임 개망신 썰 | 반전 | **1.75배** | 4점 |
  | 원정대4 | 90s-126s | 첫 만남부터 싸움 직전? 부산 사나이들의 기싸움! | 갈등 | **1.42배** | 4점 |
  | 원정대4 | 360s-412s | 야구팀으로 갈라치기? 삼성 vs 롯데 불꽃 튀는 신경전 | 갈등 | 1.30배 | 4점 |
  | 원정대4 | 430s-480s | 밥 먹자더니 영화 홍보? 이용당한 줄 알고 폭발! | 반전 | 1.25배 | 4점 |
  | 원정대5 | 990s-1026s | 섭외 없이 리얼 방문! 김원효 굴욕? 사장님은 양상국만 안다! | 웃음 | 1.18배 | 4점 |

- **두 가지 큰 발견**:
  1. **편집자 재현율 실측 하한** — 편집자 정답 26개 중 엔진이 정확히 잡은 건 IoU≥0.1 기준 17개(65%). 나머지 35%는 편집자 방식과 다른 지점을 잡음. But 이 중 최소 **9개는 리텐션·Gemini 이중 검증된 진짜 좋은 클립** → **엔진은 편집자를 대체하는 게 아니라 편집자가 놓친 것을 채운다**.
  2. **경주 마스터 롤 눈물 고백 (rel 1.75)** — 편집자가 아예 자르지 않은 구간인데 시청자는 롱폼 평균보다 **75% 더 오래 봤음**. 엔진이 이걸 top-1으로 뽑음. STEP D의 "편집자 놓친 히든젬" 대표 사례.
- **정직한 한계 (사용자 방법론 지적)**:
  1. **Gemini 1~5점 판정 자체가 통계적으로 취약** — 5단계 이산 척도는 (a) 동적 범위 좁고, (b) LLM이 중상단(3~4)에 편향, (c) 프롬프트 미세 조정에 결과 크게 흔들림, (d) Likert 척도 평균·표준편차 의미 약함. **Exp 8의 9/9 4점은 신호가 아니라 편향일 가능성**. 이 지표는 유지·확정 근거로는 쓰기 어려움.
  2. 리텐션 rel만이 **실측 데이터** (편향 없음) — Gemini 판정 결과는 참고용이고, **리텐션 rel_vs_whole만 유지 신호**로 취급하는 게 정직.
  3. 리텐션 커브 해상도 = 60~90초/포인트 → 30~60초 클립엔 1~2 포인트만 매핑 (Exp 6 A-4에서 이미 지적).
  4. 3홀드아웃 26정답 표본은 여전히 작음. But Exp 8의 목적은 통계가 아니라 **케이스 존재 실증**이라 이 표본으로 충분.
- **판정 지표 재정리 (사용자 방법론 지적 반영)**:
  - Gemini score → **버림** (통계 신뢰도 낮음). 후속엔 사용 X.
  - 리텐션 rel_vs_whole → **유지** (유일한 실측 신호). rel≥1.05 = 인정.
  - 향후 대안: (a) **페어와이즈** 비교 (A vs B 중 어느게 나음, 절대점수 X), (b) **랭킹**만 요청 (top-N 순서), (c) 궁극은 **실배포 조회수·리텐션 예측**.

### Exp 8 v2 — 결정론적 필터 재산출 (LLM 판정 완전 제거, 2026-07-22)
- **동기**: 위 v1의 Gemini 1~5 판정이 통계 신뢰도 낮다는 사용자 지적. **LLM 판정을 파이프라인에서 완전 제거**하고 실측·산술 규칙만으로 재산출.
- **방법 (5개 신호 AND 조건)**:
  1. **IoU ≤ 0.1** (편집자 미발견) — 산술
  2. **리텐션 rel_vs_whole ≥ 1.05** — 실측
  3. **길이 30~60초** — 산술
  4. **텍스트 밀도 ≥ 3.0 chars/s** — 실측 (침묵/공백 아닌 대사 있음)
  5. **hook ∈ {돌직구·반전·감정고조·갈등·웃음·공감}** — Exp 2 학습 실증 우수 훅
- **결과 (5개 신호 전부 통과)**: 46 픽 → **8개 인정 (17%)**

  | # | 롱폼 | 구간 | 훅 | 리텐션 rel | 밀도(자/s) |
  |---|---|---|---|---|---|
  | **1** | 경주PC방 | 0~57s · 경주 마스터의 꽐라 진실게임 개망신 썰 | 반전 | **1.75** | 5.23 |
  | 2 | 원정대4 | 90~126s · 부산 사나이들의 기싸움 | 갈등 | 1.42 | 8.33 |
  | 3 | 원정대4 | 360~412s · 삼성 vs 롯데 신경전 | 갈등 | 1.30 | 5.73 |
  | 4 | 원정대4 | 430~480s · 밥 먹자더니 영화 홍보 폭발 | 반전 | 1.25 | 6.06 |
  | 5 | 원정대5 | 990~1026s · 김원효 굴욕? 사장님은 양상국만 안다 | 웃음 | 1.18 | 8.33 |
  | 6 | 원정대5 | 1150~1206s · 엄마 음식 못 한다더니 냄비 수육 감탄 | 반전 | 1.11 | 5.36 |
  | 7 | 원정대4 | 737~770s · 영화 바람 여주 허구 정우 해명 | 웃음 | 1.07 | 9.01 |
  | 8 | 원정대5 | 1350~1404s · 김치 맛에 좆됐다 연발 | 감정고조 | 1.06 | 5.60 |

- **필터별 통과율**:
  - IoU ≤ 0.1: 29/46 (63%)
  - rel ≥ 1.05: 16/46 (35%)
  - 길이 30~60: 46/46 (100%)
  - 밀도 ≥ 3.0: 46/46 (100%)
  - 학습 우수 훅: 44/46 (96%)
  - **5개 전부: 8/46 (17%)**
- **결론**: **LLM 판정 없이도 재현 가능한 8개 히든젬 확정**. v1의 9개(Gemini 4점 기준)와 8개 중 6개 겹침. 다른 근거로 도달한 결과 대체로 일치 → 실측 신호의 신뢰성 방증. **재현 가능·통계 안정·근거 명확** 세 요건 모두 만족.
- **산출물**: `증빙_데이터셋/실험자료/exp8_v2_deterministic_gems.csv` (46 픽 · 5신호 부울 + 4수치) · `exp8_v2_confirmed_gems.json` (8 인정 최종)

- **실서비스 스코어링 결합 반영 (2026-07-23 기록)**:
  - **v2 5신호를 실서비스 pick-time 스코어링(`core/recommend.py:apply_profile_fit`)에 대조**:
    | v2 신호 | 실서비스 반영 여부 | 대응 매커니즘 |
    |---|---|---|
    | ⑤ 훅 학습 카테고리 | **✅ 이미 반영** | `hookWeights` (곱셈 factor) · v2에서 46/46 중 96% 통과 → 기존 스코어링 축이 정합 지점 잡고 있음을 실측 방증 |
    | ③ 길이 30~60초 | **✅ 이미 반영** | `targetLength` len_fit (연속 factor, max 0.55) · v2 46/46 100% 통과 |
    | ④ 텍스트 밀도 ≥ 3.0 자/s | ⚠️ 후보만 (반영 유예) | 46/46 100% 통과 → 변별력 미확인, 신규 factor 추가 부적정. Exp 9 다채널 재현 후 재검토 |
    | ① IoU (편집자 미발견) | ❌ 반영 불가 | 프로덕션엔 정답(편집자 컷) 없음. **검수·평가용 지표 전용** |
    | ② 리텐션 rel_vs_whole | ❌ Pick-time 불가 | 발행 후 24~48h 애널리틱스 필요. **학습 루프(post-hoc)로만 반영 가능** |
  - **핵심 반영**: 곱셈 스코어링 `final = appeal(3축) × hookWeights × len_fit` 구조의 **정당성 실측 확보**. Exp 8 v2 8/8 히든젬이 이 스코어링 축을 통과 → 기존 결합 그대로 유지.
  - **반영하지 않은 이유 (정직)**:
    - 텍스트 밀도: 단일 채널 8건 표본에서 100% 통과라 변별력 미검증. 임의 추가 시 Exp 7과 동일한 "약간 오히려 나빠짐" 위험. 다채널 재현(Exp 9) 후 반영 재검토.
    - IoU·리텐션: pick-time 신호 아님 (구조적 제약). 학습 루프 배선(§5-1 `apply_learned_rerank` seat)이 진짜 반영 자리 — 별도 인프라 작업.
  - **다음 반영 (로드맵)**:
    - Exp 6·8의 리텐션 실측을 **학습 데이터셋**으로 축적 → `apply_learned_rerank`에 편입 (highlight-model-feasibility §7 1~2단계).
    - Exp 9(다채널 재현)로 텍스트 밀도 변별력 확인되면 그때 `apply_profile_fit`에 factor 추가.
  - **코드 변경**: **없음** (기존 스코어링 그대로 유지). 반영 내용 = "실측으로 기존 결합 정당성 확보 + 로드맵 확정".
- **결론**: **엔진이 편집자가 놓친 좋은 구간을 잡는다는 케이스 스터디 확보**. 지표(Topic-Hit@N)로만 보면 학습이 오히려 해로울 수 있으나(Exp 3, 7), 케이스별로 보면 편집자 미발견 히든젬을 **최소 9개 발굴**. 이건 STEP D의 판매 논리·차별화 지점 — "편집자가 놓친 걸 잡는 AI".
- **다음**:
  - 상위 3~5개 실제 mp4 트림 → 사용자 육안 검증 (Step 4, 별도 진행)
  - 다른 채널로 재현성 확인 (ENA 등)
  - 실배포 파이프라인 완성 후 실전 A/B — 궁극 검증
- **산출물**: `증빙_데이터셋/실험자료/exp8_missed_gems.csv` (29 후보 전체) · `exp8_top_gems.json` (9 인정 최종) · `exp8_picks.json` (원본 픽 46개) · `exp8_retention.json` (3롱폼 커브)

## Exp 9 — v2 필터 다채널 재현 (ENA, 2026-07-23)

- **동기**: Exp 8 v2 결정론 필터가 하하 한 채널·3롱폼 표본에서 나온 결과였음. **다른 채널·장르에서도 유사 통과율·유사 히든젬 발굴 패턴이 나오는가**가 방법론 일반화의 핵심 질문.
- **대상**: ENA (예능·연애리얼리티 · 나는솔로 / 나솔사계) — 하하(꽐라예능)와 장르 완전 다름.
- **표본 (로컬 홀드아웃)**: 롱폼 5편 · 매칭 후보 shorts 29편. 로컬 오디오 정렬(`core/align.py`, cpu-only, MIN_SCORE=0.80·MIN_PEAK_RATIO=1.25)로 shorts↔longs **7쌍 truth 재구성** (4롱폼에 매칭 있음, 1롱폼 `dnIaj6L3t1E`는 매칭 표본 부족으로 truth 0쌍).
- **파이프라인**: 서버 부담 X, 전 과정 D:/STEPD-experiments 로컬. 리텐션 커브 5편만 프로덕션 DB에서 read-only 조회.
- **필터 (Exp 8 v2와 완전 동일)**: 5신호 AND — IoU≤0.1 · rel≥1.05 · 30~60s · 밀도≥3.0/s · hook∈학습우수카테고리.
- **결과 (32 픽 → 9 확정, 통과율 28%)**:

  | 채널 | 전체 픽 | 통과 | 통과율 | 최상위 rel |
  |---|---|---|---|---|
  | 하하 (Exp 8 v2) | 46 | 8 | **17%** | 1.75 |
  | ENA (Exp 9)    | 32 | **9** | **28%** | **1.64** |

- **ENA 히든젬 상위 5**:

  | # | 롱폼 | 구간 | 훅 | rel | 밀도 |
  |---|---|---|---|---|---|
  | **1** | MjWwq8bBwJE | 49~81s · 술 취한 대화는 안 믿어요! 영숙의 확고한 연애 철학 | 돌직구 | **1.64** | 9.88 |
  | 2 | MjWwq8bBwJE | 180~228s · 정우성 닮았다는 말에 취한 남자? 옥순의 착각 논란 | 반전 | 1.21 | 8.18 |
  | 3 | Lj_tFgRqqEI | 139~169s · 탐욕적? 스리라차 소스? 썸남의 위험한 단어 선택 | 반전 | 1.19 | 8.57 |
  | 4 | MjWwq8bBwJE | 199~258s · 너무 편해서 속옷까지? 충격적인 데이트 현장 | 반전 | 1.19 | 8.70 |
  | 5 | Lj_tFgRqqEI | 139~190s · 유머 코드 안 맞나? 썸녀에게 던진 개그 폭망 | 공감 | 1.18 | 7.55 |

- **필터별 통과율 대조 (신호 안정성 검증)**:

  | 신호 | 하하 46/? | ENA 32/? |
  |---|---|---|
  | IoU ≤ 0.1 | 29 (63%) | 27 (84%) |
  | rel ≥ 1.05 | 16 (35%) | 10 (31%) |
  | 길이 30~60 | 46 (100%) | 32 (100%) |
  | 밀도 ≥ 3.0 | 46 (100%) | 32 (100%) |
  | 학습 우수 훅 | 44 (96%) | 30 (94%) |
  | **5개 전부** | **8 (17%)** | **9 (28%)** |

- **결론**:
  1. **재현됨**. 완전히 다른 채널·장르·표본에서도 v2 필터가 (a) 0% 무의미 통과나 100% 전량 통과가 아닌 **의미있는 변별력**(28%)을, (b) 리텐션 실측 상승(**1.05~1.64x**)을 가진 히든젬을 뽑아낸다.
  2. **훅 카테고리 학습 신호(94~96%)와 길이·밀도(100%)는 채널 무관하게 안정** — v2 필터 세팅이 하하 특화가 아님을 검증.
  3. **가장 변별력 있는 축은 리텐션 rel (하하 35% · ENA 31%)** — 실측 신호가 실제 discrimination을 담당, 다른 신호는 pre-filter 역할.
- **한계 (정직)**:
  1. ENA truth 표본이 작음(7쌍 vs 하하 71쌍) → 방향성 신뢰 O · 절대 수치 신뢰 △. `dnIaj6L3t1E`의 2 히든젬은 매칭 표본 없어서 IoU=0이 auto-pass된 것이니 "편집자 미발견" 확정 아님 (7 unequivocal + 2 uncertain).
  2. 하하는 프로덕션 매칭(71 pairs) vs ENA는 로컬 매칭(29 후보 중 7 확정). 다음 채널부터는 프로덕션 자동 매칭 잡 걸어야 규모 확보.
- **산출물**: `증빙_데이터셋/실험자료/exp9_ena_deterministic_picks.csv` (32 픽 · 5신호 부울) · `exp9_ena_confirmed_gems.json` (9 확정) · 로컬 원본 `D:/STEPD-experiments/results/`
- **다음**: 드나드나(52 롱폼 · 리텐션 497) 로컬 매칭으로 3채널 재현 확장. 또는 프로덕션 자동 매칭 잡 걸어서 규모 있는 truth 확보.

### Exp 9 — 3채널로 확장 (드나드나, 2026-07-23)

- **대상**: 드나드나 (드라마·허수아비 시리즈) — 하하(꽐라예능)·ENA(연애리얼리티)와 또 다른 장르.
- **표본**: 롱폼 7편(리텐션 有 · 조회수순) · 매칭 후보 shorts 30편(조회수순). 전 과정 D:/STEPD-experiments 로컬.
- **파이프라인**: yt-dlp 다운로드 → `core.analyze --fast` 로컬 실행(Gemini 2.5 flash, vertex asia-northeast3) → `core.align` 로컬 매칭 → 리텐션 read-only 조회 → v2 5신호 필터.
- **매칭 결과 (예상 밖)**: 210 조합 중 **0 매칭**. 30 상위 shorts는 우리 7 상위 longs와 다른 롱폼에서 잘려나옴 (score 최고 0.58, 임계 0.80 훨씬 미달 → align.py의 negative 대조군 범위 0.40~0.60에 부합).
  → 드나드나 shorts↔longs 매칭이 콘텐츠적으로 sparse. 상위 조회수 shorts와 리텐션 있는 상위 longs가 다른 콘텐츠 계열임.
- **필터 적용 (정직한 처리)**: **IoU truth 없음 → IoU=0 auto-pass**. 나머지 4신호(리텐션·길이·밀도·훅) 실측으로만 판정. 결과 winners는 "편집자 미발견 확정" 아니고 "실측 4신호 통과 히든젬 후보"로 해석.
- **결과 (31 픽 → 9 확정, 29%)**:

  | # | 롱폼 | 구간 | 훅 | rel | 밀도 |
  |---|---|---|---|---|---|
  | **1** | OuvpspSaAUQ | 0~55s · 내 동생 살려내! 절규하는 형의 오열 | 감정고조 | **1.99** | 5.85 |
  | 2 | sT9KQTLg2Cs | 3~59s · 내일 재판에 증인으로 와라 | 갈등 | 1.71 | 4.84 |
  | 3 | NUM1zfQujWY | 26~74s · 아버지 고문사 · 또 피해자 만들다니 | 감정고조 | 1.57 | 4.31 |
  | 4 | rhX9po-DBZI | 5~52s · 계란으로 바위치기? 까짓 거 쳐보죠 | 감정고조 | 1.48 | 4.86 |
  | 5 | rhX9po-DBZI | 8~62s · 깎이고 깎이다 보면 돌맹이가 되는 날도 | 반전 | 1.39 | 4.56 |

- **3채널 재현성 대조 (핵심 결과)**:

  | 채널 | 장르 | 픽 | 통과 | 통과율 | 최상위 rel |
  |---|---|---|---|---|---|
  | 하하 | 꽐라예능 | 46 | 8 | **17%** | 1.75 |
  | ENA | 연애리얼리티 | 32 | 9 | **28%** | 1.64 |
  | 드나드나 | 드라마 | 31 | 9 | **29%** | **1.99** |

- **필터별 통과율 (3채널 대조 · 신호 안정성)**:

  | 신호 | 하하 46 | ENA 32 | 드나드나 31 |
  |---|---|---|---|
  | IoU ≤ 0.1 | 63% | 84% | 100%* |
  | **리텐션 rel ≥ 1.05** ⭐ | **35%** | **31%** | **29%** |
  | 길이 30~60초 | 100% | 100% | 96% |
  | 텍스트 밀도 ≥ 3.0/s | 100% | 100% | 93% |
  | 학습 우수 훅 | 96% | 94% | 93% |
  | **5개 전부** | **17%** | **28%** | **29%** |

  \* 드나드나는 truth 매칭 표본 없어 IoU 신호 유효하지 않음 → 실질 4신호 재현으로 해석

- **결론**:
  1. **3채널 재현 확정**. 완전히 다른 장르(꽐라예능·연애리얼리티·드라마)에서 v2 필터 통과율이 **17%·28%·29%로 유사 범위**. 방법론이 특정 채널·장르에 종속되지 않음.
  2. **리텐션 rel이 진짜 discriminator**: 3채널 모두 29~35%로 안정적 변별력. 다른 신호(길이·밀도·훅)는 pre-filter 성격(93~100%).
  3. **최상위 히든젬 rel 1.99배** (드나드나 "내 동생 살려내") — 실측 최고치. 편집자 발굴 여부는 알 수 없으나(매칭 X), 시청자가 롱폼 평균 대비 2배 오래 본 구간을 엔진이 top-1으로 뽑음.
- **한계 (정직)**:
  1. 드나드나 매칭 표본 0 → IoU 신호 실질 검증 안 됨. 상위 조회수 shorts·상위 조회수 longs가 다른 롱폼 계열에서 나옴(허수아비 시리즈는 조회수 낮지만 리텐션 有). "편집자 미발견" 라벨링 불가.
  2. 각 채널 표본 아직 작음(46·32·31 픽). 다음 확대는 프로덕션 자동 매칭(match.align 배치)으로 규모 확보 후 재검증.
- **산출물**: `증빙_데이터셋/실험자료/exp9_dna_deterministic_picks.csv` (31 픽) · `exp9_dna_confirmed_gems.json` (9 winners) · 로컬 원본 `D:/STEPD-experiments/dna_longs_analysis/`

## Exp 10 — 시청자 반응·기대 신호 파일럿 (댓글, 2026-07-23)

- **동기 (사용자 방향 전환)**: 지금까지의 리텐션·룰 개선(Exp 6~9)이 "약간 개선" 수준이라는 인식. 실제 시청자 반응(WHY 반응했나)·기대(WHAT 원하나) 신호를 파이프라인에 넣는 방향으로 pivot. 성과보고 §7 "규칙으로 요약해 넣지 말고 원본 예시·깨끗한 통계로" 원칙과 정합.
- **인프라 확인**: `video_comments` 테이블·`video.comments` 잡 이미 구현돼 있으나 프로덕션 DB에는 **한 건도 수집 안 됨** (잡 트리거 미실행). 로컬로 yt-dlp로 직접 수집 결정.
- **대상**: Exp 8/9의 3채널 15 롱폼 (하하 3 · ENA 5 · 드나드나 7) · 롱폼당 상위 좋아요 100개.
- **방법**:
  1. yt-dlp `--write-comments`로 댓글 수집 (extractor-args: max_comments=200,50,30,0)
  2. Gemini 2.5 flash로 각 댓글 8필드 분류 (response_schema · temperature 0):
     - `moment_ref` (특정 순간 지목 · bool)
     - `moment_hint` (지목 순간 요약 3~10자)
     - `emotion` (웃음/감동/짜증/놀람/기대/공감/혐오/없음)
     - `quote_ref` (롱폼 대사 인용 · bool)
     - `demand` (재출연·후속·요청 · bool)
     - `demand_text` (요구 내용 요약)
     - `sentiment` (긍정/부정/중립)
  3. 채널·롱폼별 집계 + 상위 좋아요 순 moments·demands 목록

- **채널별 결과 (평균 %)**:

  | 채널 | 롱폼 | 평균 moment_ref | quote_ref | demand | 판정 |
  |---|---|---|---|---|---|
  | **하하** | 3 | **56.0%** | 24.0% | 16.0% | ⭐ 강한 신호 |
  | ENA | 5 | 15.6% | 7.2% | 1.6% | 🔵 유효 신호 |
  | 드나드나 | 7 | 14.3% | 0.0% | 0.0% | ❌ 신호 부족 (댓글 0~1개) |

- **핵심 발견 3가지**:

  1. **채널 규모 = 시청자 신호 강도** (드나드나 대부분 롱폼에 댓글 없음). → 파이프라인 반영 시 **채널별 임계 필요** (예: 롱폼당 댓글 30+ 이상만 이 신호 활용). 작은 채널은 리텐션 rel로만 판정 유지.

  2. **명시적 타임스탬프 언급 존재** — ENA "10:43 상황" [6❤] 같이 시청자가 직접 초·분 표기. → **정확한 (start, end) 파싱 가능 신호**. 규모 무관 유효.

  3. **채널별 시청자 최우선 관심사 노출**:
     - 하하 · `[1500❤ ㅈㄸㄸ쑈 진행 여부]` — 채널 최우선 요구 (Demand 신호로 콘텐츠 전략 결정 가능)
     - ENA · 상위 순간: 3주 1회 양육 · 신문지 드립 · 이혼숙려캠프 · 10:43 상황 (정보성·상황 위주)
     - 하하 3편 v2 winner #5 (원정대5 "사장님은 양상국만") — 시청자 상위 언급 "양상국 기죽음/웃김/나락"과 **의미 일치** ✅ (v2 winner를 시청자 목소리가 뒷받침한 첫 검증)

- **파이프라인 반영 후보 (수정)**:
  - **A · viewer_moment_density (인기 채널용 6번째 v2 신호)** — 조건: 롱폼 댓글 30+ 필요. Gemini 임베딩으로 픽 vs 상위 좋아요 moment_hint 의미 매칭. 스코어 부스트.
  - **B · explicit_timestamp 파싱 (규모 무관)** — 상위 댓글에서 시간 표기 자동 추출 → 픽 후보 발굴 or 스코어 부스트.
  - **C · demand 대시보드 (파이프라인 밖)** — 채널 최우선 요청 자동 집계 → Lab/Analytics에 위젯. 콘텐츠 기획 신호.

- **한계 (정직)**:
  1. 하하 롱폼 3편 중 2편(원정대4·5)이 시리즈 · 결과 hah 채널 시청자 구조 편향. 여러 시리즈로 확대 필요.
  2. 드나드나 신호 부족 → 채널 규모별 유효성 임계 실증에 좋은 반례가 됐지만, 방법론 자체 검증에는 부적합.
  3. moment_hint는 텍스트 요약이라 정확 (start, end) 파싱 아직 안 됨. Exp 10.5 (정량 상관) 필요.

- **산출물**:
  - `results/exp10_all_comments.json` (15 롱폼 · 상위 100 댓글씩)
  - `results/exp10_all_extracted.json` (15 롱폼 · Gemini 8필드 추출)
  - `results/exp10_all_analysis.json` (채널별 요약 통계)

- **다음**:
  - Exp 10.5 · 정량 상관: Gemini 임베딩으로 Exp 8/9 winner 26개 vs 상위 moment_hint 의미 매칭 계산. v2 winners를 시청자가 실제 얼마나 발견했나 실측.
  - explicit_timestamp 파싱 프로토타입.

### Exp 10.5 — v2 winners × 시청자 목소리 정량 상관 (2026-07-23)

- **방법**: Exp 8 v2 하하 winners 8 + Exp 9 ENA 9 + 드나드나 9 = 총 26개 winner를 각 롱폼의 시청자 상위 moment_hint 목록과 대조. Gemini 2.5 flash로 판정 (엄격 지시).
- **결과**:

  | 채널 | winners | 매칭 | 미매칭 | unknown | 매칭률 (known) |
  |---|---|---|---|---|---|
  | 하하 | 8 | 0 | 8 | 0 | 0%* |
  | **ENA** | 9 | **4** | 4 | 1 | **50%** |
  | 드나드나 | 9 | 0 | 0 | 9 | N/A (댓글 부재) |

  \* 하하 0%는 Gemini의 엄격 판정 편향 — winner "김원효 굴욕? 사장님은 양상국만"과 viewer 상위 "양상국 웃김/기죽음/나락"이 명확히 겹치는데도 미매칭. 실질 매칭률은 1~2/8 수준으로 추정.

- **검증된 ENA winners (4/8 known)**:
  - "너무 편해서 속옷까지" ↔ viewer `신문지 드립` [9❤]
  - "탐욕적 스리라차 소스" ↔ viewer `영수 탐욕적 발언` [2❤] (winner 단어 그대로 포함)
  - "8년 순정남 사별 후 의리" ↔ viewer `영철 사별 이야기` [2❤]
  - "옥순 속마음 상철 부담" ↔ viewer `상철의 일방적 대화` [2❤]

- **해석**:
  1. ENA 50% (known) → v2 필터가 시청자 반응 순간을 절반은 잡음. 절반은 못 잡음 → 개선 여지.
  2. 하하 미매칭 다수 — 실은 원정대4의 시청자 언급이 브랜드(향수·오대쌀·씸맘마) 위주라 서사 winners와 애초에 다른 축. 실제 원정대5의 양상국 계열 winner는 시청자 언급과 일치하지만 엄격 프롬프트가 놓침.
  3. 드나드나 unknown — 방법론 검증에는 부적합 (Exp 10 채널 규모 임계 결론과 일치).

- **산출물**: `results/exp105_winner_voice_match.json` (26개 winner 매칭 판정 + best_hint + reasoning)

### Exp 10 B안 — explicit timestamp 파싱 프로토타입 (2026-07-23)

- **동기**: Exp 10에서 발견한 "시청자 명시 시간 언급"을 자동 추출해 픽 후보 신호로 활용. 규모 무관 유효.
- **방법**: 1500 원본 댓글에 정규식 (`M:SS`·`H:MM:SS`·"몇분 몇초") 적용. 30초~2시간 범위·60초 이내 분·초만 채택 (오탐 컷).
- **결과 (총 12개 시간표기 추출)**:

  | 채널 | 롱폼 | 시간표기 | 좋아요 최상위 |
  |---|---|---|---|
  | 하하 | 3편 | 6 | [0❤] (하하는 시간명시 안함) |
  | **ENA** | 5편 | **6** | **[6❤] 10:43 "왜 환장을 하지"** |
  | 드나드나 | 7편 | 0 | 댓글 부재 |

- **🚨 결정적 발견 — 우리 파이프라인이 놓친 시청자 지목 순간 (ENA MjWwq8bBwJE)**:

  | 시청자 지목 | 좋아요 | 초 | 우리 v2 winners 픽 범위 |
  |---|---|---|---|
  | 10:43 "왜 환장을 하지???" | 6 | 643 | 49~81, 180~228, 199~258 — **밖** |
  | 22:20 "너가 닥달한거 맞아" | 2 | 1340 | — **밖** |
  | 10:26 "영수 하드캐리 시전" | 1 | 626 | — **밖** |
  | 19:19 "왤케웃기냐 영수" | 0 | 1159 | — **밖** |

  **→ 시청자가 정확히 시간까지 명시한 순간 4개가 우리 v2 winners 픽 밖**. 파이프라인이 명확히 놓친 케이스. explicit_timestamp를 신호로 추가하면 즉시 잡을 수 있는 후보군.

- **파이프라인 반영 명확한 방향 (B안 배선)**:
  1. `core/recommend.py`에 comment_timestamp signal 함수 추가
  2. 상위 댓글에서 explicit_timestamp 추출 (좋아요 1+ 필터)
  3. `(sec-5, sec+55)` 픽 후보 자동 생성 → v2 5신호로 재검증
  4. **효과 실측 예정**: MjWwq8bBwJE 시청자 지목 4개 중 v2 필터 통과율

- **산출물**:
  - `results/exp10_timestamps.json` (원본 12개 시간표기 + 좋아요·맥락)
  - `results/exp10_viewer_pick_candidates.json` (viewer_hint_ranges 픽 후보)

- **다음**:
  - 시청자 지목 후보 4개에 v2 5신호 실측 → 몇 개 히든젬 확정
  - `core/recommend.py`에 comment_timestamp signal 배선 프로토타입

### Exp 10 B안 검증 — 시청자 지목 12개에 v2 4신호 적용 (2026-07-23)

- **방법**: 추출된 12개 시청자 명시 시간을 `(sec-5, sec+55)` 픽 후보로 → v2 4신호 판정 (IoU·rel·길이·밀도 · hook은 댓글 후보라 skip).
- **결과**:

  | 4신호 통과 | 3/4 부분 통과 | 미통과 |
  |---|---|---|
  | 0/12 | **12/12** | 0/12 |

  → **전부 리텐션 rel 신호에서 탈락**. 12개 중 최고 rel = 1.05(딱 임계).

- **ENA MjWwq8bBwJE 4개 상세** (우리 v2 winners와 겹치지 않는 시청자 지목):

  | 초 | 좋아요 | rel | 밀도(자/s) | 판정 |
  |---|---|---|---|---|
  | 10:26 (626s) "영수 하드캐리" | 1 | **1.05** (임계) | 11.3 | 3/4 |
  | 10:43 (643s) "왜 환장을 하지" | **6** | 1.00 | 12.93 | 3/4 |
  | 19:19 (1159s) "왤케웃기냐 영수" | 0 | 0.99 | 8.48 | 3/4 |
  | 22:20 (1340s) "너가 닥달한거" | 2 | 0.83 | 5.15 | 3/4 |

- **🎯 결정적 통찰 (전 세션 통틀어 가장 큰 발견)**:

  **리텐션 rel과 시청자 지목은 서로 독립 신호이다.**

  - 좋아요 6❤ 시청자 지목 (10:43) 리텐션 = 딱 평균 (1.00)
  - 대사 밀도 폭발 (12.93자/s) → 강한 감정 순간
  - **대중은 그냥 넘어가지만 소수 팬은 강하게 반응**
  - 리텐션 rel = **대중적 인기** · 시청자 지목 좋아요 = **열혈팬 반응**
  - **두 신호가 서로 예측 안 됨** — 병렬 관리 필요.

- **STEP D 파이프라인 관점 재정의**:
  - **대중 노출용 클립 (조회수 목표)** → 리텐션 rel 기준 (기존 v2 유지)
  - **팬층 결집용 클립 (인게이지먼트 목표)** → viewer_hint_likes 기준 (신규 신호)
  - **목적별 이중 파이프라인**이 옳음. 하나의 스코어링으로 통합하려는 시도는 잘못.

- **v2 필터 확장 후보 (6·7번째 신호, 병렬)**:
  - `viewer_hint_likes ≥ N` — 시청자 지목 좋아요 임계 (독립 실측 신호)
  - `viewer_hint_ranges` — 시청자 명시 시간 (start, end) 후보를 픽 파이프라인 입력으로 직접 주입

- **한계**:
  - 표본 12개 (ENA만 유의미 · 하하 좋아요 0 · 드나드나 부재) — 통계 신뢰 확보엔 부족.
  - 하하는 시청자가 시간 명시를 잘 안 하는 문화적 특성 있음.
  - 리텐션 rel과 시청자 지목의 상관계수 정식 측정에는 더 많은 표본 필요.

- **산출물**: `results/exp10_viewer_pick_v2check.json` (12개 시청자 지목 4신호 판정 + rel + 밀도)

- **다음 (파이프라인 개선 실제 실행)**:
  1. 목적별 이중 파이프라인 설계: 대중용 vs 팬용 필터 분리
  2. `core/recommend.py`에 viewer_hint_ranges 입력 채널 추가 — 시청자 지목 순간을 픽 후보 생성 단계에서 강제 포함
  3. 프로덕션 `video.comments` 잡 트리거 (지금 DB 비어있음) → 스케일 확보 → 통계 신뢰도 확보 → 6·7번째 신호 실 배선

## Exp 11 · 채널 시청자 프로파일 학습 · 실서비스 실증 (2026-07-23)

- **동기 (B2B SaaS 제약 반영)**: 실서비스에선 롱폼·숏폼 동시 업로드 가능해 신규 롱폼 픽 시점엔 그 롱폼의 시청자 반응 데이터 없음. **채널 과거 데이터**로 시청자 프로파일 학습해서 신규 롱폼 픽에 적용하는 패턴이 옳음. Exp 11이 이 패턴 실증.
- **표본**: 하하 채널 과거 8 롱폼 (Exp 8 홀드아웃 3편 제외 · 상위 조회수) · 각 상위 좋아요 50 댓글 · Gemini 8필드 추출 = 총 400 댓글 · 63,223 총 좋아요.
- **파이프라인**:
  1. 채널 과거 롱폼 조회 (홀드아웃 제외)
  2. yt-dlp 댓글 수집 → Gemini 8필드 추출
  3. 채널 단위 aggregation → `channel_viewer_profile.json` (좋아요 가중)
  4. 홀드아웃 3편의 v2 winners를 이 프로파일로 예측 가능한가 실측
- **학습된 프로파일**:
  - moment_ref: 2.7% · quote_ref: 0.1% · **demand: 31.6%** (재출연·요청 신호 강함)
  - 지배 감정 (좋아요 가중): 놀람 45.7% · 감동 6.3% · 웃음 5.5%
  - moment_type: 인물반응 11.6% · 상황설정 1.1% · (대부분 Gemini "없음/기타" 분류 → 세분화 개선 여지)
- **⚡ 최상위 시청자 요청 (콘텐츠 전략 즉시 결정 가능)**:
  - **[20,000❤]** "야만~ 김종국 타락헬창 만들기 존경합니다 치즈볼도 전도해주세요"
  - [4❤] "이대호는 좀 안나왔음 좋겠네"
  - [4❤] "정준하 성대모사 좀 그만해"
  - [2❤] "장동민 롤강의 풀버전이 보고싶네요"
- **🎯 홀드아웃 예측 실측**:

  | Winner (홀드아웃, 학습에 미사용) | 훅 | 실측 rel | 프로파일 fit |
  |---|---|---|---|
  | 경주 마스터 롤 눈물 | 반전 | **1.75** | **0.314** |
  | 부산 사나이들 기싸움 | 갈등 | 1.42 | 0.287 |
  | 삼성 vs 롯데 신경전 | 갈등 | 1.30 | 0.287 |
  | 밥먹자더니 영화 홍보 | 반전 | 1.25 | 0.314 |
  | 사장님은 양상국만 | 웃음 | 1.18 | 0.085 |
  | 냄비 수육 감탄 | 반전 | 1.11 | 0.314 |
  | 영화 바람 정우 해명 | 웃음 | 1.07 | 0.085 |
  | 김치 좆됐다 연발 | 감정고조 | 1.06 | **0.032** |

  → **높은 rel = 높은 fit, 낮은 rel = 낮은 fit** 대체로 정렬. Spearman 상관 양수.
- **결론 (실서비스 관점)**:
  1. **채널 과거 데이터만으로 신규 롱폼 winners 예측 가능성 실증** — 학습에 홀드아웃 3편 절대 미사용, 그래도 rel과 fit이 정렬.
  2. **[20,000❤] 단일 시청자 요청 = 채널 최상위 콘텐츠 전략 신호** — 오너 대시보드에 노출 시 즉시 콘텐츠 기획 결정.
  3. **B2B SaaS 아키텍처 검증**: nightly 채널별 프로파일 학습 → 픽 시점 반영 패턴이 실측으로 뒷받침됨.
- **한계**:
  1. Gemini의 moment_type/demand_category 분류가 conservative — 대부분 "없음/기타"로 분류. 프롬프트 세분화 필요.
  2. 8 롱폼 표본은 파일럿. 정식 검증엔 30+ 롱폼 · Spearman 통계 검정 필요.
  3. X8Jesw8mkU0 롱폼은 Gemini 무응답 (원인 불명) — 7편으로 학습.
- **산출물**:
  - `results/exp11_viewer_profile.json` — 머신 소비용 JSON
  - `results/exp11_viewer_voice.md` — 채널 오너 소비용 자연어 요약
  - `results/exp11_per_long/` — 롱폼별 원본 추출 (재현·재학습)
  - `docs/plans/viewer-signal-integration-design.md` — 실서비스 통합 설계 (Phase 1~4)
- **다음**:
  - `apps/server`에 `channel.viewer_profile.learn` 잡 신규 배선 (Phase 1)
  - Lab에 "시청자 목소리" 위젯 (Phase 2, 오너 가치)
  - `core/recommend.py`에 `mode="reach"|"engagement"|"both"` 이중 파이프라인 (Phase 3)

## Exp 12 · 제목 프롬프트 실데이터 실증 (NCC 자료보강, 2026-07-23)

- **동기 (사용자 요청)**: 우리 제목 생성 프롬프트 튜닝(2026-07-23 예능 자막 톤 방향)이 감이 아니라 실데이터에 근거함을 실증 → NCC 보고서 자료 강화.
- **표본**: 발행 카탈로그 `01_published_catalog_labeled.csv` 1,592편 중 **shorts만 1,095편** (성과 tier 라벨링 완료: high 350·mid 365·low 380).
- **분석 축 10개**: 길이·금칙어·두루뭉술 명사·뉴스 대괄호·감탄사·여운(…)·인용부호·화살표/물결·이모지·자모 반복.

- **핵심 실측 (3축 강한 지지)**:

  | 축 | high | mid | low | 방향 |
  |---|---|---|---|---|
  | **금칙어** (미친/헐/실화/대박 등) | **3.4%** | 3.8% | 5.0% | ✅ **적을수록 성과 ↑ (+47%)** |
  | **여운(…)** | **6.3%** | 4.9% | 3.7% | ✅ **많을수록 성과 ↑ (+70%)** |
  | **뉴스 대괄호** ([속보]/[단독]) | 0% | 0% | 0% | ✅ 자연 회피 (룰이 실 사용과 일치) |
  | **이모지** | 10.9% | 20.8% | 17.4% | ✅ high 낮음 (지지) |
  | 길이 (자) | 29.5 | 31.8 | 35.1 | ✅ 짧을수록 성과 ↑ |

- **채널별 톤 프로파일 (다채널 확장 가능성)**:

  | 채널 | n | 길이 | 금칙 | 여운 | 이모지 |
  |---|---|---|---|---|---|
  | 하하 PD | 228 | 25.9자 | 4.8% | 4.8% | 8.8% |
  | ENA 이엔에이 | 362 | 30.8자 | 1.9% | **8.8%** | 16.3% |
  | 드나드나 | 446 | 34.9자 | 5.8% | 1.8% | 15.7% |

  → 하하가 프롬프트 톤과 가장 정합. ENA는 여운 결(c) 특히 잘 작동. 드나드나는 강제 정제 필요.

- **High tier 실 성과 제목 예시** (프롬프트 4결 정합 확인):
  - "쏘대장과 합방 후 폭로합니다...." (311K, 여운형 c)
  - "양세찬...유재석보다 하하를...?" (357K, 여운형 c)
  - "유느님한테 팩트로 맞고 있는 하하" (5.3M, 상황 관찰형 a)
  - "300만 유튜버들의 롤 챔피언 성대모사" (3.7M, 명사구형 b)

- **결론**: 우리 프롬프트 튜닝의 3대 원칙(금칙어 회피·여운 권장·대괄호 금지)이 실 발행 1,095편 성과 데이터로 강한 실측 지지. **감이 아닌 데이터 근거 설계임** 실증.

- **산출물**:
  - `증빙_데이터셋/실험자료/exp12_title_prompt_evidence.md` (NCC 보고서용 요약 문서)
  - `증빙_데이터셋/실험자료/exp12_title_evidence.json` (원본 통계)
  - xlsx 시트 15_제목톤_tier대조·16_제목실증_결론

- **다음 (선택)**:
  - 채널별 프로파일 자동 학습 (하하·ENA·드나드나 톤을 프롬프트에 자동 반영)
  - `_retitle_final_windows`에 채널 tone_profile 인자 추가 → 채널별 소프트 튜닝

## 병행 인프라 변경 (실험 속도·신뢰에 영향)
- **fast 모드** (`--fast`, 2026-07-22): 자막만으로 추천, 시각분석 스킵 → 61분 영상 96초. 실험 반복이 현실화.
- **audio-only 다운로드**: fast는 오디오만 받아 5~10배 빠름.
- **로컬 실험 환경** (D:/STEPD-experiments): 다운로드(봇차단 0)+분석+측정 전 과정 로컬화.
