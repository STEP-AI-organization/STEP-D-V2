# core 테스트 실행에만 필요한 것 — 프로덕션 이미지에는 안 들어간다.
#
# 테스트 103개는 **네트워크를 안 탄다**(원가 원장 불변식·chyron 정규화·클립 조합·
# 프롬프트 규칙·번역 감지…). 그래서 opencv·torch 같은 무거운 것 없이 돌릴 수 있고,
# CI 가 1분 안에 끝난다.
#
# ⚠️ 다만 **import 는 탄다.** 다섯 파일이 `core.stt.translate` 등을 거쳐
# `from google import genai` 에 닿는다 — 호출은 안 하지만 모듈이 없으면 수집 단계에서
# 죽는다(2026-09-07 CI 실측: ModuleNotFoundError 로 5개 파일이 통째로 실패).
# 그래서 google-genai 만 같이 깐다. 나머지 무거운 것들은 여전히 필요 없다.
#
# ## 왜 `==` 인가 (2026-09-11)
#
# 전엔 `pytest>=8.0` 이었다. 열어 두면 **CI 가 어느 날 갑자기 빨개진다** — 우리가 아무것도
# 안 바꿨는데 pytest 가 메이저를 올렸다는 이유로. 실제로 이 제약은 오늘 pytest **9.1.1** 을
# 끌어온다(8 이 아니다). 테스트 러너가 그날 PyPI 에 뭐가 올라왔느냐로 바뀌면, CI 가 빨개졌을 때
# "내 변경이 깬 건가, 도구가 바뀐 건가"를 매번 먼저 가려야 한다.
#
# 아래 조합은 **실측 검증했다**: python 3.11 + pytest 9.1.1 + google-genai 2.23.0 에서
# `pytest core/tests -q` → 103 passed, 12 subtests passed.
# google-genai 는 프로덕션 워커와 **같은 버전**으로 맞춘다(core/requirements.lock.txt).
#
#   설치:  pip install -r core/requirements-dev.txt
#   실행:  pytest core/tests -q      (리포 루트에서)

pytest==9.1.1
google-genai==2.23.0

# pytest 의 전이 의존성 — 여기까지 고정해야 "같은 커밋 = 같은 실행" 이 성립한다.
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
