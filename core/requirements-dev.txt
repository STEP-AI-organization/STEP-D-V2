# core 테스트 실행에만 필요한 것 — 프로덕션 이미지에는 안 들어간다.
#
# 테스트 87개는 **Vertex·GCS 없이 순수 함수만** 검증한다(원가 원장 불변식, chyron 정규화,
# 클립 조합, 프롬프트 규칙…). 그래서 requirements.txt 의 무거운 것들(google-genai·
# opencv 등)을 안 깔아도 CI 에서 돌릴 수 있다 — 그게 CI 가 30초에 끝나는 이유다.
#
#   설치:  pip install -r core/requirements-dev.txt
#   실행:  pytest core/tests -q      (리포 루트에서)
pytest>=8.0
